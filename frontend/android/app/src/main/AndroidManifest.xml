<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 장소 사진 등록(react-native-image-picker) — 카메라 촬영 + 앨범 선택 둘 다 지원.
         READ_MEDIA_IMAGES는 안드로이드 13(API 33) 이상, READ_EXTERNAL_STORAGE는 그 이하 대상. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
      android:name="android.permission.READ_EXTERNAL_STORAGE"
      android:maxSdkVersion="32" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- 카카오 로그인 OAuth 리다이렉트 수신용 — @react-native-seoul/kakao-login 필수 설정.
           android:scheme은 strings.xml의 kakao_app_key와 동일한 값이어야 함(자동 동기화 안 되니 두 곳
           다 함께 바뀌어야 함). 구글/네이버 로그인은 이 매니페스트에 별도 항목이 필요 없음(JS 설정만으로
           충분). -->
      <activity
        android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
        android:exported="true">
        <intent-filter>
          <action android:name="android.intent.action.VIEW" />
          <category android:name="android.intent.category.DEFAULT" />
          <category android:name="android.intent.category.BROWSABLE" />
          <data
            android:host="oauth"
            android:scheme="kakaoa454c7fda7f467f834dc4f614afe03dc" />
        </intent-filter>
      </activity>
    </application>
</manifest>
