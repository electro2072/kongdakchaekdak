# design/ — 디자인 시스템 & Hi-Fi 목업

> **📌 앱 이름 확정 (2026-08-27): "콩닥책닥"** — 책을 완독하고 기록할 때 심장이 콩닥콩닥 뛴다는 뜻의 "콩닥콩닥" + "책"에서 따온 이름입니다. 그동안 임시로 쓰던 "독서 자랑 어플"이라는 명칭은 각 산출물의 문서 제목(`<title>`/`<h1>`/footer)에서 "콩닥책닥"으로 교체했습니다. 앱 내부 화면에 실제로 노출되는 워드마크는 `hifi_mockup.html`의 Frame 01 로그인/온보딩 화면(라이트·다크)뿐이었고, 여기도 반영 완료(v1.6).
>
> **📌 앱 아이콘 전면 교체 (2026-08-27): 쑥송편 → "콩닥/책닥" 텍스트 워드마크** — 앱 이름 확정 직후 아이콘을 텍스트 기반으로 다시 만들고 싶다는 요청에 따라 `app_icon.html`이 v2로 교체되었습니다(쑥송편 그린 배경 + 크림 텍스트, "콩닥"/"책닥" 두 줄). **기존 쑥송편 아이콘(v1)은 삭제하지 않고 `app_icon_songpyeon_v1_archived.html` + `assets/icon/archive_songpyeon_v1/`에 그대로 보관** — 더 이상 실제 앱 아이콘으로 쓰이지 않을 뿐입니다.
>
> **📌 스플래시 화면 신규 확정 (2026-08-27)** — 그동안 보류 상태였던 스플래시 화면을 처음으로 만들었습니다. "① 심플 아이콘 중앙형"과 "② 풀블리드 워드마크형"(쑥송편 그린 배경 전체 + "콩닥/책닥" 워드마크 크게) 두 시안을 비교해 **②번으로 확정**(`splash_screen.html`) — 앱 아이콘과 동일한 색을 재사용해 아이콘→스플래시→앱 진입까지 색이 이어지도록 했습니다.
>
> **📌 보관용 파일 정리 (2026-08-27): `design/deprecated/` 폴더 신설** — 그동안 `design/` 최상위와 `assets/icon/archive_songpyeon_v1/`에 흩어져 있던 "보관용"(더 이상 활성 아님) 파일들을 `design/deprecated/` 밑으로 모았습니다. 대상: `app_icon_songpyeon_v1_archived.html`, `assets/icon/*.png` 3종. 내용은 전혀 바뀌지 않았고 위치만 정리됨 — 앞으로 다른 산출물이 폐기(archived)될 때도 이 규칙(삭제 대신 `deprecated/`로 이동)을 따릅니다. 참고로 "탐색용" 시안 문서(`app_icon_text_concept.html`, `splash_screen_concept.html`)는 폐기된 게 아니라 결정 과정 참고 자료라 이 폴더 대상이 아닙니다.
>
> **📌 iOS 다크·틴트드 아이콘 변형 추가 (2026-08-27): `app_icon.html` v2.1** — iOS 18의 홈 화면 아이콘 커스터마이징(라이트/다크/틴트) 대응. 다크는 배경을 기존 팔레트의 `--p900`(`#1e2903`)로 교체, 틴트드는 Apple 가이드에 따라 원본 색의 지각 휘도를 그대로 유지한 그레이스케일(`#848484`/`#f1f1f1`)로 제작. 레이아웃은 라이트 버전과 동일.
>
> **📌 Frame 01 온보딩 아이콘 수정 (2026-08-27): `hifi_mockup.html` v1.7** — 사용자가 Frame 01 로그인/온보딩 화면의 로고 자리가 여전히 범용 `{{icon:book-open}}` 글리프였음을 지적. 실제 확정된 앱 아이콘(쑥송편 그린 배경 + "콩닥"/"책닥" 크림 텍스트)을 그대로 축소 재현한 배지로 교체.
>
> **📌 태블릿·폴더블 대응 (Expanded 레이아웃) 추가 (2026-08-27): `hifi_mockup.html` v1.8** — "태블릿(아이패드) 환경 디자인이 필요 없냐" + "갤럭시 폴드도 나오는 세상" 질문에서 시작. 지금까지 모든 화면이 폰 폭 기준으로만 제작돼 있었음을 확인하고, "아이패드"가 아니라 **화면 폭 구간(600px 기준 Compact/Expanded)**으로 대응 범위를 잡기로 함(iOS 사이즈 클래스 / Android Material 3 window size class와 동일한 사고방식 — 아이패드와 폴드 펼침 상태를 같은 로직으로 커버). 콘텐츠가 많은 핵심 3개 화면(서재 목록·대시보드·일정 탭)만 실제 Expanded 레이아웃을 새로 그렸고, 나머지 폼/단일 컬럼 화면은 max-width+중앙정렬 규칙으로 처리(별도 목업 없음, 실제 구현 시 적용). 신규 `.tablet-frame` 프레임 클래스 추가.
>
> **📌 태블릿 레이아웃 재설계 (2026-08-27): `hifi_mockup.html` v1.9** — 사용자 피드백: "지금 태블릿용으로 만든 화면 진짜 다 못생겼다, 넓어지는 게 무슨 의미인지 먼저 알아야 할 거 같다." 정확한 지적이었음 — v1.8은 폰용 카드·차트를 그대로 두고 여백만 넓힌 수준이었고, 대시보드는 콘텐츠가 상단 40%에만 몰려 있었음. 세 화면 전면 재설계: 서재 목록은 **표지 중심 3열 서가 그리드**(6권), 대시보드는 **4-스탯 카드 전체 폭 + 확대된 도넛·막대 차트 + "최근 활동" 리스트**, 일정 탭은 **"다가오는 일정"·"최근 완독한 책" 섹션 추가**로 프레임 하단까지 실제 콘텐츠로 채움. 핵심 원칙: 넓은 화면은 여백이 아니라 "더 많이" 또는 "더 크게" 보여주는 데 써야 함.
>
> **📌 Frame 02 개선 2건 (2026-08-28): `hifi_mockup.html` v1.10** — ① "읽고 있는 책이 n개면 오른쪽으로 스크롤(터치 스와이프)" 요청 → 그리드/줄바꿈 대신 가로 스크롤로 처리하는 공용 `.hscroll` 클래스 신설, 폰·태블릿 모두 적용(폰은 2권→4권으로 늘려 스크롤 필요 상황 시연). ② "태블릿에서는 캘린더가 맨 위에 길게, 그 아래 좌='읽을 차례예요'+'일정 알림', 우='다가오는 일정', 새 책 등록하기는 전체 폭으로" 요청 → v1.9의 좌/우 2단 구성을 전면 재배치. 프레임 안에 다 들어가도록 재검증(오버플로 133px→9px).
>
> **📌 6개 장르 고정색 도입 (2026-08-28): `hifi_mockup.html` v1.11 + `콩닥책닥_디자인시스템_컬러팔레트.md` v3** — "6개 장르에 고정 색을 부여하는 걸 염두에 두고 있다"는 요청에 따라, 기존 4개 장르(소설·에세이·자기계발·인문, `--chart1~4`)만 있던 고정색 세트에 과학·경제·경영 2개를 신규 추가. `dataviz` 스킬 공식 검증기(색약 시뮬레이션 CVD 분리도 등)로 검증한 **청화블루**(과학, `--chart5`, `#0089ff`/다크 `#009af3`)와 **먹빛 남색**(경제·경영, `--chart6`, `#1c1a46`/다크 `#98bed3`) 확정 — 처음 시도한 "고서 갈색"은 색약 시뮬레이션에서 기존 그린과 분리도 미달로 탈락. 적용 위치 3곳: ① 장르 비율 도넛차트(등수 기반 "기타" 회색 버킷 제거, 4번째 구간을 인문/보라로 교체 — 6개 장르가 모두 고정색을 가지므로 "기타"가 더 이상 불필요), ② 관심분야·장르 선택 칩(Frame 01.1/01.2/05.2 — 구버전 하드코딩 잔재였던 자기계발/과학 색 오류도 함께 정정), ③ 서재 목록 책 태그(Frame 03 폰·태블릿에 장르 배지 신규 추가). 프론트 요청: `GenreDonutChart.tsx`의 등수 기반 색상 로직을 고정 매핑으로 변경 필요 — 별도 요청 문서 참고.
>
> **📌 앱 이름 표기 잔재 정리 (2026-08-28)** — 앱 이름이 "콩닥책닥"(영문 `kongdakchaekdak`)으로 확정된 지 오래됐지만, 두 군데에 옛 임시 명칭이 남아있던 것을 발견해 정정했습니다: ① `hifi_mockup.html`의 `<title>`/`<h1>`이 v1.6 이후로도 계속 "독서 기록 공유 앱"이었던 것(v1.6은 Frame 01 온보딩 화면 안의 워드마크 텍스트만 교체했을 뿐 문서 제목·헤딩은 놓쳤던 것), ② `branding_proposal_songpyeon.html`의 `<title>`이 "독서 자랑 어플"이었던 것. 둘 다 "콩닥책닥"으로 교체(화면 내용·색상·버전 번호는 변경 없음). 함께, claude.ai 프로젝트 문서함의 컬러 팔레트·타이포_아이콘·Hi-Fi 목업 3개 문서도 파일명 접두어를 `독서기록앱_` → `콩닥책닥_`으로 리네임했습니다(아래 문서 목록·산출물 표의 파일명도 갱신됨). 이 3개 문서를 옛 파일명으로 참조하던 다른 프로젝트 문서(개발현황·기획서 등)는 이번 범위에 포함되지 않았습니다.
>
> **📌 파일명의 "_v1" 접미사 일괄 제거 (2026-08-28)** — "왜 문서 이름을 안 바꾸고 계속 v1만 붙이냐"는 질문에, 실제로는 이 프로젝트에서 "_v1"이 "_v2"로 이어진 적이 한 번도 없어 의미 없는 고정 접미사였다는 걸 확인 → "그럴 바엔 차라리 지우자"는 결정에 따라 `design/` 폴더의 HTML 산출물 4개와 이름이 겹치는 claude.ai 프로젝트 문서 3개에서 "_v1"을 제거했습니다: `hifi_mockup_v1.html`→`hifi_mockup.html`, `branding_proposal_songpyeon_v1.html`→`branding_proposal_songpyeon.html`, `app_icon_text_concept_v1.html`→`app_icon_text_concept.html`, `splash_screen_concept_v1.html`→`splash_screen_concept.html`, 그리고 `콩닥책닥_디자인시스템_컬러팔레트_v1.md`→`콩닥책닥_디자인시스템_컬러팔레트.md` · `_타이포_아이콘_v1.md`→`_타이포_아이콘.md` · `_Hifi목업_v1.md`→`_Hifi목업.md`(프로젝트 문서 쪽 리네임은 사용자가 HTML 파일에 대해 명시적으로 결정한 것과 같은 논리를 디자인 에이전트가 그대로 확장 적용한 것 — 별도로 확인받지는 않았습니다). 파일 안에 남아있던 `Ver 1.x`/`v1.11` 같은 **내용상의 버전 표기는 그대로 유지** — 이번 정리는 파일 경로(이름)에서만 의미 없는 "_v1"을 뗀 것이고, 실제 버전 추적은 여전히 각 파일의 `doc-note`/changelog가 기준입니다. 위 문서 목록·산출물 표·아래 버전 불일치 안내의 파일명도 모두 새 이름으로 갱신했습니다.
>
> **📌 로딩 스피너 컴포넌트 신규 추가 (2026-08-31): `color_chips.html` v2.1** — "앱/네트워크 로딩바를 만든 적 있냐"는 질문에 확인해보니, 서재 탭 전용 "로딩 스켈레톤"(`hifi_mockup.html` Frame 07.1) 하나뿐이고 화면 어디서나 쓸 범용 로딩 인디케이터는 없었던 것으로 확인. 형태는 스피너(원형)와 상단 진행 바 중 스피너로 결정(React Native 앱에서 더 흔한 패턴) — 버튼 내부(16px)·섹션/카드(28px)·전체 화면 오버레이(40px) 3가지 크기로 라이트/다크 각각 제작해 `color_chips.html`에 "10. 로딩 스피너" 섹션으로 추가했습니다. 색상 규칙은 기존 진행률 바와 동일(트랙 `--p100`/`--d-border`, 강조 호 `--p600`/`--d-p400`). 프론트 구현 필요 사항은 별도 요청 문서(`독서기록앱_프론트요청_디자인_로딩스피너컴포넌트.md`) 참고.
>
> **📌 전체 화면 dim 로딩 오버레이 실제 화면 예시 추가 (2026-08-31): `hifi_mockup.html` v1.12** — 위 스피너 컴포넌트를 실제 화면 위에 적용하면 어떻게 보이는지 예시가 필요하다는 요청에 따라, **Frame 07.2 서재 탭 (전체 화면 로딩 오버레이 · dim)**을 신규 추가했습니다. Frame 03과 동일한 실제 책 목록 위에 반투명 어두운 스크림(`rgba(10,11,8,0.58)`)을 깔고 중앙에 흰색 스피너(40px) + "불러오는 중..." 라벨을 배치 — 콘텐츠가 이미 있는 상태에서 새로고침/재조회할 때 쓰는 패턴으로, 콘텐츠가 아예 없을 때 쓰는 Frame 07.1 로딩 스켈레톤과는 용도가 다릅니다(대체가 아니라 별도 상황을 위한 신규 프레임). 스크림 위 스피너·라벨은 스크림 자체가 이미 어두워 라이트/다크 모드 구분 없이 흰색으로 고정. "상태 UI 세트"가 4종 → 5종으로 늘었습니다.

> **📌 토스트/스낵바 메시지 컴포넌트 신규 추가 (2026-09-07): `color_chips.html` v2.2 + `hifi_mockup.html` v1.13** — "우리 어플 메시지용 디자인이 없어요, 저장했습니다 와 같은 메시지를 표시하려면 디자인이 있어야 될 것 같다"는 지적에 확인해보니 실제로 없었던 것으로 확인. 위치는 AskUserQuestion으로 "하단(스낵바 스타일)"을 확정 — `color_chips.html`에 "11. 토스트/스낵바 메시지" 섹션을 신규 추가(성공/오류/경고/정보 4가지 상태). 로딩 오버레이와 같은 원칙으로 **화면이 라이트든 다크든 항상 고정된 어두운 서피스**(`#262723` 배경 + `#f6f7f3` 텍스트)를 사용해 다크모드 전용 절을 따로 두지 않았습니다. 탭바 유무에 따라 `.toast`(하단 20px)/`.toast-above-tabbar`(하단 76px) 두 클래스로 분리. 아이콘 `alert-triangle`·`info` 신규 추가. 실제 화면 적용 예시로 `hifi_mockup.html`에 **Frame 05.3 프로필 편집 (저장 완료 토스트)**을 신규 추가했습니다. 프론트 구현 필요 사항은 별도 요청 문서(`독서기록앱_프론트요청_디자인_토스트스낵바컴포넌트.md`) 참고.

> **📌 확인/경고 다이얼로그 + 하단 액션 시트 컴포넌트 신규 추가 (2026-09-07): `color_chips.html` v2.3 + `hifi_mockup.html` v1.14** — 토스트 작업 직후 "우리 그냥 메시지도 없지 않아요? 요즘은 그런 메시지창 안 만드나?"는 질문을 받아 확인해보니, 화면 전체를 채우는 책 검색/등록 모달(Frame 08)은 있었지만 "정말 로그아웃하시겠어요?" 식의 작은 확인/경고 다이얼로그는 없었습니다(Frame 05 로그아웃 버튼이 확인창 없이 바로 실행되는 구조였음). 사용자가 AskUserQuestion에서 "둘 다 만들기"(중앙 확인 다이얼로그 + 하단 액션 시트)를 선택해 `color_chips.html`에 "12. 확인/경고 다이얼로그"·"13. 하단 액션 시트" 섹션을 신규 추가했습니다. **토스트/로딩 오버레이와 반대로, 다이얼로그와 액션 시트는 화면 콘텐츠와 같은 표면이라고 판단해 화면 자체의 라이트/다크 테마를 그대로 따르도록 설계**했습니다(고정 서피스 아님). 다크모드에서 위험 버튼(`--error`가 밝은 톤 `#eb596e`)에 흰 텍스트를 쓰면 대비가 부족해 신규 `--on-error` 변수(라이트 흰색/다크 진한 텍스트)를 추가했습니다. 아이콘 `trash-2` 신규 추가. 실제 화면 적용 예시로 `hifi_mockup.html`에 **Frame 05.4 프로필 탭 (로그아웃 확인 다이얼로그)**, **Frame 03.2 서재 탭 (기록 카드 액션 시트)**를 신규 추가했습니다. 프론트 구현 필요 사항은 별도 요청 문서(`독서기록앱_프론트요청_디자인_확인다이얼로그액션시트컴포넌트.md`) 참고.

> **📌 알림(푸시) 아이콘 신규 제작 (2026-09-07): `notification_icon.html` v1 (신규 파일)** — "우리 앱 알림용 아이콘은 따로 없어도 되나요?"라는 질문에 확인해보니 앱 아이콘·스플래시만 있고 알림 전용 아이콘은 없었던 것으로 확인. **iOS는 알림에 앱 아이콘을 그대로 재사용**해서 별도 에셋이 필요 없지만, **Android는 상태 표시줄 알림 아이콘을 OS가 강제로 흰색 실루엣 + 완전 투명 배경으로 렌더링**하기 때문에 배경이 있는 지금의 앱 아이콘을 그대로 쓸 수 없고, 두 줄 텍스트 워드마크(콩닥/책닥)는 작은 실루엣으로 축소하면 알아보기 어려워 완전히 별개의 신규 심볼이 필요했습니다. 사용자가 AskUserQuestion에서 "'콩' 하나로, 그냥 글자로" 만들어달라고 요청 — 앱 이름 첫 글자 한 글자만 굵게 사용하는 단순한 방식으로 결정했습니다. Android 표준 5개 밀도(mdpi 24×24 · hdpi 36×36 · xhdpi 48×48 · xxhdpi 72×72 · xxxhdpi 96×96) 투명 배경 PNG로 내보냈습니다(`assets/icon/notification/`, RGBA 알파 확인 완료). iOS는 별도 에셋 대상이 아닙니다. 프론트 참고 사항은 별도 안내 문서(`독서기록앱_디자인요청_프론트_알림아이콘안내.md`) 참고.

> **📌 하단 탭바 선택 아이콘 filled 전환 (2026-09-07): `hifi_mockup.html` v1.15 + `typography_and_icons.html` v1.3** — "이모지(📚) 쓰면 안 되냐, 탭바 아이콘이 못생겼다"는 지적에서 출발. 이모지는 기기·폰트별 렌더링이 달라지고 선택 상태에 맞춰 색을 입힐 수 없어 부적합하다고 판단, 대신 AskUserQuestion으로 **"선택된 탭만 채워진(filled) 아이콘"** 방향을 확정했습니다. 기반 아이콘셋인 Lucide는 전부 outline 전용이라 filled 변형이 없어 콩닥책닥이 자체 제작(`calendar-days-fill`·`library-fill`·`share-2-fill`·`user-fill`, Lucide 라이선스 대상 아님) — 단색(`currentColor`) 안에서 명암을 내기 위해 불투명도 1.0/0.55 두 겹을 쌓는 기법을 사용했습니다. `hifi_mockup.html`의 활성 탭 22곳 전체를 이 아이콘으로 교체(미선택 탭·기존 `--p700` 색상 규칙은 변경 없음), `typography_and_icons.html`에는 "5. 탭바 Active 아이콘" 섹션을 신규 추가해 outline↔filled 비교와 제작 기법을 문서화했습니다.

> **📌 탭바 아이콘 전체 filled로 전환 (2026-09-07): `hifi_mockup.html` v1.16 + `typography_and_icons.html` v1.4** — 바로 위 v1.15에서 "선택된 탭만 filled"로 만든 실제 화면을 보고 "그냥 다 채운 아이콘이 훨씬 예쁘다"는 피드백을 받아 방향을 다시 조정했습니다. 비활성 탭 3곳(66개 지점)의 outline 아이콘도 모두 v1.15에서 만든 filled 버전으로 교체 — 신규 아이콘 제작 없이 기존 4종을 재사용했습니다. 이제 탭바 4개 아이콘은 활성/비활성 관계없이 항상 filled이고, 선택 상태 구분은 기존과 동일하게 색상(`--p700`)과 라벨 굵기만으로 표시합니다. `typography_and_icons.html`의 "5. 탭바 아이콘" 섹션 예시·설명도 함께 갱신했습니다.

> **📌 입력형 다이얼로그 컴포넌트 신규 추가 (2026-09-08): `color_chips.html` v2.4** — 프론트가 책 상세 화면의 "장소사진 라벨 입력"(예: "홍대 카페") UI를 만들다가, 기존 확인/경고 다이얼로그(v2.3, "12.")가 "텍스트 입력 필드가 들어가는 경우는 범위 밖"이라고 명시해둔 케이스에 부딪혀 신규 요청했습니다. `color_chips.html`에 "14. 입력형 다이얼로그" 섹션을 추가 — 카드·스크림·버튼 규칙은 기존 확인 다이얼로그와 동일하고, 본문 자리에 한 줄 입력 필드만 들어갑니다(입력 필드 자체는 새 토큰 없이 기존 폼 화면의 입력 필드 토큰 재사용). 버튼 라벨은 "취소/확인" 대신 상황에 맞게 "건너뛰기/저장"처럼 바꿔 쓰도록 안내했습니다. 답변은 프론트 요청 문서(`독서기록앱_프론트요청_디자인_소감작성화면_장소사진추가화면_v1.md`)에 함께 남겼습니다 — 소감(BookNote) 목록형 UI(카드 리스트, 500자 제한, 전체화면 push)와 장소사진 추가 흐름("+" 타일, 길게 누르기 삭제) 답변도 같은 문서에 있습니다.

> **📌 스토어 마케팅 스크린샷 추가 (2026-09-08, 사용자 직접 반영)** — App Store 6.9"용 6장(`assets/store/screenshots/app_store_6.9/`), Google Play용 6장(`assets/store/screenshots/google_play/`)이 로컬에 커밋됨. 온보딩 화면(Frame 01) 위에 "오늘 읽은 한 페이지를 자랑해보세요" 캡션을 얹은 형태. `store_assets.html`(디자인 소스)에도 v1.1(2026-09-08)에서 이 changelog를 반영했습니다.
>
> **📌 스토어 등록 텍스트 v2 작성 (2026-09-08, 사용자 직접 반영): `store/listing_text_v2.md` 신규** — 위 스크린샷 6장 각각의 캡션 문구(로그인/서재/일정/리캡 대시보드/공유/서재 다크)가 이 문서에 표로 정리되어, 위 콜아웃에서 언급했던 "캡션 문구 미기록" 갭이 해소되었습니다. 그 외에도 앱 이름(`콩닥책닥 - 나만의 독서 기록`, 확정), 카테고리(Google Play 도서/참고자료, App Store 도서+라이프스타일), 지원 URL(GitHub 저장소, 임시), Google Play 짧은/전체 설명, App Store 부제·프로모션 텍스트·키워드(13개, 앱 이름과 중복되는 단어 제외)까지 스토어 등록에 필요한 텍스트 전량을 확정했습니다. 문의처는 `electro2072@gmail.com`, 최소 이용 연령은 "만 14세 이상"으로 명시됨 — 이 두 항목은 개인정보처리방침/이용약관(TODO 리스트 2번 섹션, 아직 미작성) 작성 시 그대로 반영하면 됩니다.
>
> **📌 UX 문구에 "밥상" 보이스 레이어 적용 (2026-09-08): `hifi_mockup.html` v1.17** — "지금까지 쓴 문구들이 딱딱하다"는 피드백을 받고, 사용자가 준비한 UX 보이스 확장 문서(책거리/콩송편 음식 은유를 §1 세 규칙 — 빈도가 높을수록 담백하게 · 잘 됐을 때만 맛을 낸다 · 읽은 양을 음식으로 평가하지 않는다 — 안에서만 쓰는 방식) 기준으로 개정했습니다. 이 문서에 실제로 있던 문구 중 규칙상 "맛을 내도 되는" 자리만 골라 교체(완독 버튼 라벨 간결화, 리캡 하이라이트, 최근 활동 리스트, 서재 최초 빈 상태, 알림 목록 각 1곳) — 실패·경고·삭제·고빈도(저장/등록 토스트 등) 문구는 원칙대로 손대지 않았습니다. 자세한 전/후 목록은 `독서기록앱_프론트요청_UX문구_밥상보이스적용_v1.md` 참고 — 프론트 코드의 실제 문자열은 이 문서를 참고해 반영해야 합니다(디자인 쪽은 목업만 갱신, 실제 앱 문구는 별도).
>
> **📌 로그인 화면 — 비회원 모드 폐기 + Apple 로그인 추가 (2026-09-08): `hifi_mockup.html` v1.18** — 사용자 결정으로 Frame 01의 "비회원으로 둘러보기" 링크를 제거하고, iOS에서만 노출될 `Apple로 로그인` 버튼을 버튼 스택 맨 위에 추가했습니다(다른 소셜 로그인이 있으면 Apple 로그인도 동등 이상으로 노출해야 하는 App Store 심사 가이드라인 4.8 대응 — 아래쪽에 두면 심사에서 지적받을 수 있음). 버튼 색은 기존 `--n900`/`--n50` 토큰을 그대로 재사용해 라이트 모드 검정 버튼 / 다크 모드 흰 버튼이 테마 전환만으로 자동 적용되도록 했습니다 — Apple도 "라이트 화면엔 검정, 다크 화면엔 흰 버튼"을 권장해 별도 색상 결정 없이 기존 토큰으로 충분했습니다. 다른 3개 버튼처럼 로고 아이콘 없이 텍스트만 사용(실제 구현은 각 SDK 공식 버튼 컴포넌트 권장 — 특히 Apple은 버튼 디자인 규정을 심사에서 실제로 확인함). 프론트/백엔드에 필요한 실제 작업은 별도 요청 문서(`독서기록앱_프론트백엔드요청_Apple로그인추가_비회원모드폐기_v1.md`) 참고.

> **📌 로그인 버튼 4개 스타일 통일 (2026-09-09): `hifi_mockup.html` v1.19** — "Apple 버튼만 색이 다른 게 이상하다, 다 통일하거나 실제 브랜드 아이콘 원형 로우로 바꾸자"는 사용자 제안에서 시작. 아이콘 로우 방향(참고 이미지 첨부, Kakao·Naver·Facebook·Apple·Google 5개 원형 아이콘)을 검토하기 위해 각 제공사의 공식 브랜드 에셋 제공 여부를 실제로 조사했습니다 — 구글은 공식 브랜딩 가이드 페이지에 PNG/SVG ZIP 키트가 있고, 카카오도 디벨로퍼스 "리소스 다운로드"에서 PNG/PSD를 받을 수 있지만, 네이버는 등록된 앱 기준으로 개발자센터 콘솔에서 생성해야 해 디자인 쪽에서 독립적으로 받을 수 없고, Apple은 로고 자체를 공식 버튼 컴포넌트 밖에서 쓰는 것 자체가 상표 가이드라인 위반이라 대체 에셋이 없습니다. 5개 제공자의 에셋 신뢰도가 서로 다른 상태로 아이콘 로우를 만들면 어색해질 것으로 판단해, 사용자가 AskUserQuestion에서 **텍스트 버튼 통일**을 선택했습니다(Facebook 추가도 함께 물었으나 미채택 — 이 프로젝트에 Facebook 로그인 계획 자체가 없음, 4개 유지). Apple/네이버/카카오/Google 4개 버튼을 모두 `.btn-fill`(연한 회색 채움) 스타일로 통일하고, v1.18에서 추가했던 전용 `.btn-apple` 클래스(`--n900`/`--n50` 토큰 재사용)는 더 이상 쓰이지 않아 CSS에서 제거했습니다. 버튼 순서(Apple → 네이버 → 카카오 → Google)와 여백(10px×3 + 마지막 20px)은 v1.18 그대로 유지했고, 스타일이 완전히 동일해졌으므로 App Store 4.8의 "Apple 로그인이 다른 소셜 로그인과 최소 동일한 노출도를 가져야 한다"는 조건도 자연히 충족됩니다.

이 폴더는 앱의 비주얼 디자인 산출물(컬러 팔레트, 타이포그래피/아이콘, Hi-Fi 목업)의 실제 소스 파일을 담습니다. 각 산출물의 결정 사항 요약은 claude.ai 프로젝트 문서함에도 마크다운으로 정리되어 있습니다:

- `콩닥책닥_디자인시스템_컬러팔레트.md`
- `콩닥책닥_디자인시스템_타이포_아이콘.md`
- `콩닥책닥_디자인시스템_Hifi목업.md`

## 파일 구성

| 파일 | 현재 버전 | 설명 |
|---|---|---|
| `color_chips.html` | v2.4 | 쑥송편 테마 컬러 팔레트 — 라이트/다크 스와치, 대비 검증 결과, 버튼/배지/카드 적용 예시. v2(2026-08-09)에서 브랜드 컨셉이 "책거리 × 송편"으로 전환되면서 프라이머리 컬러를 실제 쑥송편 색(hue 124.6°)에서 추출한 그린으로 전면 교체하고, 카테고리 구분용 포인트 멀티컬러를 송편 5색 중 4색(분홍·치자·자색, 흰색 제외)으로 교체. **다른 산출물이 참조하는 기준 팔레트** — 이 파일의 색상 값이 바뀌면 아래 두 파일에도 동기화해야 함. (2026-08-27) 문서 제목·footer의 임시 명칭을 "콩닥책닥"으로 교체(색상 값 변경 없음). **v2.1(2026-08-31)**에서 범용 로딩 스피너 컴포넌트 신규 추가(버튼 내부 16px · 섹션/카드 28px · 전체화면 오버레이 40px, 라이트/다크) — 기존 팔레트 색상 값 변경 없음. **v2.2(2026-09-07)**에서 토스트/스낵바 메시지 컴포넌트 신규 추가(성공/오류/경고/정보 4종, 화면 테마 무관 고정 다크 서피스). **v2.3(2026-09-07)**에서 확인/경고 다이얼로그(취소+확인)·하단 액션 시트(핸들바+옵션 행+취소) 신규 추가 — 토스트와 달리 화면 표면을 따르는 라이트/다크 컴포넌트, 위험 옵션은 `--error`색 **v2.4(2026-09-08)**에서 입력형 다이얼로그 신규 추가 — 확인 다이얼로그와 동일 규칙 + 한 줄 입력 필드, 장소사진 라벨 입력 등 텍스트 입력이 필요한 다이얼로그용. |
| `typography_and_icons.html` | v1.4 | Pretendard 타입 스케일 + Lucide 아이콘 24종 가이드. v1.1(2026-07-22)에서 `color_chips.html` v1.2와 어긋나 있던 프라이머리 그린 값을 동기화. v1.2(2026-08-09)에서 `color_chips.html` v2의 쑥송편 그린으로 프라이머리 색상값 재동기화(타입 스케일·아이콘 목록 자체는 변경 없음). (2026-08-27) 문서 제목·footer의 임시 명칭을 "콩닥책닥"으로 교체(내용 변경 없음) **v1.3(2026-09-07)**에서 "5. 탭바 Active 아이콘" 섹션 신규 추가 — 하단 탭바 선택 상태 전용 자체 제작 filled 아이콘 4종(`calendar-days-fill`·`library-fill`·`share-2-fill`·`user-fill`) 문서화, Lucide 24종 세트와 별도이며 Lucide 라이선스 대상 아님. **v1.4(2026-09-07)**에서 "5. 탭바 아이콘" 섹션을 전체 filled 방향에 맞춰 갱신(제목·예시·설명 변경, 아이콘 4종 자체는 동일). |
| `hifi_mockup.html` | v1.19 | `../docs/독서기록공유앱_화면설계서_wireframe_v2-5.html`의 11개 프레임 + 책 검색/등록 모달(Frame 08) + 일정 상세/프로필 편집/알림 목록/저장 완료 토스트/로그아웃 확인 다이얼로그(Frame 02.1·05.2·05.3·05.4·10) + 서재 기록 카드 액션 시트(Frame 03.2) + 상태 UI 세트(Frame 07·07.1·07.2·09·01.2)에 디자인 시스템을 입힌 Hi-Fi 목업. v1.1부터 각 프레임 오른쪽에 다크모드 짝을 나란히 배치(별도 파일 아님, CSS 변수 스코프 `.dark`로 컬러만 전환하고 마크업은 동일). v1.2에서 새 프라이머리 그린 + 포인트 멀티컬러를 실제 화면에 적용. v1.3에서 "아이폰 감성" 피드백에 따라 Dynamic Island · 헤어라인+그림자 카드 · 블러 탭바 · 캡슐형 CTA 등으로 조정. v1.3.1(2026-07-22)에서 책 검색/등록 모달 흐름(Frame 08 · 08.1 · 08.2) 신규 추가. v1.3.2(2026-07-22)에서 일정 상세 · 프로필 편집 · 알림 목록 3개 화면 신규 추가. v1.4(2026-07-22)에서 서재 빈 상태 · 로딩 스켈레톤 · 네트워크 에러 · 회원가입 폼 검증 에러 4개 상태 UI 신규 추가(앱 아이콘·스플래시는 별도 파일 대상이라 여전히 보류). v1.5(2026-08-09)에서 `color_chips.html` v2(쑥송편 브랜드)에 맞춰 프라이머리 그린 램프·관심분야 칩 포인트 컬러·장르 비율 차트 색상을 전면 교체(화면 구조·문구는 변경 없음). v1.6(2026-08-27)에서 앱 이름 확정에 따라 Frame 01 로그인/온보딩 화면(라이트·다크)의 워드마크 텍스트를 "독서 자랑" → "콩닥책닥"으로 교체. v1.7(2026-08-27)에서 Frame 01의 로고 자리를 범용 book-open 글리프에서 실제 확정 앱 아이콘 배지로 교체. v1.8(2026-08-27)에서 태블릿·폴더블 펼침(≥600px Expanded) 대응 레이아웃 3종 신규 추가 — 신규 `.tablet-frame`(640×900, Dynamic Island 없음) 클래스 사용. **v1.9(2026-08-27)에서 v1.8의 확장 레이아웃 3종을 재설계** — 사용자 피드백("폰 요소를 여백만 넓혀 배치했을 뿐, 넓어지는 게 왜 의미 있는지를 담지 못했다")에 따라: 서재 목록은 리스트 2열 → **표지 중심 3열 서가 그리드**(6권)로, 대시보드는 통계 2장 → **4장 전체 폭 배치 + 도넛·막대 차트 확대(76px→140px, 70px→130px) + "최근 활동" 리스트 신규 추가**로, 일정 탭은 좌우 양쪽에 **"다가오는 일정"·"최근 완독한 책" 섹션 신규 추가**로 각각 다시 그려 프레임 하단까지 실제 콘텐츠로 채움. 나머지 폼/단일 컬럼 화면은 별도 목업 없이 max-width+중앙정렬 규칙으로 처리하기로 결정(실제 구현 시 적용). **v1.10(2026-08-28)에서 Frame 02 개선 2건** — 공용 `.hscroll` 클래스로 "읽고 있는 책" 목록을 그리드 대신 가로 스크롤(터치 스와이프)로 전환(폰·태블릿 공통), 태블릿 Frame 02는 캘린더를 전체 폭 최상단으로 올리고 좌="읽을 차례예요"+"일정 알림" / 우="다가오는 일정"으로 재배치, "새 책 등록하기"는 전체 폭으로 이동. **v1.11(2026-08-28)에서 6개 장르 고정색 도입** — 과학·경제·경영 2개 장르에 신규 검증색(청화블루 `--chart5`, 먹빛 남색 `--chart6`) 추가해 6개 장르 전체 고정 매핑 완성, 장르 비율 도넛차트의 등수 기반 "기타" 회색 버킷을 인문 색으로 교체, 관심분야·장르 선택 칩의 구버전 하드코딩 오류(자기계발/과학) 정정, 서재 목록(Frame 03) 책 카드에 장르 배지 신규 추가. **v1.12(2026-08-31)에서 전체 화면 dim 로딩 오버레이 실제 화면 예시 추가** — `color_chips.html` v2.1의 로딩 스피너 컴포넌트를 실제 화면(Frame 03 서재 목록과 동일 콘텐츠)에 적용한 **Frame 07.2** 신규 추가(반투명 스크림 + 흰 스피너 + 라벨, 라이트/다크 동일). **v1.13(2026-09-07)에서 저장 완료 토스트 실제 화면 예시 추가** — `color_chips.html` v2.2의 토스트 컴포넌트를 Frame 05.2(프로필 편집)에 적용한 **Frame 05.3** 신규 추가(탭바 없는 화면이라 `.toast` 기본 하단 20px 오프셋 사용). **v1.14(2026-09-07)에서 확인 다이얼로그·액션 시트 실제 화면 예시 추가** — `color_chips.html` v2.3의 두 컴포넌트를 각각 적용한 **Frame 05.4**(프로필 탭 로그아웃 확인 다이얼로그, 취소/로그아웃(위험) 버튼)와 **Frame 03.2**(서재 탭 기록 카드 액션 시트, 기록 수정하기/기록 삭제하기(위험)/취소)를 신규 추가 — 신규 CSS 변수 `--on-error`로 다크모드 위험 버튼 텍스트 대비 확보. **v1.15(2026-09-07)에서 하단 탭바 선택 아이콘을 outline → filled로 교체** — 활성 탭 22곳 전체에 자체 제작 filled 아이콘(`calendar-days-fill`·`library-fill`·`share-2-fill`·`user-fill`) 적용, 미선택 탭과 기존 `--p700` 색상 규칙은 변경 없음. **v1.16(2026-09-07)에서 비활성 탭 3곳도 filled로 통일** — 탭바 4개 아이콘이 활성/비활성 관계없이 항상 filled, 선택 상태는 색상만으로 구분. **v1.17(2026-09-08)에서 "밥상" 보이스 레이어 적용** — 사용자가 준비한 UX 보이스 확장 문서 기준으로, 문서에 실제로 있던 문구 중 "맛을 내도 되는" 자리(완독 버튼 라벨 간결화, 리캡 하이라이트·최근 활동 리스트·서재 빈 상태·알림 목록 각 1곳씩)만 골라 교체 — 실패/경고/고빈도 문구는 원칙대로 유지. **v1.17에서 v1.1~v1.16 변경 이력을 `hifi_mockup_changelog.md`로 분리**(파일 용량 절감 목적, 화면 내용 변경 없음) — 문서 내 `doc-note`에는 최신 버전 이력만 남고, 전체 이력은 아래 행 참고. **v1.18(2026-09-08)**에서 Frame 01 로그인 화면 2건 변경 — ① "비회원으로 둘러보기" 링크 제거, ② iOS 전용 `Apple로 로그인` 버튼 신규 추가(버튼 스택 맨 위, App Store 4.8 가이드라인 대응). 신규 `.btn-apple` 클래스는 기존 `--n900`/`--n50` 토큰만 재사용해 라이트=검정/다크=흰색 버튼이 자동 전환됨(Apple 자체 권장 규칙과 일치, 신규 색상 변수 없음). **v1.19(2026-09-09)**에서 4개 로그인 버튼을 `.btn-fill` 하나로 통일 — 공식 브랜드 원형 아이콘 로우 방향도 검토했으나 제공사별 에셋 신뢰도 편차(구글·카카오는 공식 다운로드 가능, 네이버는 콘솔 필요, Apple은 로고 단독 사용 자체가 불가)로 텍스트 버튼 통일 채택, v1.18에서 추가한 `.btn-apple` 클래스는 미사용으로 제거 |
| `hifi_mockup_changelog.md` | — | `hifi_mockup.html`의 v1.1~v1.16 상세 변경 이력 아카이브. v1.17(2026-09-08)에서 본문 파일 용량이 계속 늘어나는 것을 줄이기 위해 분리했습니다 — 화면 구조·CSS·아이콘 등 실제 내용은 변경 없음, 순수 이력 텍스트만 이동. 이후 버전(v1.17~)의 이력은 당분간 `hifi_mockup.html` 본문에 남기고, 필요할 때 이 파일에 이어서 옮길 예정 |
| `gen_palette.js`, `gen_dark*.mjs`, `gen_status.mjs`, `check_contrast.mjs`, `check_status.mjs`, `search_warning.mjs`, `gen_mugwort_palette.mjs` | — | 컬러 값을 도출/검증하는 데 쓴 스크립트 (참고/재현용 기록). `gen_mugwort_palette.mjs`(2026-08-09)는 culori로 실제 쑥송편 색(#7D8F5D)에서 hue를 추출해 v2 프라이머리 램프를 도출한 기록 |
| `app_icon.html` | v2.1 (확정) | **앱 아이콘 최종 디자인 소스 — "콩닥"/"책닥" 두 줄 텍스트 워드마크형**(쑥송편 그린 배경 `#7D8F5D` + 크림 텍스트 `#f7f1e4`, Pretendard 800). 2026-08-27, 앱 이름 확정 직후 3가지 색상 조합을 시안(`app_icon_text_concept.html`)으로 비교한 뒤 이 조합(그린 배경+크림 텍스트)으로 확정하며 **기존 쑥송편 아이콘(v1)을 완전히 교체**. 이 파일 안에 실제 1024px/432px 크기의 내보내기 전용 엘리먼트가 있어 `assets/icon/`의 PNG들을 그대로 재현할 수 있음 — Android 전경 레이어는 텍스트 bbox를 실측(getBBox)해 안전영역(66% 원) 안에 여유 있게 들어가도록 계산. **v2.1(2026-08-27)에서 iOS 다크·틴트드 아이콘 변형 추가** — 다크는 배경만 `--p900`(`#1e2903`)로 교체, 틴트드는 원본과 동일한 지각 휘도의 그레이스케일(`#848484`/`#f1f1f1`)로 변환. 레이아웃은 라이트와 동일 |
| `deprecated/app_icon_songpyeon_v1_archived.html` | v1 (보관용, 비활성) | **2026-08-27부로 실제 아이콘 자리에서 내려온 이전 쑥송편 아이콘** — 삭제하지 않고 기록·재사용 가능성을 위해 그대로 보관. 원래 `app_icon.html`이었던 파일을 이름만 바꿔 보존했으며 내용은 동일. (2026-08-27) 보관용 파일을 한곳에 모으기 위해 `design/` 최상위에서 `design/deprecated/`로 이동 |
| `app_icon_text_concept.html` | — (탐색용) | v2 텍스트 아이콘을 확정하기 전 비교했던 3가지 색상 조합(크림+그린 텍스트 / 그린+크림 텍스트 / 진한 그린+크림 텍스트) 시안 문서. 실제 크기 축소본(80/60/40/29px)으로 가독성도 함께 확인함 |
| `assets/icon/ios_icon_1024.png` | v2 | iOS 앱 아이콘, 1024×1024, 알파 없음(OS가 마스크를 씌우므로 정사각형 그대로 제출). "콩닥/책닥" 텍스트 워드마크 |
| `assets/icon/android_adaptive_bg_432.png` | v2 | Android adaptive icon 배경 레이어, 432×432(=108dp×4), 쑥송편 그린 단색 풀블리드 |
| `assets/icon/android_adaptive_fg_432.png` | v2 | Android adaptive icon 전경 레이어, 432×432, **투명 배경**(RGBA 알파 확인 완료) + 안전영역(66% 원, 반지름 142.5px) 안에 여유 있게 들어가는 "콩닥/책닥" 텍스트 |
| `assets/icon/ios_icon_dark_1024.png` | v1 | iOS 18 다크 모드 아이콘, 1024×1024, 알파 없음. 배경 `--p900`(`#1e2903`) + 크림 텍스트(라이트 버전과 동일 색) |
| `assets/icon/ios_icon_tinted_1024.png` | v1 | iOS 18 틴트드 아이콘, 1024×1024, 알파 없음, **그레이스케일**(RGB 채널 간 최대 오차 5 이내로 색 없음 확인). 배경 `#848484` / 텍스트 `#f1f1f1` — 원본 그린/크림과 같은 지각 휘도로 변환한 값. 시스템이 사용자가 고른 임의의 틴트 컬러를 이 위에 얹음 |
| `notification_icon.html` | v1 (신규) | Android 상태 표시줄 알림 전용 실루엣 아이콘 디자인 소스 — 앱 이름 첫 글자 "콩" 한 글자, 흰색, 투명 배경. iOS는 알림에 앱 아이콘을 그대로 재사용해 별도 에셋 대상 아님(문서 내 "iOS는 왜 없는가" 참고) |
| `assets/icon/notification/notification_mdpi_24.png` | v1 | Android 알림 아이콘, mdpi 밀도, 24×24, 투명 배경(RGBA 알파 확인 완료) |
| `assets/icon/notification/notification_hdpi_36.png` | v1 | Android 알림 아이콘, hdpi 밀도, 36×36, 투명 배경 |
| `assets/icon/notification/notification_xhdpi_48.png` | v1 | Android 알림 아이콘, xhdpi 밀도, 48×48, 투명 배경 |
| `assets/icon/notification/notification_xxhdpi_72.png` | v1 | Android 알림 아이콘, xxhdpi 밀도, 72×72, 투명 배경 |
| `assets/icon/notification/notification_xxxhdpi_96.png` | v1 | Android 알림 아이콘, xxxhdpi 밀도, 96×96, 투명 배경 |
| `deprecated/assets/icon/*.png` | v1 (보관용) | 이전 쑥송편 아이콘의 최종 PNG 3종을 그대로 보관(내용 동일, 위치만 이동). (2026-08-27) `assets/icon/archive_songpyeon_v1/`에서 `design/deprecated/assets/icon/`으로 재이동 |
| `store_assets.html` | v1.1 | **스토어 등록용 그래픽** — Google Play 피처 그래픽(1024×500), 고해상도 아이콘(512×512). 사양은 Google Play/Apple 공식 문서 확인 후 정확한 픽셀 크기로 제작. **v1.1(2026-09-08)**에서 changelog 추가 — `assets/store/screenshots/`의 마케팅 스크린샷 12장(app_store_6.9·google_play 각 6장)과 `store/listing_text_v2.md`(스토어 등록 텍스트 전량)가 사용자 직접 반영으로 완료됐음을 문서 내 `doc-note`에 기록. 이 파일 자체가 다루는 그래픽(피처 그래픽·512 아이콘)에는 변경 없음, 위치 안내용 changelog 성격 |
| `assets/store/play_feature_graphic_1024x500.png` | v1 | Google Play 피처 그래픽, 1024×500, 알파 없음 |
| `assets/store/play_store_icon_512x512.png` | v1 | Google Play 고해상도 아이콘, 512×512 |
| `store/listing_text_v2.md` | v2 (신규, 사용자 직접 작성) | **스토어 등록 텍스트 모음** — Google Play Console·App Store Connect에 그대로 입력할 텍스트 전량(앱 이름·카테고리·지원 URL 공통, Google Play 짧은/전체 설명, App Store 부제·프로모션 텍스트·키워드) + 스크린샷 6장의 캡션 문구 표 + 결정 이력. 글자 수 전부 스토어 제한 이내로 검증됨. 디자인 소스 파일(`.html`)이 아니라 등록용 콘텐츠 문서라 `design/store/`에 별도 위치 — 위 `store_assets.html`(그래픽 소스)과는 역할이 다름 |
| `splash_screen.html` | v1 (확정) | **스플래시 화면 최종 디자인 소스** — 쑥송편 그린(`#7D8F5D`) 풀블리드 배경 + "콩닥"/"책닥" 크림색(`#f7f1e4`) 워드마크. 앱 아이콘과 동일한 색 재사용. 다크모드 짝 없음(스플래시는 iOS/Android 관례상 시스템 테마와 무관한 단일 버전) · 로딩 인디케이터 없음(정적 화면). 워드마크 로고를 투명 배경 960×960 PNG로 내보내는 실제 export 엘리먼트 포함 |
| `splash_screen_concept.html` | — (탐색용) | 스플래시 방향을 정하기 전 비교한 2가지 시안 — ① 심플 아이콘 중앙형(크림 배경+작은 아이콘), ② 풀블리드 워드마크형(그린 배경 전체+큰 워드마크). ②번이 채택되어 `splash_screen.html`로 확정됨 |
| `assets/splash/splash_logo.png` | v1 | 스플래시용 워드마크 로고, 960×960, **투명 배경**(RGBA 알파 확인 완료). 배경색(`#7D8F5D`)은 이미지가 아니라 앱 실행 설정에 직접 지정하고, 이 로고 파일만 그 위에 얹는 구성 |
| `branding_proposal_songpyeon.html` | v1 (참고용, draft) | "책거리 × 송편" 리브랜딩을 처음 제안했던 문서 — 책거리 설명, 송편 5색 소개, 옵션 A/B 비교(옵션 B가 채택되어 `color_chips.html` v2 등에 실제 반영됨). **현재 진행 중인 산출물이 아니라 결정 경위를 남겨둔 기록용 파일**이라 위 표의 "현재 버전" 파일들과 달리 갱신되지 않음 — 다만 2026-08-09에 이 파일의 `:root` 프라이머리 색이 옛 비비드 그린으로 남아있던 걸 발견해 실제 채택된 쑥송편 그린으로 동기화했음(내용 자체는 변경 없음, 색상 변수만 정정). (2026-08-28) `<title>`에 남아있던 임시 명칭 "독서 자랑 어플"을 "콩닥책닥"으로 교체(본문·색상 변경 없음) |

### ⚠️ 로컬 저장소와 claude.ai 프로젝트 문서 간 버전 불일치 주의 (2026-07-22 발견 → 대부분 해소)

claude.ai 프로젝트 문서함의 `콩닥책닥_디자인시스템_Hifi목업.md`(2026-08-28부로 `독서기록앱_디자인시스템_Hifi목업_v1.md` → `콩닥책닥_디자인시스템_Hifi목업_v1.md` → `콩닥책닥_디자인시스템_Hifi목업.md` 순서로 두 차례 리네임됨)은 "확정 v1.4"(서재 빈 상태·로딩 스켈레톤·네트워크 에러·회원가입 필드 에러 상태 UI + 앱 아이콘·스플래시 신규 제작)까지 기록하고 있었지만, 발견 당시 이 로컬 저장소(`git pull` 최신 상태 기준)에는 그 변경들이 반영되어 있지 않았습니다(원인 미확인 — git 동기화 이슈로 추정).

같은 2026-07-22 세션 동안 순차적으로 로컬에 없던 화면들을 신규 제작해 채워 넣었습니다: 책 검색/등록 모달(v1.3.1) → 일정 상세·프로필 편집·알림 목록(v1.3.2) → 서재 빈 상태·로딩 스켈레톤·네트워크 에러·회원가입 폼 검증 에러(v1.4). 그 결과 `hifi_mockup.html`은 이제 이 문서가 설명하는 v1.4 내용을 모두 포함합니다. **(2026-08-09 갱신)** 그동안 의도적으로 보류했던 `design/app_icon.html`과 `design/assets/icon/`도 이번에 "책거리 × 송편" 브랜딩에 맞춰 신규 제작했습니다 — 쑥송편 하나만 크게 그린 플랫 스타일입니다. `design/splash_screen.html`은 2026-08-27에 신규 확정되었습니다(풀블리드 워드마크형) — 자세한 내용은 위 표와 파일 상단 콜아웃 참고. 이 문서를 참고할 때는 각 파일 안의 `doc-note`(버전 changelog) 블록을 실제 소스로 삼는 습관을 유지하세요 — claude.ai 프로젝트 문서와 로컬 파일이 다시 어긋날 수 있습니다.

## 스크립트 실행에 대한 주의

`gen_*`/`check_*` 스크립트는 다음 두 외부 경로에 의존합니다 (이 저장소 밖에 있음):

- `culori` npm 패키지 — 로컬에서 재현하려면 `npm install culori` 필요
- dataviz 스킬의 `validate_palette.js` (Anthropic 번들 스킬 경로) — 이 검증기가 없으면 색맹 안전성/대비 체크 로직을 다시 구현해야 함

즉 이 스크립트들은 **"이 색이 어떻게 도출됐는지"를 기록해두기 위한 자료**이고, 그대로 재실행 가능한 빌드 스크립트는 아닙니다.

## 버전 관리 방식

- 각 산출물은 파일명에 `v1`, `v2` 형태로 버전을 표기합니다.
- 이 폴더가 git으로 커밋되기 시작한 시점(2026-07-21)부터는, 새 버전을 만들 때 기존 파일을 덮어쓰지 않고 새 파일(`_v2.html` 등)을 추가하거나, 같은 파일 안에 변경 이력(changelog) 섹션을 남기는 두 방식을 상황에 따라 씁니다 — 와이어프레임 문서가 후자 방식(문서 내 changelog)의 예시입니다.
- git 커밋 이력 자체가 세부 변경 diff를 보존하므로, 파일명 버전 표기는 "현재 어떤 버전을 보고 있는지"를 사람이 알아보기 위한 용도이고, 실제 변경 이력 추적은 git log가 담당합니다.
- **(2026-07-22부터 표준화)** HTML 산출물 3종(`color_chips.html`, `typography_and_icons.html`, `hifi_mockup.html`) 모두 `<h1>` 제목과 상단 메타 정보 바로 아래에 `.doc-note` 스타일의 버전 changelog 박스를 둡니다 — 버전 번호별로 날짜와 변경 내용을 한 줄씩 누적 기록. git log를 뒤지지 않고도 파일을 열어보는 것만으로 "지금 이게 몇 버전이고 뭐가 바뀌었는지"를 바로 알 수 있게 하기 위함입니다. 이 위와 아래 표의 "현재 버전" 열은 그 `.doc-note`의 최신 항목과 항상 일치해야 합니다(둘 중 하나만 갱신하고 잊어버리지 않도록 주의).
- 이 저장소(로컬 클론)와 claude.ai 프로젝트 문서함의 "확정" 버전 표기가 git 동기화 문제로 어긋날 수 있다는 것이 2026-07-22에 확인되었습니다 — 아래 "로컬 저장소와 claude.ai 프로젝트 문서 간 버전 불일치 주의" 참고. 실제 파일 상태(`.doc-note`)를 항상 1차 소스로 삼으세요.
- **(2026-08-27부터 표준화)** 어떤 산출물이 완전히 교체되어 더 이상 활성 버전이 아니게 되면(예: 앱 아이콘 v1 쑥송편 → v2/v3 텍스트 워드마크), **삭제하지 않고 `design/deprecated/` 폴더로 옮겨 보관**합니다. 원래 있던 상대 경로 구조(`assets/icon/...` 등)를 그대로 `deprecated/` 밑에 재현합니다. 결정 과정을 비교하기 위해 만든 "탐색용" 시안 문서(파일명에 `_concept_v1` 등이 붙는 것들)는 폐기된 게 아니므로 이 폴더 대상이 아닙니다 — `deprecated/`는 오직 "한때 확정이었다가 완전히 교체된 것"만을 위한 폴더입니다.
