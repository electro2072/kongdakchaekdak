# 데일리 스크럼 — 2026-09-09 (KST)

- 작성: PM 에이전트 (관측 기반)
- 관측 시점: 2026-09-09 22:28 KST
- 관측 HEAD: `74974fe` (main, origin/main과 동기)
- 대상 기간: 직전 스크럼(2026-09-08 08:31, `b30c9e9`) 이후 ~ 관측 시점
- 관측 방법: 루트에서 영역별 `git log` + 실제 파일/README/리포트/연동매트릭스 대조

## 이번 기간 커밋 개요

직전 스크럼 이후 **약 24시간 사이 활동이 집중**됨. 대부분 09-08 20:45~09-09 01:20 사이의
자동 개발 세션 1회 + 09-09 07:30~09:00 테스터 세션 1회 + 09-09 12:36~12:41 사용자 직접 커밋.

| 영역 | 커밋 수 | 미커밋 | 비고 |
|---|---|---|---|
| backend | 1 | **있음 (진행 중)** | 장소사진 카메라 오류 조치 / 미커밋 = Apple 로그인 |
| frontend | 13 | **있음 (진행 중, 27파일)** | S0·S1·S2 실연동 + 탭바 filled + 서재 API / 미커밋 = gender·한글IME·타입리팩터링·Apple |
| design | 7 | **있음 (진행 중)** | 탭바 filled·입력다이얼로그·스토어 스크린샷 / 미커밋 = 목업 v1.17·v1.18 |
| test (docs/) | 7 | 없음 | S0 PASS / S1 PASS / S2 major버그 / **S3 blocker** |
| docs (사용자 직접) | 4 | — | 연동매트릭스·기획서·계정삭제 문서 갱신 |

---

## 백엔드

**신규 커밋 1건 + 관측 시점 미커밋(진행 중).**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-09 01:18 | `852ba98` | **장소 사진 카메라 업로드 오류 조치.** 프론트 `0c727b6`와 짝. 루트에 `photo-upload-500-fix-backend.patch` 아티팩트 잔존(미추적). |

- **미커밋 변경 (관측 시점 진행 중):** Apple 로그인 백엔드 구현. 신규 `AppleOAuthClient.java`(+49), `AppleOAuthProperties.java`, `dto/AppleIdTokenRequest.java`, 수정 `OAuth2Config.java`(+42), `SocialAuthService.java`(+14), `AuthController.java`(+17), `application.yml`, `.env.example`. 테스트 신규 `AppleOAuthClientTest.java`(+132), `SocialAuthControllerTest.java`(+38). → 연동매트릭스 `#42`(Apple 로그인, v1 범위 확정, D14) 대응. 아직 커밋·푸시 전.
- **TODO/FIXME/WIP:** committed 소스에서 특이 키워드 없음.
- **미해결 (개발자 몫):**
  - **[blocker · G13 · BUG-20260909-23] 등록한 책이 서버에 저장되지 않음.** 테스터가 09-09 08:59 리포트(`reports/2026-09-09c.md`)로 제출. `POST /api/books`는 201을 반환하는데 앱 재시작 시 새 책이 사라짐(4~5회 재현). 연동매트릭스는 이걸 "앱 코어 기능이 깨져 있다"로 규정하고 **최우선 병목**으로 지정. 권장 1차 조치는 백엔드가 Railway 로그의 `BOOK_REGISTERED` audit 이벤트 + MySQL 직접 조회로 실제 저장 여부·`user_id` 확인. **관측 시점까지 백엔드의 이 건 관련 커밋·미커밋 흔적 없음** (미커밋 작업은 Apple 로그인) → 착수 전으로 추정. 테스터 리포트 제출 후 약 13시간 경과.
  - **[정보 · G17] enum 직렬화 규칙 통일** — `Gender`·`BookStatus`에 `@JsonValue toLowerCase()` 적용 + `BookController.search`의 `toUpperCase()` 임시코드 제거. 연동매트릭스가 **"G13보다 먼저 하면 안 된다"**(같은 파일을 건드리는 FE 코드가 깨짐)고 명시.
- **이월(사용자 액션 · 시한):** **G9 — Railway 크레딧 결제.** 미결제 시 MySQL 볼륨(데이터) 삭제 → G13 조사에 필요한 DB 상태도 함께 소멸. 이번 관측에서 크레딧/결제 상태 확인 불가.

---

## 프론트엔드

**신규 커밋 13건 + 관측 시점 미커밋 27파일(진행 중).** 직전 스크럼의 PM 피드백(S0~S2)·테스터
요청서를 대부분 반영.

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-08 20:58 | `ddb671a` | iOS 카메라·사진·위치 권한 문구 + `PrivacyInfo.xcprivacy`(수집 데이터 6종). |
| 09-08 21:14 | `3352dbe` | 책 상세 3종(표지/소감/장소사진) API 연동 준비. 소감 작성을 완독 전에도 가능하도록 목록형 UI로 변경. |
| 09-08 22:33 | `b02744a` | **전체 API 연동 설계 + 1차 구현** — 공용 클라이언트(`apiFetch`)/세션영속화/프로필/대시보드/미접속 배너. 서재·모임·공유이력은 백엔드 회신 대기로 남김. |
| 09-08 23:06 | `df85ab8` | `react-native-encrypted-storage` 설치. |
| 09-08 23:18 | `52e7501` | **S0** — 라우터 미등록으로 크래시하던 3화면(GroupManagement / ScheduleDetail / Notifications) 등록. |
| 09-08 23:19 | `ae86667` | **S1** — `secureStorage` + `AuthContext`가 암호화 스토리지에 세션 저장/복원. 관련 테스트·mock·jest 설정 포함. |
| 09-08 23:21 | `b3fd618` | **S2** — `ProfileContext` 실데이터화, `profileStatsApi`·`types/profile.ts` 추가, `mocks/profile.ts` 삭제. |
| 09-08 23:48 | `cf83881` | typecheck 오류 수정. |
| 09-09 00:43·00:55 | `ada8880`·`205120a` | 하단 탭바 변경된 디자인(filled 아이콘) 적용 x2. |
| 09-09 00:56 | `a89620b` | **서재** — `LibraryContext`를 API 연동 버전으로 변경(`useState([])`, mock fallback 없음). |
| 09-09 01:10 | `f9547b2` | `LibraryContext` 테스트 추가. |
| 09-09 01:18 | `0c727b6` | 장소 사진 카메라 업로드 오류 조치(백엔드 `852ba98`와 짝). |

- **미커밋 변경 (관측 시점 진행 중, 27파일):**
  - **G14 / BUG-20260909-21 gender 매핑 수정 (D2 결정 = FE가 BE에 맞춘다).** `userApi.ts`에 `GENDER_KEY_TO_BACKEND`(`female→FEMALE` 등) / `BACKEND_GENDER_TO_KEY` 양방향 매핑 + `mapGenderKeyToRequest`/`mapGenderResponseToKey`. 테스터 리포트(`2026-09-09b.md`) 근거로 착수.
  - **BUG-20260901-17 한글 IME 수정.** `SearchBar.tsx`에 `autoCorrect/spellCheck=false`, `textBreakStrategy="simple"`. `profileOptions.ts`에 `sanitizeNicknameWhileTyping()` 신규 — 조합 중 낱자(ㄱ-ㅎ, ㅏ-ㅣ)를 타이핑 중에는 지우지 않아 IME 조합이 끊기지 않도록(저장 시점엔 기존 `sanitizeNickname()`으로 엄격 검증).
  - **타입 리팩터링(G18 인근).** `src/types/api/` 폴더 신설 — `libraryApi.ts`·`dashboard.ts`의 요청/응답 인터페이스를 `types/api/*`로 이동하고 기존 경로에서 re-export. `libraryApi.ts` 78줄 감소.
  - **Apple 로그인** — `authApi.ts`, `SignupScreen.tsx` 등.
  - `BookNoteEditScreen.tsx`, dashboard 컴포넌트 소소 정리.
- **미해결 (개발자 몫):**
  - **[blocker · G13] BUG-20260909-23** — FE 몫은 등록 시 `POST /api/books` 요청 바디의 `userId`와 응답 `id`·`userId`를 로그로 남겨 대조(연동매트릭스 우선순위 2). 관측 시점 미커밋 27파일에 이 로깅 추가 흔적은 확인 안 됨 → 추정: 아직 착수 전이거나 위 다른 작업이 우선.
  - **[정보] FINDING-20260908-18** — S0 3화면 진입 크래시 방지용 자동화 렌더 테스트 없음(라우트가 param list에 남아 typecheck는 통과 → CI가 못 잡음). `f9547b2`로 `LibraryContext` 테스트는 추가됐으나 S0 3화면 렌더 테스트는 미추가로 보임.
  - **[minor · 이월] FINDING-20260908-19** — release cleartext 예외 없음.
- **정합성(추정):** 미커밋 타입 리팩터링이 `libraryApi.ts`를 크게 건드림. 연동매트릭스 G17/§ 경고가 **"`libraryApi.ts`·`LibraryContext.tsx`는 지금 G13 조사 대상 — 같은 파일을 건드리면 (충돌) 위험"**이라고 명시 → G13 조사와 충돌 소지 있음. 커밋 순서 조율 필요로 추정.

---

## 디자인

**신규 커밋 7건 + 관측 시점 미커밋(진행 중).** 직전 스크럼에서 지적한 커밋 파이프라인 병목이 이번엔
대부분 해소됨(7커밋 실제 반영).

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-08 20:45 | `a6755d2` | 하단 탭바 아이콘 전체 filled로 통일(목업 v1.16 상당). |
| 09-08 20:46 | `9391aa8` | 입력형 다이얼로그 컴포넌트 신규(`color_chips.html` v2.4, 장소사진 라벨 입력용). |
| 09-08 20:55 | `b3411d7` | README 문서 최신화(프로젝트 설명 상세화). |
| 09-08 21:00~21:20 | `d1c6dc3`·`dd81883`·`57c5616` | 앱 스토어 마케팅 스크린샷(App Store 6.9" 6장 / Google Play 6장) + 스토어 등록 텍스트 `store/listing_text_v2.md`. |
| 09-08 21:28 | `a0b69cf` | 스토어 스크린샷/등록 텍스트 문서화 + `store_assets.html` changelog. |

- **미커밋 변경 (관측 시점 진행 중):** `design/README.md`, `design/hifi_mockup_v1.html`, 신규 `design/hifi_mockup_changelog.md`(미추적).
  - **목업 v1.17** — "밥상" 보이스 레이어 적용. 사용자 UX 보이스 확장 문서(음식 은유 3규칙) 기준으로 "맛을 내도 되는" 자리(완독 버튼 라벨·리캡 하이라이트·최근 활동·서재 빈 상태·알림 목록)만 교체. 실패/경고/고빈도 문구는 원칙대로 유지. **실제 앱 문구 반영은 프론트 몫**(`독서기록앱_프론트요청_UX문구_밥상보이스적용_v1.md`).
  - **목업 v1.18** — Frame 01 로그인: "비회원으로 둘러보기" 링크 제거 + iOS 전용 `Apple로 로그인` 버튼(스택 최상단, App Store 4.8 대응). `.btn-apple`은 기존 `--n900`/`--n50` 토큰 재사용(라이트=검정/다크=흰색 자동).
  - v1.1~v1.16 상세 이력을 `hifi_mockup_changelog.md`로 분리(용량 절감, 내용 변경 없음).
- **미해결 (디자인 몫):**
  - **[P0 · G16] 회원 탈퇴 진입점·확인 다이얼로그** — Apple 5.1.1(v) 필수. `color_chips.html` v2.3 다이얼로그 재사용이라 가벼운 작업(연동매트릭스 우선순위 3). 관측 시점 목업에 탈퇴 화면 추가 흔적 없음.
  - **[정보 · OBS-20260909-20] 로그아웃 확인 다이얼로그** — 목업엔 Frame 05.4로 있으나 앱은 1탭 즉시 실행. 디자인은 완료, FE 반영 대기.
- **[이월 · 구조적] README ↔ 실제 파일명 불일치.** README·changelog는 `hifi_mockup.html`로 서술하나 디스크 파일은 `hifi_mockup_v1.html`. `commit_commands.txt` step 2(`git mv`로 `_v1` 제거) 미실행 상태 지속(08-30부터 이월). 연동매트릭스는 "파일명을 `연동매트릭스_v1.md`로 고정"처럼 목업도 개명 시 FE 참조가 깨진다는 입장 → **개명 대신 README 서술을 실제 파일명에 맞추는 쪽**으로 정리 권장(PM 의견).

---

## 테스터 (QA)

**신규 리포트 4건, 커밋 7건.** S0→S1→S2→S3 슬라이스를 순서대로 실기기 검증.

| 리포트 | 시각(KST) | 슬라이스 | 판정 |
|---|---|---|---|
| `reports/2026-09-08b.md` | 09-08 23:33 | **S0** — MainStack 3화면 라우터 등록 | **✅ PASS** — 3개 진입점 크래시 없음, pop 정상, logcat JS에러 0건 |
| `reports/2026-09-09.md` | 09-09 07:59 | **S1** — 공용 `apiFetch` + 토큰 저장/복원 | **✅ S1-1 PASS** — 8케이스 중 6 PASS / 1 미실시(만료토큰, 유닛테스트 커버) / 0 FAIL. A표 #5 승격 요청 |
| `reports/2026-09-09b.md` | 09-09 08:29 | **S2** — 프로필 실연동 | **🟠 §5 PASS, 미종료** — 닉네임 왕복 PASS. 단 **BUG-20260909-21(gender, major)** + OBS-20260909-22(닉네임 규칙 불일치) |
| `reports/2026-09-09c.md` | 09-09 08:59 | **S3** — 서재 | **🔴 §5 FAIL** — **BUG-20260909-23(blocker)** 등록한 책이 서버에 안 남음 |

- **발견된 버그/이슈:**

| ID | 심각도 | 내용 | 담당 | 상태 |
|---|---|---|---|---|
| BUG-20260909-23 | **blocker** | 등록한 책 미영속 — `POST /api/books` 201인데 재시작 시 소멸, 4~5회 재현 | BE(1차) + FE(로깅) | **open, 미착수 추정** |
| BUG-20260909-21 | major | gender enum 불일치(BE `MALE`/`FEMALE`/`NONE` ↔ FE 소문자) → 성별 항상 유실 | FE (D2) | open, **FE 미커밋 대응 중** |
| OBS-20260909-22 | 정보 | 닉네임 규칙 FE/BE 불일치 — BE `@Size(max=30)`만 / FE 2~10자 한글영숫자 | 기획 결정(G15/D4) | open |
| BUG-20260901-17 | minor | 서재 검색창 한글 자모 분해(간헐) | FE | open, **FE 미커밋 대응 중** |

- **에뮬레이터 시각 이슈(OBS):** 테스터 환경 에뮬레이터 시각이 실제(09-09)보다 하루 느린 09-08 → 날짜 의존 로직(대시보드 기간·완독일 등) 판정 시 참고 필요.
- **A표(연동매트릭스) 갱신:** `259c495`에서 S0~S3 결과 반영. **#5(`GET /api/auth/me`)·#9(프로필 PATCH)·#13(`GET /api/books`) 승격** → 공식 연동률 **4/37 → 7/42(17%)**. G13~G18 신설, §2.5 스토리지 **Cloudflare R2** 결정. (분모 37→42는 후퇴가 아니라 v1 범위 확정 — 알림 3 + 진행률 1 + Apple 로그인 1 포함.)
- **제안된 자동화 테스트 실제 추가 여부:** FE가 `LibraryContext`(`f9547b2`)·`AuthContext`(S1, `ae86667`) 유닛 테스트는 추가함. 단 **FINDING-20260908-18(S0 3화면 렌더 회귀 테스트)**은 미추가로 보임 → open 유지.
- **미커밋:** 없음(테스터 영역은 워킹트리 clean, `docs/test/`만 산출).

---

## PM 관점 종합 스크럼

### 영역별 한 줄 요약

- **백엔드:** 장소사진 오류 1건 커밋 + Apple 로그인 미커밋 진행 중. **blocker G13(책 미영속)은 리포트 접수 13시간째 미착수 추정** — 우선순위 역전 우려.
- **프론트엔드:** S0·S1·S2·서재 실연동을 하루 만에 대거 반영(13커밋), 지금은 gender·한글IME·타입리팩터링·Apple 로그인을 미커밋으로 병렬 진행. S1은 테스터 PASS.
- **디자인:** 파이프라인 병목 해소(7커밋 반영), 스토어 자산 완료. 목업 v1.17·v1.18(밥상 보이스 + Apple 버튼) 미커밋. **P0 회원탈퇴(G16) 화면은 미착수.**
- **테스터:** S0~S3 순차 검증 완료. S0·S1 PASS, S2는 gender 버그로 미종료, **S3는 blocker로 FAIL**. 검증 인프라(adb E2E) 안정적.

### 가장 급한 이슈 top 2

1. **[blocker] BUG-20260909-23 (G13) — 등록한 책이 서버에 안 남음.** 앱 코어 쓰기 경로가 영속되지 않아 S3·S4·S5 검증이 전부 막힘. 연동매트릭스가 지정한 단일 병목. 백엔드 1차 진단(Railway 로그 `BOOK_REGISTERED` + MySQL 직접 조회)이 필요한데 **관측 시점까지 백엔드가 이 건에 손대지 않고 Apple 로그인을 진행 중**으로 보임 → 순서 재조정 권장. 에이전트에 DB 접근 권한이 없으면 **사용자가 Railway 로그/MySQL 조회 결과를 떠서 전달**해야 진행 가능.
2. **[시한] G9 — Railway 크레딧 결제(사용자 액션).** 미결제 시 MySQL 볼륨 삭제 → G13 조사 대상 DB까지 소멸. G13이 DB 상태 조사에 의존하므로 이 둘은 함께 처리돼야 함. 결제 상태 미확인 상태로 며칠째 이월.

### 영역 간 소통 / 정합성 이슈 (추정 포함)

- **(추정) blocker 우선순위 역전.** 테스터가 09-09 아침 blocker를 올렸으나 백엔드 미커밋 작업은 Apple 로그인(#42, v1 범위지만 blocker 아님). Apple 로그인·gender·타입리팩터링이 병렬로 도는 사이 G13만 담당자 배정이 비어 보임.
- **(추정) 파일 충돌 소지.** FE 미커밋 타입 리팩터링이 `libraryApi.ts`를 78줄 수정. 연동매트릭스 §G17이 "`libraryApi.ts`는 G13 조사 대상, 같은 파일 건드리면 위험"이라 경고 → G13 조사자와 커밋 순서 조율 필요.
- **(추정) UX 문구 이중 관리.** 디자인은 목업 v1.17에 밥상 보이스를 반영했으나 "실제 앱 문구는 프론트가 별도 문서 보고 반영"으로 분리 → 반영 누락 시 목업↔앱 문구 괴리 지속 가능. 별도 요청 문서(`독서기록앱_프론트요청_UX문구_밥상보이스적용_v1.md`) 존재.
- **정합성 양호:** gender 버그(G14)는 테스터 리포트 → FE 미커밋 대응까지 같은 날 연결됨. S1은 FE 구현 → 테스터 PASS로 정상 종료.

---

## 커밋 / 푸시 결과

- **커밋:** `b67ba5f` `[scrum] 2026-09-09 데일리 스크럼 정리` — `docs/scrum/2026-09-09.md` 1개 파일만(151줄 추가).
- **푸시:** `git push origin main` 성공 (`74974fe..b67ba5f`).
- **참고:** 첫 커밋 시도(`5027ba1`)에 다른 세션이 인덱스에 미리 stage해 둔 백엔드 Apple 로그인 파일들이 함께 딸려 들어가 `git reset --soft` 후 스크럼 파일만 다시 커밋함. 해당 백엔드/프론트/디자인 워킹트리 변경분은 전부 그대로 보존(내용 손실 없음), 다만 stage 상태만 해제됨 — 담당 세션이 다시 `git add` 필요.
