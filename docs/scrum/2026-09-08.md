# 데일리 스크럼 — 2026-09-08 (KST)

- 작성: PM 에이전트 (관측 기반)
- 관측 시점 HEAD: `bbcff31` (main, origin/main과 동기)
- 대상 기간: 직전 스크럼(2026-09-02) 이후 ~ 관측 시점
- 관측 방법: 루트에서 영역별 `git log --all` + 실제 파일/README/리포트 대조

## 이번 기간 커밋 개요

직전 스크럼(09-02) 이후 **09-02 10:00 ~ 09-07 20:00 사이 커밋 0건** — 5일간 활동 없음.
모든 활동은 **09-07 23:51 ~ 09-08 00:36 약 45분 사이의 자동 개발 세션 1회**에 몰려 있음 (총 9커밋).

| 영역 | 커밋 수 | 비고 |
|---|---|---|
| backend | 1 | 비활성 사용자 알림 |
| frontend | 2 | 토스트/다이얼로그/로거 + 문구 1줄 |
| design | 2 | 다이얼로그·액션시트 / 알림 아이콘 (+ 미커밋 4파일) |
| test (docs/) | 4 | 시드 엣지케이스·APK·연동 요청서 |

---

## 백엔드

**신규 커밋 1건.** (5일 만의 첫 활동)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-07 23:53 | `159766d` | **비활성 사용자 인앱 알림 설계+구현.** 30일 이상 미접속자가 로그인하면 "기다렸다는" 알림을 주는 기능. `User.java`(+19), `UserResponse.java`(+32, `daysSinceLastLogin` 필드 추가), `SocialAuthService.java`(+9, 로그인 시 lastLoginAt 갱신 흐름). 자체 테스트 3건 신규: `SocialAuthControllerTest`(+24), `UserLastLoginAtTest`(+47), `UserResponseTest`(+48). |

- **TODO/FIXME/WIP:** 커밋 메시지·변경 파일에서 특이 키워드 없음.
- **미커밋 변경:** 없음 (backend/ 워킹트리 clean).
- **테스터 실측 결과 반영:** 테스터가 대시보드 시드에 엣지케이스(완독일수 동률 2권 / `total_pages` NULL 1권)를 추가해 `GET /api/dashboard`를 실호출 → **tie-break(먼저 INSERT된 id 작은 쪽 승) + null 합산(0으로 null-safe) 전부 PASS** (`reports/2026-09-08.md`). 백엔드 대시보드 로직이 실측으로 검증됨.
- **이월(사용자 액션, 추정):** Railway 크레딧 결제 결정 — 이번 세션에서 배포/크레딧 상태 미확인.

---

## 프론트엔드

**신규 커밋 2건.**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-07 23:51 | `b8a94fb` | **UX 피드백 보강.** "사용자에게 주는 정보가 너무 적다"는 판단. `Toast.tsx`(+142) 성공 토스트, `ConfirmDialog.tsx`(+130), `utils/logger.ts`(+48) 프론트 로거, `SignupScreen.tsx`(+27) 회원가입 온보딩 다이얼로그. `AuthContext.tsx`(+23), `MainTabs.tsx`(+21), `authApi/kakaoApi/aladinApi`에 로거 연동, `LoginScreen`(+23). |
| 09-08 00:36 | `bbcff31` | 프로필 수정 시 메시지 문구 1줄 변경 (`ProfileEditScreen.tsx` 1줄). |

- **TODO/FIXME/WIP:** `AuthContext.tsx` 주석에 "AsyncStorage 등 미도입"(세션 영속화 없음 = 앱 재시작 시 로그아웃) 잔존 — 테스터 요청서 §1-2에 개선 항목으로 명시됨.
- **미커밋 변경:** 없음 (frontend/ 워킹트리 clean).

### 미해결 (개발자 몫)

- **[major · 6일째 open] `MainStack` 3화면 회귀** — 직전 스크럼(09-02) **top-1**이 그대로 남음. 그 뒤 프론트 커밋 2건(`b8a94fb`, `bbcff31`) 모두 `MainStack.tsx` 미접촉.
  - `navigation/types.ts`는 `GroupManagement`(48행)·`ScheduleDetail`(53행)·`Notifications`(58행) 라우트를 선언.
  - `MainStack.tsx`가 등록하는 화면은 `Tabs / BookDetail / BookSearch / BookRegisterConfirm / Dashboard / ProfileEdit` **6개뿐** — 위 3개 미등록(import도 없음).
  - `screens/GroupManagementScreen.tsx` · `ScheduleDetailScreen.tsx` · `NotificationScreen.tsx` 파일은 **존재하지만 어디에도 연결 안 됨.**
  - 호출부는 살아 있음: `ScheduleScreen.tsx:71`(`navigate('Notifications')` — 벨 아이콘), `ScheduleScreen.tsx:180`(`navigate('ScheduleDetail')` — 일정 카드), `ShareScreen.tsx:71`(`navigate('GroupManagement')` — "그룹 관리").
  - → 일정 탭 벨/일정 상세/공유 탭 "그룹 관리" 진입 시 **네비게이션 실패.** 라우트가 param list에 남아 있어 **TS typecheck는 통과 → 프론트 CI가 못 잡음.**
- **[major · 이월] BUG-20260901-17** — 서재 검색창 placeholder 한글 자모 분해. `SearchBar.tsx` / `LibraryScreen.tsx` 09-02 이후 미변경 → open.
- **[minor · 이월] OBS-20260901-18**(검색 버튼 파란색), **DARK-01/02**(다크모드 가독성) — 관련 파일 미변경.

### 백엔드 연동

- 여전히 **소셜 로그인만 실연동.** 프로필/서재/대시보드/모임/공유/알림은 전부 `src/mocks/*` + Context 로컬 state.
- 테스터가 **오늘(`ab94da8`) Tier-1 대량 연동 요청서**(`requests/프론트_백엔드연동_미연동API_목록_v1.md`)를 프론트에 전달 — 공용 `apiFetch`(인증 헤더) + 세션 영속화 선행 → 프로필 → 서재 → 대시보드 순. **아직 착수 전** (당일 접수라 정상).

---

## 디자인

**신규 커밋 2건 + 관측 시점 미커밋 4파일.**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-07 23:59 | `6c342a9` | **확인/경고 다이얼로그 + 하단 액션 시트 컴포넌트 신규.** `color_chips.html`(+224, "12. 확인/경고 다이얼로그" / "13. 하단 액션 시트" 라이트·다크), `hifi_mockup_v1.html`(+369, Frame 05.4 로그아웃 확인 다이얼로그 / Frame 03.2 기록 카드 액션 시트). 신규 CSS 변수 `--on-error`(다크모드 위험 버튼 텍스트 대비). |
| 09-07 23:59 | `500217c` | **Android 알림 전용 아이콘 신규.** `notification_icon.html`(+151) + `assets/icon/notification/` 5개 밀도 PNG(mdpi~xxxhdpi, 투명 배경). "콩" 한 글자 실루엣 — Android가 상태표시줄 아이콘을 흰색 실루엣으로 강제 렌더링하기 때문. iOS는 앱 아이콘 재사용이라 대상 아님. 프론트 안내는 별도 문서. |

- **미커밋 변경 (관측 시점 진행 중):** `design/README.md`, `design/commit_commands.txt`, `design/hifi_mockup_v1.html`, `design/typography_and_icons.html` 4개 파일 `M` 상태. `commit_commands.txt`에 **step 6(하단 탭바 선택 아이콘 filled 전환 — `typography_and_icons.html` v1.3, `hifi_mockup.html` v1.15)**이 새로 추가됨 → 디자이너가 filled 아이콘 작업을 디스크에 반영했으나 미커밋. (이번 스크럼은 `docs/scrum/` 외 파일을 stage하지 않으므로 그대로 둠.)
- **[이월 · 구조적] 디자인 커밋 파이프라인 병목.** 디자인 에이전트는 git 권한이 없고 최근 세션들이 사용자 PC에 미연결 상태로 작업 → 산출물을 채팅으로 전달(SendUserFile) + `commit_commands.txt`에 명령을 누적. 현재 7단계 백로그 중 일부만 실행됨:
  - **step 2 "_v1" 파일명 제거(`git mv`) 미실행** → 디스크 파일은 여전히 `hifi_mockup_v1.html` 등. committed `README.md`는 `_v1` 없는 이름으로 서술 → **README ↔ 실제 파일명 불일치 지속 (08-30부터 이월).**
  - step 6(탭바 filled 아이콘) 미커밋 (위 미커밋 변경분).
- **미착수 유지:** 마케팅 스크린샷(App Store 6.5"/6.9") — 어떤 화면·캡션인지 사용자 결정 + 앱 화면 안정 대기.
- README 산출물 상태 표 기준 확정물: 앱 아이콘 v2.1 · 스플래시 v1 · hifi_mockup(커밋 기준 v1.14대) — 이번 커밋 반영분 외 상태 변화 없음.

---

## 테스터 / QA

**최신 리포트:** `docs/test/reports/2026-09-08.md` (신규). 직전은 `2026-09-01.md`.

**이번 기간 `[test]` 커밋 4건 (전부 09-08 00:13~00:32):**

| 커밋 | 내용 |
|---|---|
| `9faed6c` | `fixtures/dashboard-seed.sql` 엣지케이스 3권 추가 (완독 22→25): 완독일수 41일 **동률 2권**(id 23/24) + `total_pages` **NULL** 1권(id 25). `fixtures/README.md` 갱신. |
| `4bdf988` | **2026-09-08 리포트** — 시드 실측: `GET /api/dashboard` (month/quarter/year) **전부 PASS**. tie-break = `longestReadBook.bookId 23`(먼저 INSERT, id 작은 쪽) ✅, null totalPages는 0으로 합산(null-safe) ✅. + arm64 슬림 APK 재빌드(58.8MB→23.2MB) + GitHub Release `apk-2026-09-08`. |
| `ee7bb5b` | **슬림 APK 설치 실패 원인 규명.** 슬림화 스크립트가 zip 재작성 시 `resources.arsc`를 DEFLATED로 넣어 Android 11+(targetSdk 30+)에서 `INSTALL_PARSE_FAILED`. → 스크립트가 원본 `compress_type` 보존(`resources.arsc` STORED 유지)하도록 수정, 릴리스 자산 교체. |
| `ab94da8` | **프론트 전달 요청서** — 백엔드 미연동 화면/API 목록 + 우선순위(Tier 1/2). frontend-agent 대상. |

**발견된 버그/이슈 (심각도별):**

| ID | 심각도 | 영역 | 상태 |
|---|---|---|---|
| 대시보드 tie-break / null 합산 | — | backend | **PASS (신규 검증, 결함 없음)** |
| 슬림 APK 설치 실패(resources.arsc 압축) | major | 테스터 툴링 | **자체 수정 완료 (같은 세션)** |
| 소셜 SDK 초기화 리그레션 (blocker, 이월) | blocker | frontend | **실기 재검증 완료 → 사실상 close.** 삼성 태블릿에서 카카오/구글/네이버 3종 정상 로그인 확인 |
| E2E 이슈 2 (로그인 후 전 화면 mock) | minor→ | frontend | **open, Tier-1 연동 요청서로 승격** (`ab94da8`) |
| MainStack 3화면 회귀 | major | frontend | **open (6일째).** 테스터가 이번 세션에 재확인 안 함 — 아래 정합성 참조 |
| BUG-20260901-17 (한글 placeholder) | major | frontend | open (미변경) |
| DARK-01/02, OBS-18 | minor | frontend | open (미변경) |
| 카카오 콘솔 앱 이름 "독서 기록 어플 개발" | minor | 운영/콘솔 | open — **사용자 액션** (앱ID `1502106`, 동의화면 노출 중) |
| 16KB page size 경고 | minor(제출 시 major) | frontend/build | open |

**이번 리포트 자체 신규 제품 결함: 0건** (시드 검증 전부 PASS, APK 이슈는 테스터 툴링 문제였고 자체 수정).

**제안된 자동화 테스트 실제 추가 여부:**
- 테스터는 이번에도 자동화 테스트 코드 미작성 (원칙 — 리포트/요청서만).
- 백엔드가 `159766d`에서 자체 테스트 3건 추가 (`SocialAuthControllerTest` / `UserLastLoginAtTest` / `UserResponseTest`).
- E2E 자동화 · 커버리지 측정(JaCoCo / `jest --coverage`) · `.gitattributes` — **여전히 미도입.**

**남은 실기기 확인 (사람 필요):** 구글/네이버 로그인 완료 흐름(계정 선택 창)은 리포트에 "정상 동작 확인"으로 기록됨. `@react-native-seoul/naver-login 5.0.1` 자체 한계로 네이버 앱 설치 단말에서 멈출 가능성 → 프론트에 라이브러리 버전 검토 요청 별건 유효.

---

## 영역 간 정합성 (추정 포함)

- **[확정 · 이월·악화] `MainStack` 3화면 회귀가 6일째 미수정 + 팀 내 인지 소실.** 직전 스크럼이 top-1로 올렸으나 이후 프론트 커밋이 손대지 않았고, 게다가 **테스터의 09-08 요청서(`ab94da8`)가 `GroupManagementScreen`·`ScheduleDetailScreen`을 "존재하는 mock 화면"으로만 언급** — 접근 불가 사실이 빠짐. (추정: 테스터가 09-01 E2E "13화면 PASS" 결과를 그대로 신뢰 중, 최신 빌드로 이 3화면 진입을 재검증하지 않음.) 스크럼 액션아이템이 실제 개발/테스터 세션으로 전달되는 경로가 약한 구조적 문제로 보임.
- **[정상 · 부분구현] "알림" 기능이 3영역에서 동시 진행.** 백엔드 `daysSinceLastLogin`(`159766d`) + 디자인 Android 알림 아이콘(`500217c`) + 프론트는 아직. 단 전체 `GET /api/notifications`는 백엔드에 없음(`NotificationController` 부재) — "1달 미접속 배너" 한 종류만 `/api/auth/me`로 가능. 테스터 요청서에 계약 명시(`daysSinceLastLogin`이 `null`이면 배너 미표시). 방향 일치, 부분 구현 상태.
- **[정상] 디자인 → 프론트 컴포넌트 동기.** 디자인 `6c342a9`(다이얼로그/액션시트/토스트)와 프론트 `b8a94fb`(`Toast.tsx`/`ConfirmDialog.tsx`)가 거의 동시 — 방향 일치.
- **[해소] 소셜 로그인 리그레션.** 실기 3종 정상 확인 → 소셜 로그인 마일스톤 사실상 종료. 꼬리: 카카오 콘솔 앱 이름(사용자), iOS E2E(맥 부재), 회원탈퇴 시 unlink.
- **[정상 진행] 대시보드 시드 ↔ 백엔드 로직.** 테스터 엣지케이스로 tie-break/null 로직 실측 PASS. 단 프론트 대시보드 탭이 백엔드 미연동이라 **앱 화면 실검증은 여전히 불가** — `GET /api/dashboard` 블랙박스로만 검증됨.
- **[이월 · 구조적] 디자인 커밋 파이프라인.** `commit_commands.txt` 7단계 백로그, step 2("_v1" 제거)·step 6(탭바 아이콘) 미실행. README ↔ 파일명 불일치 지속. 디자인 에이전트가 커밋 권한이 없고 PC 미연결로 작업하는 한 계속 누적되는 구조.
- 프론트가 참조하는 API 경로에 **백엔드에 없는 것 없음** (애초에 소셜 로그인 외 연동이 전무).

---

## PM 종합

### 영역별 한 줄 요약
- **백엔드:** 5일 침묵 후 재개 — 비활성 사용자 인앱 알림(`daysSinceLastLogin`) 설계+구현 + 자체 테스트 3건. 대시보드 tie-break/null 로직 테스터 실측 PASS.
- **프론트엔드:** 토스트/확인 다이얼로그/프론트 로거/온보딩 다이얼로그로 UX 피드백 보강. 그러나 **`MainStack` 3화면 회귀 6일째 그대로**이고 백엔드 연동은 여전히 소셜 로그인뿐.
- **디자인:** 확인 다이얼로그·하단 액션 시트 + Android 알림 아이콘 신규 커밋 2건. 탭바 filled 아이콘은 디스크 반영·미커밋(진행 중), "_v1" 리네임 등 커밋 백로그 이월.
- **테스터/QA:** 대시보드 시드 엣지케이스 실측 전부 PASS, arm64 슬림 APK 릴리스(설치 실패→원인 규명·자체 수정), Tier-1 백엔드 연동 요청서 프론트 전달. 소셜 로그인 3종 실기 정상 확인.

### 가장 급한 이슈 (top 2)
1. **[major · 6일 미수정] 프론트 `MainStack` 3화면 회귀 — `reading-record-frontend-agent` 즉시 조치.** `MainStack.tsx`에 `GroupManagement` / `ScheduleDetail` / `Notifications` 3개 `Stack.Screen` + import 재등록 → 릴리스 빌드 → 게스트 E2E로 일정 탭 벨(`ScheduleScreen:71`) / 일정 상세(`:180`) / 공유 탭 "그룹 관리"(`ShareScreen:71`) 진입 재확인. 커밋은 프레임 단위 분리. **추가로 테스터는 다음 세션에 이 3화면 진입을 명시적 재검증** — 현재 요청서가 회귀를 정상으로 취급 중.
2. **[major · 이월] BUG-20260901-17 — 서재 검색창 placeholder 한글 자모 분해.** `SearchBar.tsx` / `LibraryScreen.tsx`. 라이트·다크 무관(폰트/자모 조합). `reading-record-frontend-agent`.

**부차:** 카카오 콘솔 앱 이름 "독서 기록 어플 개발" → "콩닥책닥"(**사용자**, 앱ID `1502106`) · Railway 크레딧 결제 결정(**사용자**, 추정) · 디자인 `commit_commands.txt` 백로그 실행(**사용자**) · 16KB page size(스토어 제출 차단) · 네이버 로그인 라이브러리 버전 검토(프론트) · DARK-01/02·OBS-18.

### 영역 간 소통 / 정합성
- **스크럼 액션아이템 전달 경로가 약함 (추정).** 09-02 스크럼 top-1(`MainStack` 회귀)이 6일째 미수정이고, 테스터 요청서가 해당 화면을 정상으로 참조 → 스크럼에서 올린 이슈가 다음 개발/테스터 세션으로 실제 인계되지 않는 것으로 보임. 회귀 이슈는 별도 `docs/test/requests/` 요청서로 명시 전달하는 편이 확실.
- **디자인 커밋 파이프라인 구조적 병목 이월.** 디자인 에이전트 커밋 불가 + PC 미연결 → `commit_commands.txt` 명령이 계속 쌓임. 사용자가 배치로 실행해줘야 README/파일명 정합성이 맞춰짐.
- **"알림" 기능은 3영역이 각자 부분 구현 중** — 통합 `GET /api/notifications`가 백엔드에 나오기 전까지는 "1달 미접속 배너"만 실동작 가능. 프론트 착수 시 이 계약(`daysSinceLastLogin` null 처리) 준수 필요.
- **다음 연동 덩어리 = 프로필/서재/대시보드 API.** 백엔드 컨트롤러는 이미 배포됨(테스터 Tier-1 목록 확인). 프론트가 공용 `apiFetch` + 세션 영속화만 먼저 깔면 순차 전환 가능. 이게 풀려야 "로그인해도 전부 `책읽는 콩이`" 문제가 해결됨.

---

## 커밋 / 푸시 결과

- 커밋 대상: `docs/scrum/2026-09-08.md` 1개 파일만 (명시적 경로 `git add`). `design/` 미커밋 4파일은 stage하지 않음.
- 커밋 메시지: `[scrum] 2026-09-08 데일리 스크럼 정리`
- 결과: (아래 실행 로그 참조)
