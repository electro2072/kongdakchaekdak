# 데일리 스크럼 — 2026-09-01

- **관측 방식:** 모노레포 루트 `D:\projects\reading-record-app` 단일 git 저장소. 폴더별 `git log --since` + `--all` + 실제 파일/README·리포트 상태 대조. (이 스크럼은 `determined-turing-c1f856` 워크트리에서 실행하되 `git -C`로 메인 체크아웃을 관측)
- **폴더 매칭:** `backend/`, `frontend/`, `design/`, `docs/test/` 모두 존재.
- **관측 시각:** 2026-09-01 새벽 자동 세션 (로컬 머신 시계 = KST). 커밋 타임스탬프 모두 `+0900`.
- **브랜치:** `main`(기본) = `origin/main`. `test/book-service-unit-tests`는 마지막 커밋 2026-07-20 → **여전히 실질 비활성**(이번 기간 변화 없음). `claude/*` 워크트리 브랜치 6개.
- **직전 스크럼 이후 신규 커밋:** `ab43c0e`([scrum] 2026-08-30 정정, 08-31 10:59) 이후 **비-스크럼 13건**. 08-30에는 개발 커밋 0건, **08-31 22:28 ~ 09-01 00:27 사이에 전부 몰림**. 분포: backend/ 소스 0, frontend/ 소스 4, design/ 0, docs/test/ 2, docs(공통) 7.
- **워킹트리 미커밋 변경:** **있음 — `frontend/`에 진행 중 작업 8개 파일**(Frame 08.2 책 등록 확인 화면 구현). 아래 프론트엔드 §, PM 종합 §영역 간 정합성에서 상세. backend/design/docs는 clean.

> 직전 스크럼(2026-08-30)은 커밋 시각이 08-31 오전이었고 08-29 저녁 작업까지만 커버했다. 08-30(커밋 0건)과 08-31 저녁 대량 커밋 + 09-01 새벽 커밋은 이번 스크럼이 처음 다룬다.

---

## 백엔드

**신규 커밋: 0건 (`backend/` 소스 변경 없음).**

- `[backend]` 말머리 커밋 2건(`8398637`, `ceaf2bb` "아키텍처 구조 문서 초안")은 **`docs/`에 아키텍처 문서(md+html)를 추가/치환한 것**이지 `backend/` 코드가 아니다. `ceaf2bb`는 파일명 `_v1`→`__v1` 오타 정정으로 보이는 0-byte diff(2 files, +0/-0).
- 화이트박스 상태(테스터 `2026-08-31.md` 확인): `./gradlew build` **BUILD SUCCESSFUL, 74 tests / 0 fail**, jar 패키징 정상. 이번 기간 변화 없음.
- 배포: Railway `kongdakchaekdak-production.up.railway.app` 라이브 유지(`/health` 200). 이번 기간 재배포 트리거 커밋 없음.

**미해결 작업 (직전 스크럼·테스터 리포트 기반, 이번 기간 미착수):**
- **RENAME-02 (major, open, 추정) — DB명 변경(`reading_record_app`→`kongdakchaekdak`) 데이터 마이그레이션 안내.** `docs/독서기록앱_TODO리스트_v2.md`를 재확인했으나 마이그레이션 안내 항목은 여전히 없음. 관련 커밋도 없음 → **open 유지(추정).**
- **FINDING-20260827-04 잔여 (minor, open) — `DashboardPeriod` enum 소문자 미수용** (`"month"`→400 / `"MONTH"`→201 추정). 이번 기간 커밋 없음.
- **BUG-20260827-03 "조각2" (open) — AWS 자격증명 없이 presigned-URL 실제 호출 시 graceful 실패** 확인. 기동 blocker(조각1)는 `6b710b1`로 해소됐으나 업로드 경로 자체의 우아한 실패는 미검증.
- **AWS 사진 업로드 키** `backend/.env` / Railway 환경변수 미입력 — `소셜로그인_설정현황.md` 명시. 기동은 되므로 blocker 아님.
- 소스 내 `TODO/FIXME` 4곳 (연동 대기 지점, 이전과 동일).

---

## 프론트엔드

**신규 커밋: 4건 (`frontend/` 소스).**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-31 22:50 | `4bb9c86` | **앱 빌드 오류 대응 — 의존성 버전 전면 고정.** `dependencies`의 `^`/`~` 제거, `react-native-safe-area-context` `^5.8.0`→`4.10.8`(major 다운, RN 0.74 비호환 해소), svg/lucide/navigation 등 정확 버전 픽스. 테스터가 `2026-08-31.md`에서 **Android 디버그·릴리스 APK 정상 생성 확인** → "빌드 안 됨" 해소 |
| 08-31 23:08 | `658bcb9` | **6개 장르 고정색 프론트 반영 (FINDING-20260828-11 대응).** `constants/genreColors.ts` 신설(+57), `GenreDonutChart.tsx` 등수 순환 → 고정 매핑, `theme/colors.ts`에 장르 컬러 추가, Library/ProfileEdit/Signup 칩에 적용. **design(`b325db8`, 08-28) 확정 후 ~3일 만에 연결** |
| 09-01 00:08 | `5e25dbb` | **한글(IME 조합 문자) 입력 버그 조치.** `SignupScreen`/`ProfileEditScreen` 닉네임 입력에 검증 규칙(2~10자, 한글·영문·숫자만) + `profileOptions.ts`(+11), `LibraryScreen.tsx`(+3) |
| 09-01 00:27 | `49c6d9f` | **Frame 08.2(책 등록 확인) + Frame 05.2(프로필 편집) 연결 설계 문서**를 `frontend/README.md`에 추가(+128). 코드가 아니라 설계 문서(사용자가 "코드보다 설계 먼저" 요청). 실제 구현은 워킹트리에서 진행 중(아래) |

**요약:** 08-31 저녁 빌드 blocker(`4bb9c86`)를 먼저 잡고, 그 위에서 **직전 스크럼의 열린 minor 2건을 정리** — 6장르 고정색 반영(FINDING-11)·한글 입력 규칙. 그리고 **가장 오래 방치됐던 major인 Frame 08.2(FINDING-08) 구현에 착수** — 먼저 설계 문서(`49c6d9f`)를 커밋하고, 관측 시점 현재 코드를 작성 중.

**⚠️ 워킹트리 미커밋 변경 (관측 시점 진행 중) — Frame 08.2 구현:**
- 신규: `src/screens/BookRegisterConfirmScreen.tsx`, `src/navigation/LibraryContext.tsx`
- 수정: `App.tsx`, `navigation/MainStack.tsx`, `navigation/types.ts`, `screens/BookDetailScreen.tsx`, `screens/BookSearchScreen.tsx`, `screens/LibraryScreen.tsx`
- 설계 문서(`frontend/README.md`)대로 `LibraryContext`(서재 mock state) 신설 + `BookSearch` → `BookRegisterConfirm`(장르 단일선택) → `popToTop` → `BookDetail` 동선. `bookService`에 `createBook`류 없음 / `POST /api/books` 미구현이라 **영속화 없는 mock state로 처리**(설계 문서에 감수 명시).
- **정합성 주의 (추정) — 아래 두 변경은 커밋 전 검토 필요:**
  1. `App.tsx`에서 `GoogleSignin.configure(...)` + `initializeNaverLogin()` 초기화 호출과 관련 import를 **삭제**. 이 초기화는 **오늘(09-01) E2E에서 PASS한 구글·네이버 로그인의 전제**다. 그대로 커밋되면 소셜 로그인 회귀 가능성(추정) — `LibraryProvider` 추가만 남기고 SDK 초기화는 유지해야 할 것으로 보임.
  2. `MainStack.tsx`에서 `GroupManagement`/`ScheduleDetail`/`Notifications` 3개 `Stack.Screen` 등록을 **삭제**(단 `types.ts`엔 타입 정의가 남아 있음). 이 3화면은 오늘 E2E(`2026-09-01.md` 화면 9·11·8)에서 **렌더·네비게이션 정상 확인된 화면**이다. 상위 스택 어디에도 재등록이 안 보임(grep 결과 `types.ts`에만 잔존) → 그대로 커밋 시 3화면 접근 불가 회귀(추정).
  3. `BookSearchScreen.tsx`가 프로젝트 관례(작은따옴표)와 다른 큰따옴표·다른 포매팅으로 전체 재작성됨 — OBS-01(lint CRLF/prettier 노이즈)과 겹쳐 diff 노이즈 큼.
- → **frontend-agent가 이 워킹트리를 최신 `main` 기준으로 재정리한 뒤(특히 App.tsx SDK 초기화 복원, 3화면 재등록) 커밋할 것**을 권장.

**미해결 작업 (테스터 리포트 기반):**
- **FINDING-20260828-08 (major) — Frame 08.2 미구현** → **진행 중(관측 시점).** 설계 커밋 완료, 코드 워킹트리에 존재. 다음 세션에서 커밋·빌드·E2E 재확인 시 close 판단.
- **BUG-20260901-17 (major, open) — 서재 검색창 placeholder 한글 자모 분해 렌더**(`LibraryScreen`). 테스터가 테스트한 APK는 `5e25dbb`(한글 IME 조치) **이전** 빌드 → 같은 뿌리일 수 있어 **최신 빌드로 재확인 필요**. `5e25dbb`가 `LibraryScreen.tsx`를 실제로 건드림(+3).
- **OBS-20260901-18 (minor, open) — 책 검색 "검색" 버튼이 테마 그린 아닌 머티리얼 파란색**(`SearchBar.tsx` 레거시 컴포넌트 추정). 이번 기간 미대응.
- **FINDING-20260828-11 재확인 — 관심분야 칩 색.** `658bcb9`로 조치됨. 테스터가 본 APK는 `658bcb9` 이전 빌드라 "칩 전부 그린"으로 관측 → **최신 빌드로 재확인 필요**(Dashboard 도넛엔 이미 반영 확인됨).
- **OBS-01 (이월) — `npm run lint` 469 errors** (CRLF/prettier, `.gitattributes` 부재). 프론트 CI에서 lint 스텝 제외 유지. 진행 중 워킹트리의 `BookSearchScreen` 재포매팅이 이 문제를 재노출.
- 리네임 잔재(minor, 이월): `package-lock.json` name, `android/settings.gradle` `rootProject.name`, iOS 프로젝트/타깃/스킴명.
- 소스 내 `TODO/FIXME` 14곳.

---

## 디자인

**신규 커밋: 0건 (`design/` 변경 없음).**

- `design/README.md` 산출물 상태표 기준(직전 스크럼과 동일):

| 산출물 | 상태 |
|---|---|
| `app_icon.html` v2.1 / `splash_screen.html` v1 / `color_chips.html` v2 / `typography_and_icons.html` v1.2 | 확정 |
| `hifi_mockup.html` v1.11 (태블릿 재설계 + Frame 02 + 6장르색) | 확정·커밋 완료 |
| Google Play 피처 그래픽·고해상도 아이콘 (`store_assets.html` v1) | 제작 완료 |
| App Store/Play 마케팅 스크린샷 | **미착수** — 실제 앱 화면·캡션 선행 결정 필요 |

**미해결 / 대기:**
- 6장르 고정색 → 프론트 반영: **이번 기간 frontend에서 완료(`658bcb9`)** — design→frontend 열린 연결 고리 해소. (실기 재확인만 남음)
- 마케팅 스크린샷: 에뮬레이터 실화면(게스트 경로 13화면은 오늘 캡처됨, git 미포함) + 사용자 캡션 결정 대기. **디자인 영역 3일째 실질 정지.**

---

## 테스터 / QA

**신규 커밋: 2건 (`[test]` 접두사).**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-31 23:02 | `7939de5` | **리포트 `2026-08-31.md` — 앱 빌드 검증.** 백엔드 `./gradlew build`(74 tests/0 fail) · Android 디버그 APK(135.8MB) · Android 릴리스 APK(58.8MB, JS 번들 내장 = 단독 실행) **3종 전부 BUILD SUCCESSFUL.** `4bb9c86` 버전 고정으로 "빌드 안 됨" 해소 확인. iOS만 미검증(맥 부재) |
| 09-01 00:24 | `3769f81` | **리포트 `2026-09-01.md` — Android 에뮬레이터 화면 E2E(게스트 경로).** 테스터가 adb로 직접 구동(`Pixel_7` API 34 + `app-release.apk`). "비회원으로 둘러보기" 진입 후 **13개 화면 전부 렌더 정상 · 크래시 없음 · 네비게이션 정상 · logcat 에러 없음** |

**최신 리포트:** `docs/test/reports/2026-09-01.md` (2026-09-01). 대상 APK는 `0eaa12f` 이전 빌드 — `658bcb9`·`5e25dbb` 이후 커밋 미포함(리포트에 명시).

**진행 요약:**
- **빌드 검증 라운드 완료** — `4bb9c86`(의존성 고정) 이후 백엔드·Android 디버그·릴리스 3종 빌드가 전부 성공함을 화이트박스로 확인. 릴리스 APK가 Metro 없이 단독 실행 가능(Hermes 번들 내장)함까지 검증.
- **게스트 경로 화면 E2E 완료** — 테스터가 `adb input tap` + `uiautomator dump` 좌표로 13화면을 직접 순회(프론트 에이전트가 "탭이 안 먹힌다"고 한 것에 대한 반박: `bounds` 중심값을 쓰면 정상). FINDING-10(이전 미구현 3화면)이 반영됐음을 재확인.
- **소셜 로그인 3사 E2E는 프론트 에이전트가 별도 실행** — 카카오/구글/네이버 Android 에뮬레이터 실기 **3사 전부 PASS**(2026-09-01), 로그아웃 정상. 결과는 `docs/소셜로그인_설정현황.md` "E2E 테스트 결과" 절 + 커밋 `47f2fa4`. **직전 스크럼 top 1 이슈였던 "소셜 로그인 실기기 E2E"가 닫힘.**

**발견된 이슈 (이번 기간 누적, 심각도별):**

| ID | 심각도 | 영역 | 상태 | 비고 |
|---|---|---|---|---|
| BUG-20260901-17 (서재 검색 placeholder 한글 깨짐) | **major** | frontend | **open** | 테스트 APK가 `5e25dbb` 이전 — 최신 빌드 재확인 필요 |
| OBS-20260901-18 (책 검색 "검색" 버튼 파란색, 테마 미적용) | minor | frontend | open | `SearchBar.tsx` 레거시 추정 |
| FINDING-20260828-08 (Frame 08.2 미구현) | **major** | frontend | **진행 중** | 설계 커밋 + 워킹트리 코드 존재 |
| FINDING-20260828-11 (관심분야 칩 색) | minor | frontend | **조치됨(`658bcb9`)** | 최신 빌드 재확인 필요 |
| E2E 이슈 1 (카카오 콘솔 앱 이름 "독서 기록 어플 개발") | minor | 운영/콘솔 | **open** | Kakao 앱 ID `1502106` → "콩닥책닥"으로 변경. 로그인 동의화면 노출. **사용자 액션 필요** |
| E2E 이슈 2 (신규 유저에게 mock 데이터 노출) | minor | frontend | open | 일정/서재 탭 미연동, 게스트 E2E에서도 재확인 |
| E2E 이슈 3 (16KB page size 미대응 경고) | minor(제출 시 major) | frontend/build | open | **Google Play 제출 차단 요인.** RN 0.75+ / NDK r27 필요. 스토어 준비 단계 처리 |
| E2E 이슈 4·5 (Metro 재연결 토스트 / API 37 프리뷰 이미지 Chrome 꼬임) | 개발환경 | — | 정보성 | 앱 기능 무해 |
| RENAME-02 (DB명 마이그레이션 안내) | major | backend | **open (추정)** | 이번 기간 커밋 없음, TODO 문서에도 없음 |
| BUG-20260827-03 조각2 (presigned-URL graceful 실패) | (파생) | backend | open | 기동 blocker는 해소됨 |
| FINDING-20260827-04 잔여 (`DashboardPeriod` enum 소문자) | minor | backend | open | 이번 기간 커밋 없음 |
| OBS-01 (lint CRLF 469 errors) | — | frontend | open | 프론트 CI에서 제외 유지 |

**제안된 자동화 테스트 실제 추가 여부:**
- 이번 기간 테스터 리포트는 **신규 자동화 테스트를 제안하지 않음**(빌드 검증·수동 E2E 위주).
- 기존: 백엔드 CI(`backend-test.yml`)·프론트 CI(`frontend-test.yml`, typecheck+jest, lint 제외)는 직전 라운드에서 추가됨 — 이번 기간 변경 없음.
- 커버리지 측정(JaCoCo / `jest --coverage`), `.gitattributes`(OBS-01), 자동 E2E: **여전히 미도입.**
- 다음 예정(리포트 §미실시): 다크모드 13화면(사용자 지시 2026-09-02), 폼 입력 상호작용, 태블릿/폴드 레이아웃.

**영역 간 정합성 문제 (추정 포함):**
- **[신규·추정, 높은 우선순위] 프론트 워킹트리 미커밋 변경이 오늘 PASS한 기능을 되돌릴 소지.** Frame 08.2 구현 중인 워킹트리가 `App.tsx`의 소셜 SDK 초기화 호출과 `MainStack.tsx`의 3개 화면(Group/Schedule/Notification) 등록을 삭제한 상태 — 둘 다 이번 기간에 "정상"으로 검증된 것. WIP라 아직 커밋 전이지만, frontend-agent가 최신 `main` 기준으로 재정리 후 커밋해야 함. (프론트 CI의 typecheck는 `types.ts`에 타입이 남아 통과할 수 있어 이 회귀를 못 잡을 수 있음 — 추정.)
- **[해소] design → frontend 6장르색 전달 지연** — `658bcb9`로 반영 완료(약 3일 소요).
- **[진행] 신규 유저 mock 데이터(E2E 이슈 2)** — 로그인·온보딩만 백엔드 연동, 나머지 탭은 mock/empty-state 미구현. Frame 08.2가 `LibraryContext`(mock state)를 도입하지만 이것도 `POST /api/books` 없이는 영속화 불가 — 프론트–백엔드 연동의 다음 큰 덩어리는 **도서/서재 API**.
- **[이월] 구글 브랜딩 인증** — `PRIVACY.md` 내용 완성(`0eaa12f`로 추가 보완, +53/-15). 남은 건 공개 `https://` 호스팅 + 콘솔 URL 등록, 실도메인 대기. `콩닥책닥_배포_필요문서_목록.md`(`edcfdfd`)에 배포 문서 체크리스트 신규 정리됨.
- 프론트가 참조하는 API 경로는 모두 백엔드에 실재(없는 API 참조 없음). 단 `POST /api/books`는 프론트가 "있었으면 하는" 미구현 API로 설계 문서에 명시.

---

## PM 종합

### 영역별 한 줄 요약
- **백엔드:** `backend/` 소스 커밋 0건. 아키텍처 문서(`docs/`)만 추가. 빌드·배포·74 tests 그린 유지. 잔여는 RENAME-02(DB 마이그레이션 안내, 여전히 문서에도 없음)·`DashboardPeriod` enum·BUG-03 조각2 — **3일째 미착수.**
- **프론트엔드:** 08-31 빌드 blocker(`4bb9c86`)를 잡고 그 위에서 6장르색(FINDING-11)·한글 입력 규칙을 정리, **가장 오래된 major인 Frame 08.2(FINDING-08) 구현에 착수**(설계 커밋 + 워킹트리 코드). 다만 미커밋 워킹트리가 오늘 검증된 소셜 SDK 초기화·3개 화면 등록을 삭제한 상태라 **커밋 전 재정리 필요.**
- **디자인:** 커밋 0건, 3일째 실질 정지. 유일한 열린 연결 고리(6장르색)는 프론트가 이번에 소화. 다음 작업(마케팅 스크린샷)은 앱 화면 + 사용자 결정 대기.
- **테스터/QA:** 빌드 3종 검증 + 게스트 경로 13화면 E2E 완료. 소셜 로그인 3사 실기 E2E PASS(프론트가 실행, 테스터가 추적)로 **직전 스크럼 top 1이 닫힘.** 신규 major 1건(BUG-17 서재 검색 한글) 발견, 단 구버전 APK라 재확인 대기.

### 가장 급한 이슈 (top 2)
1. **[신규·추정] 프론트 워킹트리 미커밋 변경의 회귀 소지 — frontend-agent 재정리 후 커밋.** Frame 08.2 구현 중인 워킹트리(`App.tsx`, `MainStack.tsx` 등 8파일)가 **오늘 E2E에서 PASS한 구글·네이버 로그인 초기화 호출**과 **정상 확인된 3개 화면(GroupManagement/ScheduleDetail/Notifications) 등록**을 삭제한 상태. WIP이므로 아직 실피해는 없으나, 그대로 커밋되면 소셜 로그인 + 3화면 접근이 깨질 수 있음(추정). 최신 `main` 기준으로 rebase/재정리 → App.tsx SDK 초기화 복원 + 3화면 재등록 → 커밋 → 빌드·E2E 재확인. **frontend-agent.**
2. **BUG-20260901-17 (major, open) — 서재 검색창 placeholder 한글 자모 분해.** `LibraryScreen`. `5e25dbb`(한글 IME 조치)가 같은 파일을 건드렸으니 **최신 빌드(≥`5e25dbb`)로 재확인**이 먼저 — 이미 고쳐졌으면 close, 아니면 frontend-agent가 추가 조치. FINDING-11(칩 색)도 같은 "최신 빌드 재확인" 묶음.

부차: 카카오 콘솔 앱 이름 "콩닥책닥" 변경(**사용자 액션**, 로그인 동의화면 노출 중), 16KB page size 대응(스토어 제출 차단 요인, 준비 단계), RENAME-02 DB 마이그레이션 안내(backend, 3일째 방치), `DashboardPeriod` enum 소문자 수용(backend), OBS-20260901-18 검색 버튼 테마색(frontend).

### 영역 간 소통 / 정합성
- **피드백 루프 한 사이클 완주(부분).** 직전 스크럼의 열린 minor 2건(6장르색·한글 입력)과 top 1(소셜 E2E)이 이번 기간에 닫힘. top 2였던 Frame 08.2도 착수됨. 반면 **backend 잔여 3건은 3일째 무변동** — 백엔드 담당의 이번 주기 활동이 문서 작성에 집중됨.
- **"커밋 전 로컬 검증" 이슈 재발 우려(추정).** 이번엔 CI가 아니라 워킹트리 단계에서 포착 — Frame 08.2 WIP가 무관한 코드(소셜 초기화, 3화면 등록)를 삭제한 채로 있음. 프론트 CI typecheck는 `types.ts`에 타입이 남아 있어 이 회귀를 놓칠 수 있으므로, 커밋 시 실제 빌드 + 게스트/소셜 E2E 재확인이 필요.
- **소셜 로그인 마일스톤 사실상 종료** — 3사 Android 실기 E2E PASS. 남은 꼬리: 카카오 콘솔 앱 이름(사용자), iOS E2E(맥 부재), 재로그인 시 온보딩 스킵 미검증, 구글 브랜딩 인증(실도메인 대기).
- **다음 연동 덩어리 = 도서/서재 API.** Frame 08.2가 `LibraryContext` mock state를 도입하지만 `POST /api/books` 부재로 영속화 불가. 신규 유저 mock 데이터 노출(E2E 이슈 2)도 같은 뿌리 — backend가 이 API를 열어줘야 프론트 서재/일정 탭이 실데이터로 전환 가능.
- **디자인 영역 3일째 유휴** — 마케팅 스크린샷은 프론트 화면 안정 + 사용자 캡션 결정 대기. 당분간 이 상태 예상.

---

## 커밋/푸시 결과
- 커밋: `6621605` `[scrum] 2026-09-01 데일리 스크럼 정리` (`docs/scrum/2026-09-01.md` 1개 파일만 stage — 프론트 워킹트리 미커밋 변경 8개는 건드리지 않음) → 푸시 ✅ `49c6d9f..6621605`
- 커밋: `<이 커밋>` `[scrum] 2026-09-01 스크럼 — 커밋/푸시 결과 기록` → 푸시 결과는 실행 로그 참조
