# 데일리 스크럼 — 2026-08-30

- **관측 방식:** 모노레포 루트 `D:\projects\reading-record-app` 단일 git 저장소. 폴더별 `git log --since` + `--all` + 실제 파일/README·리포트 상태 대조.
- **폴더 매칭:** `backend/`, `frontend/`, `design/`, `docs/test/` 모두 존재.
- **관측 시각:** 2026-08-30 새벽 자동 세션 (로컬 머신 시계는 KST). 커밋 타임스탬프는 모두 `+0900`.
- **브랜치:** `main`(기본) = `origin/main`. `test/book-service-unit-tests`는 마지막 커밋 2026-07-20, `origin/main` 대비 0 ahead / 93 behind — **실질 비활성**(이번 기간에도 변화 없음). `claude/*` 워크트리 브랜치 4개.
- **직전 스크럼 이후 신규 커밋:** `5ee8013`([scrum] 2026-08-29, 08-29 08:11) 이후 **18건** — 08-29 21:29 ~ 23:42 사이에 몰림. backend 5, frontend 6, design 1, test 4, docs(기획/공통) 2.
- **워킹트리 미커밋 변경:** 없음 (`git status` clean — backend/frontend/design/docs 모두).

> 직전 스크럼(2026-08-29)은 08-28 작업만 커버했고, 08-29 저녁의 대량 커밋은 이번 스크럼이 처음 다룬다.

---

## 백엔드 (신규 커밋 5건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-29 21:33 | `849a3e4` | **Gradle wrapper 도입** — `gradlew`/`gradlew.bat`/`gradle/wrapper/*` (8.14.3) 커밋. 시스템 gradle 없이 `./gradlew` 실행 가능 (+365줄) |
| 08-29 21:38 / 22:01 | `4900694` + `95421f0` | **백엔드 CI** — `.github/workflows/backend-test.yml` 신규. `backend/**` push/PR 시 JDK17 + `./gradlew test --no-daemon`, 실패 시 test 리포트 아티팩트 업로드. 커밋 메시지가 "큰 변경마다 테스트 빌드 깨지는 회귀 반복"을 명시 (BUG-01/RENAME-00 재발 방지 목적) |
| 08-29 22:33 | `07836ad` | **`TokenResponse.isNewUser`(boolean) 추가** — 프론트 요청 대응(로그인 응답만으로 신규/기존 판별). `SocialAuthService` find-or-create가 신규면 `true`, 기존이면 `false` → 카카오/구글/네이버 3개 엔드포인트 응답 전부 반영 (4 files, +31/-9) |
| 08-29 22:50 | `6b710b1` | **BUG-03 조각1** — `BookPhotoService`에서 Lombok `@RequiredArgsConstructor`를 걷어내고 명시적 생성자 + `S3Presigner` 파라미터에만 `@Lazy`. 주입 지점이 지연 프록시를 받도록 해 컨텍스트 초기화 중 빈 생성을 미룸 (직전 스크럼 top 이슈에 대한 3차 대응) |
| 08-29 23:27 | `be58577` | Railway 배포 URL 교체 (`kongdakchaekdak-production.up.railway.app`) — 1 file, +3/-4 |

**요약:** 직전 스크럼의 최우선 이슈였던 **BUG-03(로컬 서버가 AWS 자격증명 없이 기동 실패)에 세 번째 대응이 나왔고, 이번엔 테스터가 해소를 확인**(아래 QA 섹션 §BUG-03). 여기에 더해 인프라 정비가 한 세션에 몰림 — gradle wrapper, 백엔드 CI, `isNewUser` API 필드, Railway 재배포. 화이트박스 테스트는 wrapper 기준 74 tests / 0 fail 유지(테스터 확인).

**미해결 작업 (테스터 리포트·이전 스크럼 기반):**
- **RENAME-02 (major, open, 추정) — DB명 변경(`reading_record_app`→`kongdakchaekdak`) 데이터 마이그레이션 안내.** 이번 기간에 관련 커밋 없음. `docs/독서기록앱_TODO리스트_v2.md`가 갱신됐으나(`83afb45`) 마이그레이션 안내가 추가됐는지는 미확인 → 다음 세션에서 문서 대조 필요.
- **FINDING-20260827-04 잔여 (minor, open) — `DashboardPeriod` enum 소문자 미수용.** 이번 기간 커밋 없음. 요청 바디 `"dashboardPeriod":"month"` → 400, `"MONTH"` → 201 상태 그대로로 추정.
- **BUG-03 "조각2" (open) — 자격증명 없이 presigned-URL 실제 호출 시 graceful 실패** 확인은 아직. 기동 blocker는 해소됐지만 업로드 경로 자체의 우아한 실패는 테스터 다음 세션 대상.
- **AWS 키(사진 업로드) `backend/.env` / Railway 환경변수 미입력** — `소셜로그인_설정현황.md`에 명시. 기동은 되므로 blocker 아님.
- 소스 내 `TODO/FIXME` 4곳 (주로 연동 대기 지점).

---

## 프론트엔드 (신규 커밋 6건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-29 22:02 | `0e5c993` | **소셜 로그인 네이티브 SDK 3종 설치 + 네이티브 키 설정** — `@react-native-seoul/kakao-login` 6.0.4, `.../naver-login` 5.0.1, `@react-native-google-signin/google-signin` 16.1.4. AndroidManifest·Info.plist·Podfile·AppDelegate.mm·strings.xml (+443/-256) |
| 08-29 22:38 | `8e2d865` | **LoginScreen 실제 SDK 연동** — `API_BASE_URL` 추가, `isNewUser` 분기(신규→`Signup` 화면, 기존→`login()` 바로 탭 진입), provider별 로딩 상태·취소 처리 (8 files, +336/-23) |
| 08-29 22:53 | `134f13e` | **FINDING-13·14 수정** — `naverAuth.ts` `successResponse` 널 가드(typecheck 회귀), 카카오/구글/네이버 jest mock 3개 추가(App.test.tsx 실행 실패 회귀) (4 files, +47) |
| 08-29 22:57 / 23:01 | `69220ee` + `7975533` | **프론트 CI** — `.github/workflows/frontend-test.yml` 신규. `frontend/**` push/PR 시 Node 20 + `npm ci` + `npm run typecheck` + `npm test`. (`lint`는 OBS-01 469 errors 때문에 이번엔 제외 — 워크플로 주석에 명시) |

**요약:** 소셜 로그인 프론트–백엔드 실연동의 프론트 쪽 작업이 한 세션에 완성됨 — 네이티브 SDK 3종 + 키 설정 + LoginScreen SDK 호출 + `isNewUser` 온보딩 분기. 이 라운드가 typecheck·jest 회귀 2건(FINDING-13·14)을 동반했으나 같은 세션 후속 커밋에서 수정, 테스터가 재실행으로 확인(`npx tsc --noEmit` exit 0, `npm test` 6/6 스위트·20/20). **직전 스크럼에서 반복 지적된 "프론트 CI 부재"(FINDING-16)도 이번에 해소** — 다만 리포트 갱신(22:55) 이후 커밋이라 테스터 재검증은 아직.

**미해결 작업 (테스터 리포트 기반, 담당 개발자 미반영):**
- **FINDING-20260828-08 (major, open) — Frame 08.2(책 등록 확인) 미구현.** 이번 기간 커밋 없음. `BookSearchScreen`에서 책 선택 시 여전히 `Alert` → `goBack`, 장르 선택·시작일·"등록하기" 단계 없음 → 책 검색→서재 등록 핵심 동선 단절. **이제 가장 오래 방치된 열린 major.**
- **FINDING-20260828-11 (minor, open) — 6개 장르 고정색 프론트 미반영.** design은 08-28 `b325db8`로 확정. 프론트 `theme/colors.ts`는 여전히 `chart1~4`(4색) 추정, `GenreDonutChart` 등수 순환 방식 유지. design→frontend 리드타임 약 2일 경과.
- **OBS-01 (이월) — `npm run lint` 469 errors** (CRLF/prettier 노이즈, `.gitattributes` 부재). 프론트 CI에서도 lint 스텝은 이 때문에 제외됨 — 정리 후 CI에 추가 권장.
- `.env` `API_BASE_URL` 미설정 → fallback `http://10.0.2.2:8080`(에뮬레이터→호스트 localhost). 아래 정합성 참고.
- 리네임 잔재 (minor, 이월): `package-lock.json` name `ReadingRecordApp`, `android/settings.gradle` `rootProject.name`, iOS 프로젝트/타깃/스킴명.
- 소스 내 `TODO/FIXME` 14곳.

---

## 디자인 (신규 커밋 1건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-29 21:29 | `9efcc3d` | 산출물 이름 표기에서 불필요한 `_v1` 접미사 일괄 제거 (파일명/문서 8 files, +42/-36). 화면 내용·색상·버전 표기 변경 없음 |

**요약:** 실질적 디자인 작업은 **변경 없음.** 이번 커밋은 파일명 정리(하우스키핑)뿐. 08-28 `b325db8`(6장르 고정색) 이후 디자인 산출물 자체는 정지 상태.

**산출물 상태 (`design/README.md` 표 기준 — 이전 스크럼과 동일):**

| 산출물 | 상태 |
|---|---|
| `app_icon.html` v2.1 / `splash_screen.html` v1 / `color_chips.html` v2 / `typography_and_icons.html` v1.2 | 확정 |
| `hifi_mockup.html` v1.11 (태블릿 재설계 + Frame 02 + 6장르색) | 확정·커밋 완료 |
| Google Play 피처 그래픽·고해상도 아이콘 | v1 제작 완료 |
| App Store/Play 마케팅 스크린샷 | **미착수** — 실제 앱 화면·캡션 선행 결정 필요 |

**미해결 / 대기:**
- 6장르 고정색 → 프론트 반영(FINDING-11) 여전히 미완 — design 단독 이슈 아님(frontend-agent 대상).
- 마케팅 스크린샷: 에뮬레이터 실화면 + 사용자 결정 대기.

---

## 테스터 / QA (신규 커밋 4건 + 관련 문서)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-29 22:44 | `6505dfe` | **리포트 `2026-08-29.md` 초판** — 피드백 반영 + 소셜 로그인 SDK 연동 검증 (+126줄) |
| 08-29 22:55 | `8386733` | **리포트 갱신** — FINDING-15 철회, 13/14 closed 확인, BUG-03 조각1 확인 (+49/-21) |
| 08-29 23:42 | `90b9107` | **소셜 로그인 온보딩 E2E** — `scenarios/social-login-onboarding-e2e.md` 신규 + 리포트 `2026-08-29b.md`(사전 점검 + 실행 체크리스트) (3 files, +187/-1) |

관련(개발/공통) 커밋: `2587445`(PRIVACY.md 추가), `29957e9`(소셜 로그인 3사 콘솔 현황 문서화 + TODO 갱신), `83afb45`(TODO리스트·소셜로그인 설정현황에 Railway 배포·SDK 연동 완료 반영).

**최신 리포트:** `docs/test/reports/2026-08-29b.md` (E2E 사전 점검 + 실행 체크리스트, HEAD `83afb45` 기준). 그 전 `2026-08-29.md`(피드백 반영 + SDK 연동 검증, 두 차례 갱신).

**진행 요약:**
- **소셜 로그인 SDK 연동 라운드를 화이트박스로 검증** — 백엔드 `./gradlew test` 74 tests / 0 fail, `TokenResponse.isNewUser`가 Jackson으로 `"isNewUser"`로 직렬화됨(프론트 `authApi.ts` 필드명 일치) + OpenAPI 스키마 노출 확인. `SocialAuthControllerTest` `isNewUser` true/false 단언 7건(3사) 통과.
- **프론트 회귀 2건 잡아냄** — FINDING-13(`npm run typecheck` 회귀), FINDING-14(`npm test` `App.test.tsx` 실행 실패) → 둘 다 같은 세션 후속 커밋에서 수정, 테스터 재실행 확인, **closed**.
- **FINDING-15(react-native-screens 버전 불일치) 철회** — 테스터가 실제 확인 없이 단정한 오판. 재확인 결과 `react-native-screens ~4.8.0` + RN 0.74 + Paper 렌더러(`newArchEnabled=false`)는 공식 지원 범위. 프론트 반박이 옳았음.
- **E2E 시나리오는 사람 실행 대기** — FE-SL-01~09는 테스터 에이전트가 Android 에뮬레이터 조작·소셜 계정 로그인을 자동화할 수 없어 사람이 직접 눌러야 함. 테스터는 사전 점검(빌드/타입/계약/취소 처리)만 완료: `tsc` PASS, `npm test` 20/20, Metro 번들 SUCCESS(3.2MB), 취소 처리 3사 코드가 시나리오 문서와 정확히 일치.
- 백엔드 A→B 계약 검증(`isNewUser` true→false 전환)은 **유효한 카카오 access token 1개 필요 → 사용자 제공 대기 중**.

**발견된 이슈 (누적, 심각도별):**

| ID | 심각도 | 영역 | 상태 | 반영 |
|---|---|---|---|---|
| BUG-20260829-13 (FE typecheck 회귀) | major | frontend | **closed** (`134f13e`) | ✅ 테스터 재확인 |
| BUG-20260829-14 (FE `npm test` 회귀) | major | frontend | **closed** (`134f13e`) | ✅ 테스터 재확인 |
| FINDING-20260829-15 (screens 버전) | minor | frontend | **철회(withdrawn)** | — 테스터 오판 |
| **BUG-20260827-03 (`local` 기동 실패)** | **major** | **backend** | **조각1로 blocker 해소** (`6b710b1`) | ✅ 테스터 `gradle bootRun` 정상 기동 확인 / "조각2"만 잔여 |
| **FINDING-20260828-08 (Frame 08.2 미구현)** | **major** | **frontend** | **open** | ❌ 이번 기간 미착수 |
| **RENAME-02 (DB명 마이그레이션 안내)** | **major** | **backend** | **open (추정)** | ❌ 관련 커밋 없음 |
| FINDING-20260829-16 (프론트 CI 부재) | minor | frontend | **조치됨** (`69220ee`/`7975533`) | ⚠️ 리포트 갱신 이후 커밋 — 테스터 재검증 아직 |
| FINDING-20260827-04 잔여 (`DashboardPeriod` enum) | minor | backend | open | ❌ |
| FINDING-20260828-11 (프론트 6장르 고정색) | minor | frontend | open | ❌ design만 완료 (~2일 경과) |
| OBS-01 (lint CRLF 469 errors) | — | frontend | open | ❌ 프론트 CI에서도 제외됨 |
| RENAME-03~06 (package-lock, android settings, iOS 프로젝트명) | minor | frontend | open | ❌ |

**제안된 자동화 테스트 실제 추가 여부:**
- **백엔드 CI (`backend-test.yml`) 추가됨** (`4900694`/`95421f0`) — 리포트 FINDING-16(초판)이 지목. `backend/**` 한정.
- **프론트 CI (`frontend-test.yml`) 추가됨** (`69220ee`/`7975533`) — 리포트 FINDING-16 갱신본이 지목. `frontend/**` 한정, typecheck + jest. lint는 제외.
- 소셜 SDK jest mock 3개 추가됨(`134f13e`) — FINDING-14 대응.
- 커버리지 측정(JaCoCo / `jest --coverage`), `.gitattributes`(OBS-01): **여전히 미도입.**
- 자동 E2E: 프론트–백엔드 연동 이후로 보류 유지. 이번에 사람이 실행하는 온보딩 E2E 시나리오·체크리스트만 문서화됨.

**영역 간 정합성 문제 (추정 포함):**
- **[신규·추정] 프론트 `API_BASE_URL` 미설정 vs 백엔드 Railway 배포.** 백엔드는 `kongdakchaekdak-production.up.railway.app`에 배포됐으나(`be58577`), 프론트 `.env`엔 `API_BASE_URL`이 없어 fallback `http://10.0.2.2:8080`(로컬)을 씀. 에뮬레이터 + 로컬 백엔드 E2E엔 문제 없고 테스터도 그렇게 안내했으나, "배포된 백엔드로 붙는" 경로는 아직 미배선. 로컬 E2E 우선이라 의도적일 수 있음 — 실기기 테스트 시 PC LAN IP 또는 Railway URL 필요.
- **[진행 중] 소셜 로그인 실연동의 마지막 관문 = 사람 실행 E2E.** 백엔드 배포·프론트 SDK 배선·사전 점검(그린)까지 모두 준비됨. 남은 건 (a) 사람이 에뮬레이터에서 FE-SL-01~09 실행, (b) A→B 계약 검증용 유효 카카오 토큰 사용자 제공.
- **[이월] 구글 OAuth 동의 화면 "테스트 중"** — 게시하려면 개인정보처리방침 URL 필요. `PRIVACY.md`가 저장소에 추가됐으나(`2587445`) 호스팅된 공개 URL은 아직 없음.
- **[이월·추정] design → frontend 6장르색 전달 지연 ~2일.** design 확정(`b325db8`) 후 프론트 `colors.ts` 미반영 지속.
- 프론트가 참조하는 API 경로는 모두 백엔드에 실재(없는 API 참조 없음).

---

## PM 종합

### 영역별 한 줄 요약
- **백엔드:** 한 세션에 인프라 대거 정비 — gradle wrapper·백엔드 CI·`isNewUser` API 필드·Railway 재배포, 그리고 **BUG-03 3차 대응(조각1)이 드디어 테스터 확인 통과**(로컬 기동 정상). 잔여는 RENAME-02(DB 마이그레이션 안내)·`DashboardPeriod` enum·BUG-03 조각2.
- **프론트엔드:** 소셜 로그인 네이티브 SDK 연동을 한 세션에 완성(라이브러리 3종 + 키 설정 + LoginScreen + `isNewUser` 온보딩 분기), 동반 회귀 2건도 같은 세션에 수습. **프론트 CI(FINDING-16)까지 해소.** 남은 major는 Frame 08.2(책 등록 확인) 미구현 1건.
- **디자인:** 실질 변경 없음 — 파일명 `_v1` 제거 하우스키핑 커밋 1건뿐. 6장르색 프론트 반영·마케팅 스크린샷 대기.
- **테스터/QA:** SDK 연동 라운드를 화이트박스로 검증하고 프론트 회귀 2건을 잡아 closed, 자신의 오판(FINDING-15)은 철회. 온보딩 E2E 시나리오 + 사람 실행 체크리스트 문서화 — **이제 소셜 로그인 완결은 사람 손에 달림.**

### 가장 급한 이슈 (top 2)
1. **소셜 로그인 온보딩 실기기 E2E — 사람 실행 대기 (프로젝트 최상위 관문).** 백엔드 배포·프론트 SDK 배선·사전 점검(타입/번들/취소 처리 전부 그린)까지 준비 완료. 남은 건 (a) 사용자가 Android 에뮬레이터에서 FE-SL-01~09(신규 온보딩 / 재로그인 스킵 / 취소 3종) 실행, (b) 백엔드 `isNewUser` true→false 계약 검증용 **유효한 카카오 access token 1개** 제공. 이것만 통과하면 인증/온보딩 마일스톤이 닫힘. **사용자 액션 필요.**
2. **FINDING-20260828-08 (major, open) — Frame 08.2(책 등록 확인) 미구현.** 이제 가장 오래 방치된 열린 major. `BookSearchScreen`에서 책 선택 → `Alert` → `goBack`이라 장르 선택·시작일·"등록하기"가 없어 "책 검색 → 서재 등록" 핵심 동선이 끊겨 있음. FINDING-10(미구현 3화면)이 해결됐으니 다음 차례. **frontend-agent.**

부차: BUG-03 조각2(presigned-URL graceful 실패, backend), `DashboardPeriod` 소문자 수용(backend), 프론트 6장르 고정색 반영(frontend, ~2일 지연), RENAME-02 DB 마이그레이션 안내(backend), OBS-01 lint 정리 후 프론트 CI에 lint 스텝 추가.

### 영역 간 소통 / 정합성
- **피드백 루프가 이번에도 한 사이클 완주.** PM/테스터 리포트가 지목한 항목(프론트 CI 부재, 백엔드 CI, BUG-03 3차 대응, `isNewUser` 필드)이 같은 세션에 대거 커밋됐고, 테스터가 즉시 재실행으로 검증해 회귀 2건 closed. 특히 **BUG-03는 3번의 왕복 끝에 해소** — 조각1이 "생성자 주입 때문에 `@Bean @Lazy`만으론 부족"이라는 테스터의 정확한 진단을 그대로 반영한 결과.
- **"일부만 고쳐 커밋" 패턴은 이번 세션엔 완화.** 다만 SDK 연동 커밋(`8e2d865`)이 typecheck·jest 회귀 2건을 동반한 건 여전히 "커밋 전 로컬 검증 누락" — 이번에 붙인 프론트/백엔드 CI가 앞으로 이걸 push 시점에 잡아줄 것.
- **소셜 로그인이 프론트–백엔드 실연동의 첫 완결 지점이 됨.** 카카오 백엔드 해피패스는 검증됨, 구글/네이버는 이번 실기기 E2E에서 처음 실검증. 구글 게시는 개인정보처리방침 공개 URL(현재 `PRIVACY.md`만 저장소에 존재) + 실도메인 필요 — 앱 배포 후로 보류.
- **디자인 영역이 2일째 정지.** 6장르색 프론트 반영이 유일한 열린 연결 고리이고 이건 frontend-agent 몫. 디자인 다음 작업(마케팅 스크린샷)은 프론트 화면 진척 + 사용자 결정 대기라 당분간 유휴 예상.
- **프론트 `API_BASE_URL` 미설정(추정):** 로컬 E2E엔 무해하나, 배포된 Railway 백엔드로 붙는 설정이 없다는 점은 실기기 테스트 직전에 정리 필요.

---

## 커밋/푸시 결과
- 커밋: `7a8ab6f` `[scrum] 2026-08-30 데일리 스크럼 정리` (docs/scrum/2026-08-30.md 1개 파일만 stage)
- 푸시: ✅ 성공 — `origin/main` `90b9107..7a8ab6f`
- (이 줄 추가분은 커밋 이후 기록이라 다음 [scrum] 커밋에 포함됨)
