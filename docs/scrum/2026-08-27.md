# 데일리 스크럼 — 2026-08-27

- **관측 방식:** 모노레포 루트 `D:\projects\reading-record-app` 단일 git 저장소, 폴더별 `git log --since="24 hours ago"` + 실제 파일/README 상태 대조
- **폴더 매칭:** `backend/`, `frontend/`, `design/` 모두 존재 (+ `docs/`)
- **관측 시각:** 2026-08-27 밤 (KST)
- **커밋 분포:** 지난 24시간 커밋 14건 전부 오늘 발생 — 1건 13:32(design), 나머지 13건 21:06~21:36 한 세션에 집중

---

## 백엔드 (커밋 5건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 21:20 | `a72089e` | 미지원 신규 회원가입 코드 제거 |
| 21:23 | `0575325` | 공용 `Genre` enum 6종 도입 + `User.interests`(다중선택) 추가 + `GenreConverter` |
| 21:25 | `0bd751b` | 로깅 기능 신규 추가 |
| 21:26 | `fb914fc` | 로깅 기능 신규 추가2 |
| 21:27 | `1182497` | 로깅 인프라 도입(`GlobalExceptionHandler` 등) + 이메일/PW 로그인 완전 제거 |

**요약:** 인증 정책을 소셜 로그인(kakao/google/naver) 전용으로 확정하고 이메일/PW·자체 회원가입 코드를 제거. 관심분야/장르를 6종 고정 카테고리(소설·에세이·자기계발·인문·과학·경제·경영) `Genre` enum으로 통일하고 `User.interests`에 다중선택으로 적용. 로깅/전역 예외 처리 인프라 추가.

**미해결 작업:**
- `Genre` enum을 `Book.genre`에는 아직 미적용(자유 `String` 유지). `Genre.java` 주석에 "`domain/book/dto`를 도구의 폴더 깊이 제한으로 못 읽어 다음 라운드로 보류"라고 명시된 의도적 보류.
  - **(관측 시점 진행 중)** 메인 체크아웃 워킹트리에 `Book.java`, `Genre.java`, `book/dto/*`, `dashboard/DashboardService.java`, `user/*` 등 미커밋 수정이 있음 → `Book.genre` enum 전환 후속 작업이 커밋 직전 단계로 추정.
- 로깅 작업이 `추가` → `추가2` → `인프라 도입` 3커밋으로 분할됨 — 커밋 위생 참고사항.

---

## 프론트엔드 (커밋 6건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 21:30 | `286e2f6` | 컬러 팔레트 v2 "책거리×송편" 라이트+다크 동기화 |
| 21:31 | `d8948d6` | Pretendard 폰트 5종 번들링 |
| 21:32 | `abc9885` | 프로필/독서 대시보드(Frame 05, 05.1) mock 구현 |
| 21:35 | `3142b60` | 공통 컴포넌트(빈 상태 / 로딩 스켈레톤 / 네트워크 에러) 신규 |
| 21:36 | `a49f59c` | 회원가입 폼(닉네임/성별/관심분야 6칩/독서모임 검색) 전체 구현 |
| 21:36 | `11ce7d8` | 책 검색/등록(Frame 03) 화면 구현 |

**요약:** 디자인 팔레트 v2·폰트를 반영하고 회원가입 폼·책 검색/등록·프로필/대시보드 화면과 목록 공통 상태 컴포넌트를 구현. 전부 mock 데이터 기반.

**미해결 작업 (소스 내 TODO 9곳):**
- 전 화면이 mock 상태, 실제 API 연동 0건. `GET /api/dashboard`, `GET/POST /api/books`, `PATCH /api/books/{id}/complete`, `GET /api/books/{id}/notes|photos`, `GET /api/groups` 연동 대기.
- `AuthContext.tsx` — `TODO(Step 3)`: 카카오/구글/네이버 OAuth2 실연동 시 `login()`을 실제 토큰 발급/검증으로 교체 예정.

---

## 디자인 (커밋 3건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 13:32 | `b07d15a` | 디자인 문서 업로드 (초기본, 이후 커밋들로 대체됨) |
| 21:06 | `88d39c7` | 디자인 문서 업로드 — 앱 이름 "콩닥책닥" 확정, 아이콘 텍스트 워드마크 전환, 스플래시·스토어 에셋 |
| 21:15 | `ffeb50b` | 구 "쑥송편" 아이콘 파일들을 `design/deprecated/`로 이동 정리 |

**요약:** 앱 이름을 **"콩닥책닥"**으로 확정. 앱 아이콘을 송편 일러스트에서 **"콩닥/책닥" 2줄 텍스트 워드마크(v2)**로 전면 교체하고 iOS 1024·Android adaptive bg/fg PNG까지 갱신. 스플래시 화면을 2개 시안 비교 후 풀블리드 워드마크형으로 신규 확정. hi-fi 목업·브랜딩·컬러칩·스토어 에셋 문서 대량 정리, 구 아이콘 파일은 `design/deprecated/`로 보관 이동.

**산출물 상태 (`design/README.md` 표 기준, 전부 오늘 커밋):**

| 산출물 | 상태 |
|---|---|
| `app_icon.html` v2 (텍스트 워드마크) | **확정** |
| `splash_screen.html` v1 (풀블리드 워드마크형) | **확정** |
| Google Play 피처 그래픽(1024×500)·고해상도 아이콘(512×512) | v1 **제작 완료** (공식 사양 확인 후 제작) |
| `hifi_mockup_v1.html` | v1.6 (앱 이름 반영 완료) |

**미해결 작업:**
- App Store/Play **마케팅 스크린샷**(6.5"/6.9" 등 실제 앱 화면 기반)만 미착수 — `store_assets.html`에 "별도 라운드 진행 예정"으로 명시. 어떤 화면·캡션을 넣을지 먼저 정해야 하는 의존성 때문(디자인 단독 이슈 아님).

> ⚠️ 이전 보고에서 아이콘/스플래시/스토어 그래픽을 "임시 상태"로 기재했으나 **오류**. 커밋 메시지의 초기 문구(`b07d15a` "신청해야할듯")에만 앵커링하고 이후 커밋 + `design/README.md`(확정 표기)를 대조하지 않은 것이 원인. 위 표가 정정된 상태.

---

## PM 종합

### 영역별 한 줄 요약
- **백엔드:** 인증을 소셜 전용으로 확정하고 관심분야 6종 enum·로깅 인프라까지 마무리. 엔드포인트는 거의 다 준비됨. `Book.genre` enum 전환이 커밋 직전.
- **프론트엔드:** 주요 화면 6개를 mock으로 구현 완료. 화면은 있으나 데이터가 전부 가짜 — 다음은 API 연동.
- **디자인:** 앱 이름 "콩닥책닥" 확정 + 아이콘 v2(텍스트 워드마크)·스플래시 신규 확정. 릴리스 그래픽 에셋은 마케팅 스크린샷만 남음.

### 가장 급한 이슈 (top 1)
1. **프론트–백엔드 실연동 0건.** 백엔드 컨트롤러(`/api/books`, `/api/dashboard`, `/api/groups`, `/api/books/{id}/complete` 등)는 준비됐는데 FE는 전 화면 mock. 다음 스프린트 핵심 병목 — 연동 착수 우선순위 지정 필요.

### 액션 아이템 (급하지 않음)
- 디자인: 마케팅 스크린샷용 화면·카피 선정 (FE mock 화면이 어느 정도 나온 지금 착수 가능).
- 백엔드: `Book.genre` enum 전환 워킹트리 변경분 커밋 마무리 + 관련 DTO 회귀 확인.

### 영역 간 정합성 (현재 양호 — 특이 리스크 없음)
- 관심분야 칩(FE 6종)과 백엔드 `Genre` enum 6종이 표기까지 완전 일치("경제·경영"). 백엔드 코드 주석에 "디자이너 확인 답변(`claude/…관심분야장르확인_v1.md`)을 그대로 반영"이라고 명시 → 3영역이 문서 Q&A로 실제 협의 중인 정황 확인.
- FE가 참조하는 API 경로가 모두 백엔드 컨트롤러에 실제 존재 — 없는 API를 참조하는 커밋 없음.
- 로그인: 백엔드 소셜 전용 ↔ FE `AuthContext`(카카오/구글/네이버) 방향 일치, FE에 이메일/PW 로그인 잔재 없음.
