# 데일리 스크럼 — 2026-08-28

- **관측 방식:** 모노레포 루트 `D:\projects\reading-record-app` 단일 git 저장소, 폴더별 `git log --since="24 hours ago"` + `--all` + 실제 파일/README·리포트 상태 대조
- **폴더 매칭:** `backend/`, `frontend/`, `design/`, `docs/test/` 모두 존재
- **관측 시각:** 2026-08-28 08:13 (KST)
- **브랜치:** `main` (기본), 원격 `origin/main`. `test/book-service-unit-tests` 브랜치는 존재하나 `origin/main` 대비 신규 커밋 없음(빈 diff) — 실질적으로 비활성. `claude/*` 워크트리 브랜치 2개.
- **커밋 분포:** 지난 24시간 커밋 27건(`--all` 기준). 직전 스크럼(`55628f4`, 08-27 22:12) 이후 신규는 13건 — backend 3, frontend 2, design 2, test 5, (scrum 1은 직전 것).
- **워킹트리 미커밋 변경:** `design/README.md`, `design/hifi_mockup_v1.html` (아래 디자인 섹션 참조). backend·frontend·docs 워킹트리는 clean.

---

## 백엔드 (신규 커밋 3건, 직전 스크럼 이후)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-27 22:21 | `8ed908b` | `Book.genre` 자유 String → `Genre` enum 전환. Book DTO 3종 타입 변경, 스테일 Javadoc 정리 (직전 스크럼에서 "커밋 직전"으로 관측했던 작업이 커밋됨) |
| 08-27 22:39 | `ef13bbd` | Genre 마이그레이션 누락 테스트 3파일 수정 — `:compileTestJava`가 깨진 상태였음(BUG-20260827-01) |
| 08-27 23:00 | `9da7880` | `BookControllerTest`/`UserControllerTest`에 Genre/interests 계약 테스트 +7 추가, 그 과정에서 버그 1건 발견·수정(커밋 메시지 명시, 테스터 별도 확인은 안 함 — 스위트 그린) |

**요약:** 직전 스크럼에서 "커밋 직전"이던 `Book.genre` enum 전환을 완료. 전환 과정에서 테스트 3파일이 누락돼 컴파일이 깨졌고(테스터가 BUG-01로 리포트), 같은 세션에 `ef13bbd`로 복구. 이후 Genre/interests 계약 테스트를 추가(66 → 73 tests). 백엔드 테스트 스위트는 현재 그린(테스터 검증: `gradle test` → 73 tests / 0 failures).

**미해결 작업 (테스터 리포트 기반, 담당 개발자 미반영):**
- **BUG-20260827-03 (major, open):** `local` 프로필 서버가 AWS 자격증명 없이 기동 실패. `S3Config.s3Presigner()`가 기동 시점에 빈 accessKey로 폭발(`NullPointerException: Access key ID cannot be blank`). 문서(`개발현황.md §12-3`)의 "자격증명 없어도 기동 정상" 주장이 사실이 아님. 08-27 23:15 리포트(`0154f92`) 이후 backend 커밋 없음 → **미수정.**
- **FINDING-20260827-04 (major, open):** `POST /api/share-records`의 `shareType`/`scope`/`platform`/`targets[].targetType`이 JSON에서 enum 이름(UPPERCASE)만 수용. 소문자는 400. 반면 DB 저장은 소문자, `Book.status`는 대소문자 무관, `Genre`는 한글 라벨 — enum 직렬화 방식이 코드베이스 안에서 4가지로 제각각. **미수정.**
- **OBS-06:** 잘못된 enum 값 요청이 `400 MALFORMED_REQUEST` + 제네릭 메시지 + 빈 `fieldErrors`로 떨어져 클라이언트가 문제 필드를 알 수 없음.
- 소스 내 TODO 4곳(대부분 "Step 3/5 이후" 의도적 보류 — 소셜 로그인 실연동 시 User NOT NULL 제약, 카드 이미지 서버 렌더링 등). 신규 위험 아님.

---

## 프론트엔드 (신규 커밋 2건, 직전 스크럼 이후)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-27 22:22 | `b7ab73c` | 프로필 편집(Frame 05.2) 구현 |
| 08-27 23:03 | `ed91a36` | 프로필 편집 기능 테스트 코드 반영 + `ProfileScreen.tsx`에 testID 추가 (jest 4 → 20 tests) |

**요약:** 프로필 편집 화면을 구현하고 관련 jest 테스트(`ProfileContext`/`ProfileEditScreen`/`ProfileScreen`/`profileOptions`)를 추가. 테스터의 "테스트 코드 작성 요청 v1"에 대한 첫 반영 — jest 스위트 4 → 20 tests로 확대되고 전부 통과.

**미해결 작업 (테스터 리포트 기반):**
- **BUG-20260827-02 (major, open):** `ed91a36` 신규 테스트 파일 때문에 `npm run typecheck` 깨짐 — `ProfileScreen.test.tsx:17`, `ProfileEditScreen.test.tsx:17` TS2698 (`jest.requireActual` 반환 `unknown` 스프레드). **jest 런타임은 통과하지만 `tsc --noEmit` 실패** → CI에 typecheck 게이트가 있으면 빌드 차단. `ed91a36` 직전까지 그린이었던 회귀. 이후 frontend 커밋 없음 → **미수정.**
- **FINDING-20260828-05 (minor, open):** `theme/colors.ts` `darkColors` 뉴트럴 램프 비단조 — `n50(#1e211b)`이 `n100(#1a1d17)`보다 밝음(다크는 n50이 가장 어두워야 함).
- **FINDING-20260828-06 (minor, open):** 다크 앱 배경 전용 토큰 부재. design은 `--d-surface-app(#181a14)` ≠ `--d-surface-card(#242721)` 2단계인데, 프론트는 카드용 `surface`만 있고 `RootNavigator`가 `colors.n50`을 배경으로 대체 사용 → design 의도보다 밝음.
- **전 화면 mock 상태 유지 — 실제 백엔드 API 연동 0건.** 소스 내 TODO 11곳(주로 API 연동 대기 지점 + `AuthContext` OAuth 실연동 `TODO(Step 3)`). 직전 스크럼의 최우선 병목 그대로.
- **OBS-01 (이월):** lint CRLF/prettier 노이즈 440건. `.gitattributes` 부재 확인(루트·`frontend/` 둘 다 없음).

---

## 디자인 (신규 커밋 2건 + 워킹트리 진행 중)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-27 22:28 | `dfd03ee` | iOS 다크/틴트드 아이콘 반영 완료 (`app_icon.html` v2.1, `ios_icon_dark_1024.png` / `ios_icon_tinted_1024.png` 신규) |
| 08-27 22:40 | `9245337` | Frame 01 로그인/온보딩 화면(라이트·다크)의 로고 자리를 범용 book-open 글리프 → 실제 확정 앱 아이콘 배지로 교체 (`hifi_mockup_v1.html` 코드 변경) |

**요약:** iOS 18 홈 화면 커스터마이징(라이트/다크/틴트) 대응 아이콘 변형을 추가하고, Frame 01 목업의 로고를 실제 확정 아이콘으로 맞췄다. `app_icon.html`은 v2.1로 확정(README 표 기준).

**산출물 상태 (`design/README.md` 표 기준):**

| 산출물 | 상태 |
|---|---|
| `app_icon.html` v2.1 (텍스트 워드마크 + iOS 다크/틴트드) | **확정** |
| `splash_screen.html` v1 | **확정** |
| `color_chips.html` v2 / `typography_and_icons.html` v1.2 | 확정 (값 변경 없음) |
| Google Play 피처 그래픽·고해상도 아이콘 | v1 제작 완료 |
| `hifi_mockup_v1.html` | 커밋된 상태 v1.6 표기 · **워킹트리 v1.8 진행 중** (아래) |

**미해결 / 진행 중:**
- **(관측 시점 진행 중)** `design/hifi_mockup_v1.html`에 미커밋 변경 411줄(+411/−3), `design/README.md`도 미커밋. 내용: **v1.7**(Frame 01 로고 아이콘 배지 — 코드는 `9245337`에 이미 반영됐으나 README 버전 표/changelog 동기화가 이 미커밋분에 있음) + **v1.8 태블릿·폴더블 대응**(≥600px Expanded 레이아웃 3종: 서재 목록 2열 그리드 / 대시보드 좌우 2단 / 일정 탭 좌우 2단, 각 라이트·다크, 신규 `.tablet-frame` 클래스). 커밋 분리 규칙상 v1.7·v1.8을 별도 커밋으로 마무리 예정으로 추정.
- App Store/Play **마케팅 스크린샷**(실제 앱 화면 기반) 미착수 — `store_assets.html`에 "별도 라운드" 명시. 어떤 화면·캡션을 넣을지 선행 결정 필요(디자인 단독 이슈 아님).

---

## 테스터 / QA (신규 커밋 5건, 직전 스크럼 이후)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-27 22:46 | `22f1f0a` | 테스터 에이전트 역할 도입 + 시나리오/리포트 체계(`docs/test/` 구조, `report-template.md`, 시나리오 3종) |
| 08-27 22:48 | `e96729e` | 리포트 갱신 — BUG-20260827-01 closed 확인 |
| 08-27 22:55 | `d0bab42` | 자동 E2E 도입 결정 보류 — 프론트·백엔드 연동 후로 |
| 08-27 23:15 | `0154f92` | **2026-08-27 2차 리포트** — 전체 화이트박스 + 백엔드 블랙박스 스모크 첫 실행. `mint-jwt.mjs` 헬퍼 추가 |
| 08-28 00:05 | `ea7e254` | **2026-08-28 리포트** — 디자인 정합성 검증 범위 추가(`design-conformance.md`) + 토큰 대조 1차 |

**최신 리포트:** `docs/test/reports/2026-08-28.md` (2026-08-28, 디자인 토큰 대조). 그 전날 `2026-08-27.md`(1차, 화이트박스) / `2026-08-27b.md`(2차, 화이트박스 전체 + 백엔드 블랙박스 스모크).

**진행 요약:**
- 백엔드 `gradle test` 73 / 0 failures, 프론트 `npm test` 20 통과 확인.
- 백엔드 블랙박스 스모크 30여 케이스 1회 완료 — 소유권/인가(403)·인증(401)·검증(400)·비로그인 공개뷰·대시보드 집계 등 핵심 계약 정상 동작 확인.
- 디자인 토큰 대조: 프론트 `theme/` 토큰이 디자인 시스템 v2와 거의 완전 일치(primary/neutral 램프, 상태색, 포인트 4색, 시맨틱 토큰, 타입 스케일 10종 전부 일치). 불일치는 minor 2건.

**발견된 이슈 (누적, 심각도별):**

| ID | 심각도 | 영역 | 상태 | 담당자 반영 |
|---|---|---|---|---|
| BUG-20260827-01 (BE 테스트 컴파일) | blocker | backend | **closed** (`ef13bbd`) | ✅ 반영됨 |
| BUG-20260827-02 (FE typecheck TS2698) | major | frontend | open | ❌ 미반영 |
| BUG-20260827-03 (`local` 서버 기동 실패) | major | backend | open | ❌ 미반영 |
| FINDING-20260827-04 (share enum 대소문자 계약) | major | backend(+docs) | open | ❌ 미반영 |
| FINDING-20260828-05 (다크 뉴트럴 램프 비단조) | minor | frontend | open | ❌ 미반영 |
| FINDING-20260828-06 (다크 앱 배경 토큰 부재) | minor | frontend | open | ❌ 미반영 |
| OBS-01 / OBS-02 / OBS-06 (관찰) | — | FE / FE / BE | open | — |

**미수정 항목 (추정):** 08-27 23:15·00:05 리포트 이후 backend·frontend 개발 커밋이 없어, closed된 BUG-01을 제외한 **open 이슈 5건 전부 담당 개발 에이전트 미반영 상태**. 24시간이 채 지나지 않았으므로 지연이라 단정하긴 이르며, 다음 개발 세션(새벽 자동 세션 포함) 반영 여부를 관찰 필요.

**제안된 자동화 테스트 실제 추가 여부:**
- 테스터의 `requests/테스트코드작성요청_v1.md`(08-27 초안)에 대해 **backend `9da7880`(+7 계약 테스트), frontend `ed91a36`(+16 테스트)로 1차 반영됨** — 단 frontend 반영분이 BUG-02(typecheck 회귀)를 동반.
- 커버리지 측정(JaCoCo / `jest --coverage`) 도입: 리포트에 제안만, **미도입.**
- `.gitattributes`(CRLF 정리, OBS-01): **미추가.**
- `local`/`dev` 프로필 테스트 계정 시더(GAP 항목): **미추가** — 소셜 로그인 외 사용자 생성 수단이 없어 블랙박스·로컬 확인이 h2-console 직접 seed 없이는 불가.

**영역 간 정합성 문제 (추정 포함):**
- **[확인됨] FINDING-04 — 프론트↔백엔드 share enum 계약 불일치.** `frontend/src/types/share.ts`가 `ShareScope='all'|'group'|'custom'`, `SharePlatform='app'|'instagram'|...` 소문자를 쓰는데 백엔드는 UPPERCASE만 수용 → 프론트가 이 타입 그대로 `POST /api/share-records` 호출 시 100% 400. **연동 착수 전 반드시 정리 필요.** 개발현황.md §13도 소문자로 서술돼 있어 문서까지 3중 불일치.
- **[추정] `개발현황.md §12-3` 문서 부정확** — "자격증명 없어도 기동 정상"이 실제 Gradle 실행으로 검증된 적 없이 작성됨(BUG-03). 문서 신뢰도 이슈.
- **[양호]** 관심분야/장르 6종은 프론트 칩 ↔ 백엔드 `Genre` enum 표기까지 완전 일치. 로그인 방향(소셜 전용)도 FE `AuthContext` ↔ BE 일치. 디자인 토큰도 FE 구현과 거의 완전 일치.

---

## PM 종합

### 영역별 한 줄 요약
- **백엔드:** `Book.genre` enum 전환 완료 + 계약 테스트 확대(73 tests 그린). 단 테스터가 올린 major 3건(로컬 기동 실패·share enum 계약·문서 부정확) 미반영.
- **프론트엔드:** 프로필 편집 화면·테스트 추가로 jest 4→20. 그러나 신규 테스트가 `typecheck`를 깨뜨린 회귀(BUG-02) 미수정, 전 화면 여전히 mock — API 연동 0건.
- **디자인:** iOS 다크/틴트드 아이콘 확정, Frame 01 로고 실제 아이콘화. 태블릿·폴더블(Expanded) 레이아웃 v1.8 워킹트리에서 작업 중(미커밋).
- **테스터/QA:** 역할·리포트 체계 정착, 백엔드 블랙박스 스모크 1회 + 디자인 토큰 대조 1차 완료. open 이슈 5건 추적 중, 전부 개발자 미반영.

### 가장 급한 이슈 (top 2)
1. **FINDING-20260827-04 (major) — share enum 대소문자 계약 불일치.** 직전 스크럼의 최우선 병목("프론트–백엔드 실연동 0건")을 해소하려면 연동을 시작해야 하는데, share API는 프론트 타입 그대로 호출 시 전부 400. 백엔드 enum에 `@JsonValue`/`@JsonCreator`(소문자) 통일 + `개발현황.md`/Swagger 정합화가 연동 착수의 선결 조건. **backend-agent 우선 처리 권장.**
2. **BUG-20260827-03 (major) — `local` 서버가 AWS 자격증명 없이 기동 실패.** 클린 체크아웃에서 문서화된 실행 명령이 깨지고, 블랙박스 테스트·신규 참여자 로컬 확인이 선행 차단됨. `S3Presigner` 빈 `@Lazy`화 또는 자격증명 없을 때 지연 생성으로 해결. **backend-agent.**

부차: BUG-02(FE typecheck 회귀 — `jest.requireActual` 제네릭 지정으로 간단 수정 가능, frontend-agent), 디자인 v1.8 미커밋분 커밋 마무리.

### 영역 간 소통 / 정합성
- 관심분야 6종·로그인 방향·디자인 토큰은 3영역 정합성 양호 — 문서 Q&A 기반 협의 정황 지속 확인.
- **테스터 → 개발 피드백 루프가 아직 1사이클도 안 돎:** open 이슈 5건이 리포트된 지 9~32시간 경과했으나 개발 반영 커밋 0건. 다음 개발 세션에서 major 3건 우선 반영 여부가 협업 체계의 첫 시금석. (추정 — 24h 미경과라 "지연"으로 단정하진 않음)
- 프론트가 참조하는 API 경로는 여전히 모두 백엔드에 실재(없는 API 참조 없음). 단 enum 계약(FINDING-04)은 "경로는 맞고 페이로드가 틀린" 유형.
