# 데일리 스크럼 — 2026-09-10 (KST)

- 작성: PM 에이전트 (관측 기반)
- 관측 시점: 2026-09-10 08:13 KST
- 관측 HEAD: `d72889d` (main, origin/main과 동기)
- 대상 기간: 직전 스크럼(2026-09-09 22:28 관측, `abfa23c` 커밋) 이후 ~ 관측 시점 (약 10시간)
- 관측 방법: 루트에서 영역별 `git log` + 실제 파일/README/리포트/연동매트릭스 대조

## 이번 기간 커밋 개요

직전 스크럼이 밤(22:33)에 돌았고 이번은 아침 정규 시각이라 대상 기간이 짧다. 활동은
**직전 스크럼 관측 직후(22:44~) ~ 09-10 새벽 00:32** 자동 개발 세션 1회 + 09-10 01:16
테스터 세션 1회 + 09-10 07:28·08:12 사용자 직접 커밋(연동매트릭스)로 구성.

| 영역 | 커밋 수 | 미커밋 | 비고 |
|---|---|---|---|
| backend | 4 | 없음 | Apple 로그인 / ErrorCode 체계 1·2단계 / **G20 조회 권한 검증(P0 보안)** |
| frontend | 7 | 없음 | 한글 IME 수정 / API 타입 리팩터 / Apple 로그인 / 비회원 둘러보기 폐기 / **UX 문구 전면 리소스화(strings/)** |
| design | 2 | 없음 | 목업 v1.17·v1.18 커밋 + v1.19(로그인 버튼 4개 통일) |
| test (docs/) | 1 | 없음 | **BUG-23 재검증 → 재현 실패** + apk-2026-09-10 스모크·배포 |
| docs (사용자 직접) | 3 | 없음 | 아키텍처 문서 최신화 / 연동매트릭스 2회 갱신(분모 42→41, 연동률 20%) |

> 직전 스크럼이 "미커밋 진행 중"으로 잡았던 backend Apple 로그인 · frontend 27파일 ·
> design v1.17/v1.18 은 이번 기간에 **전부 커밋됨**. 현재 워킹트리 clean — 진행 중 변경 없음.

---

## 백엔드

**신규 커밋 4건. 미커밋 없음.**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-09 22:44 | `5333aa3` | **Apple 로그인 구현.** 카카오/구글/네이버와 동일 흐름, Nimbus 라이브러리. 신규 `AppleOAuthClient`/`AppleOAuthProperties`/`dto/AppleIdTokenRequest`, `OAuth2Config`·`SocialAuthService`·`AuthController`·`SecurityConfig` 수정. 테스트 `AppleOAuthClientTest`(+132)·`SocialAuthControllerTest`(+38). → 연동매트릭스 `#42`. |
| 09-10 00:22 | `a32d5e3` | **ErrorCode 1단계** — `CodedException` 도입, `ErrorCode` enum(+96), `ErrorResponse`·`GlobalExceptionHandler` 개편, 각 예외 클래스에 코드 부여. 핸들러/필터/엔트리포인트만 먼저. |
| 09-10 00:25 | `bc66694` | **ErrorCode 2단계** — 도메인 서비스(book/booknote/bookphoto/group/share/user/auth·oauth2 4종)에서 기존 예외 클래스 제거하고 새 체계로 교체. 예외 클래스 5종 삭제. 테스트 다수 조정. |
| 09-10 08:12 | `38008fa` | **G20 — 조회 계열 소유자 검증(P0 보안).** `GET /api/books`·`/api/users/{id}`·`/api/groups`·`/api/groups/{id}`·`/api/groups/{id}/members`가 클라이언트가 보낸 값을 그대로 신뢰하던 문제. `@AuthenticationPrincipal`로 교체, `findAll()` 분기 제거, 비멤버 403. **`GET /api/users`(#8, 전 회원 명부) 엔드포인트 삭제.** 테스트 3개 컨트롤러 보강. |

- **TODO/FIXME/WIP:** committed 소스에서 특이 키워드 없음.
- **최근 진행상황 요약:** 직전 스크럼의 blocker(BUG-23)는 **테스터가 재현 실패로 판정**(아래 테스터 섹션) → 백엔드가 별도 수정 커밋을 낼 필요는 없어졌고, 대신 BUG-23 조사 중 발견한 **G20 보안 결함을 이번에 처리**했다. Apple 로그인·ErrorCode 체계도 함께 진척.
- **미해결 (개발자 몫):**
  - **[정보 · G17] enum 직렬화 규칙 통일** — `Gender`·`BookStatus`에 `@JsonValue toLowerCase()` + `BookController.search`의 `toUpperCase()` 임시코드 제거. 연동매트릭스가 "선행: G13"으로 묶어놨는데 **G13이 이번에 해제**됐으므로 이제 착수 가능. 이번 커밋에는 없음(ErrorCode 작업이 우선이었던 것으로 보임).
  - **[정보 · G21] ErrorCode FE 대응** — 연동매트릭스 `58fe310`에서 신설. 백엔드가 에러 응답 스키마(`ErrorResponse` + 코드)를 바꿨는데 FE가 이 코드 체계에 맞춰 에러 처리를 하는지 미확인. 아래 정합성 참고.
  - **[구조 · G20 후속] `GET /api/users/{id}`(#7) 본인 전용 잠금의 S6 부작용** — 그룹 멤버 프로필을 화면에 띄우려면 타인 조회가 필요. 연동매트릭스는 #7을 다시 여는 대신 `GET /api/groups/{id}/members`(#31) 응답에 닉네임·프로필이미지를 싣는 쪽을 권장. S6 진입 전 확정 필요.
- **이월(사용자 액션 · 시한):** **G9 — Railway 크레딧 결제.** 연동매트릭스가 만료일 **2026-09-28** 명시. BUG-23의 유력 원인 후보가 "그 시점 Railway MySQL 데이터 불안정"이라 미결제 시 재발 소지 — 여전히 시한 이슈.

---

## 프론트엔드

**신규 커밋 7건. 미커밋 없음.** 직전 스크럼이 "미커밋 27파일"로 잡았던 작업이 커밋으로 확정됨.

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-09 22:49 | `1db1513`·`e144b97` | **닉네임 칸 한글 입력 안 되는 버그 수정** ×2 — IME 조합 입력 모드 오류. |
| 09-09 22:51 | `86ccc4f` | **백엔드 API 통신 규약 확인용 리팩터링** — `src/types/api/` 폴더 신설(auth/dashboard/library/profileStats/user), `libraryApi.ts` 약 78줄 축소, `userApi.ts` 77줄 조정(직전 스크럼의 gender 양방향 매핑 포함으로 추정), `authApi.ts` 정리. |
| 09-09 23:24 | `effc579` | **비회원 둘러보기 폐기 + Apple 로그인 1** — `socialAuth/appleAuth.ts` 신규, `LoginScreen`(+98) 개편, `AuthContext`·`ProfileContext`·`LibraryContext`에서 게스트 분기 제거. |
| 09-10 00:04 | `95040b1` | Apple 로그인 2 — `package-lock.json`(`react-native-apple-authentication` 계열). |
| 09-10 00:25 | `bcbc589` | Apple 로그인 3 — jest용 `__mocks__/react-native-apple-authentication.js`, `jest.config.js`. |
| 09-10 00:32 | `25a5613` | **UX 문구 전면 리소스화 + 리소스 분리(42파일, +1534/−349).** `src/strings/ko.ts`(491줄)·`src/strings/index.ts`(84줄) 신규, `src/utils/josa.ts`(조사 처리, +150)·`__tests__/josa.test.ts`·`__tests__/strings.test.ts` 신규. 화면 전반의 하드코딩 문자열을 `strings/`로 이동. `genreColors.ts`·`profileOptions.ts`·`SearchBar.tsx`·대시보드 컴포넌트 다수 동반 수정. 디자인 목업 "밥상 보이스"(v1.17) 반영으로 보임. |

- **TODO/FIXME/WIP:** committed 소스에서 특이 키워드 확인 안 됨.
- **최근 진행상황 요약:** Apple 로그인 FE(백엔드 `5333aa3`와 짝), 비회원 둘러보기 완전 폐기(디자인 v1.18·백엔드와 정합), 한글 IME 버그 2건, API 타입 정리, UX 문구 리소스화까지 폭넓게 반영.
- **미해결 (개발자 몫):**
  - **[정보 · G20 후속(G20-FE)] `services/libraryApi.ts`의 `fetchBooks`에서 `?userId=${userId}` 제거.** 백엔드 G20(`38008fa`)이 오늘 08:12에 들어왔고 FE 마지막 커밋은 00:32이므로 **아직 반영 안 됨.** 백엔드가 이제 파라미터를 무시하고 토큰 기준으로 조회하므로 당장 동작에는 영향 없으나(테스터 스모크 PASS), 죽은 파라미터가 남아 있음. 그룹 API는 전부 mock이라 FE 영향 없음.
  - **[정보 · G21] ErrorCode 체계 FE 대응** — 백엔드 에러 응답 포맷 변경(`a32d5e3`·`bc66694`)에 맞춘 FE 에러 파싱/표시 갱신 필요. 미착수로 추정.
  - **[minor · 이월] BUG-20260901-17 — 서재 검색창 placeholder 한글 자모 분해.** 테스터 2026-09-10 리포트에서 **여전히 재현**. `SearchBar.tsx`는 `25a5613`에서 손댔으나(다른 목적) 이 증상은 안 잡힘. 09-01 최초 리포트 이후 **9일째 미해결** — 이 스크럼 기준 가장 오래된 열린 결함.
  - **[정보 · 이월] FINDING-20260908-18** — S0 3화면(GroupManagement/ScheduleDetail/Notifications) 진입 크래시 회귀 테스트 미추가. `25a5613`에 추가된 테스트는 josa·strings 유닛 테스트로, S0 렌더 테스트는 아님. open 유지.
  - **[정보 · 이월] OBS-20260909-22 / G15** — 닉네임 규칙 FE(2~10자)↔BE(`@Size(max=30)`) 불일치. 기획 결정(D4) 대기.
- **정합성(추정):** 위 G20-FE·G21 두 건 모두 **백엔드가 규약을 바꾼 뒤 FE 반영이 한 사이클 밀린** 형태. 다음 개발 세션에서 함께 처리 필요.

---

## 디자인

**신규 커밋 2건. 미커밋 없음.**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-09 22:40 | `1d5d3c1` | **목업 v1.17·v1.18 커밋.** v1.17 = "밥상" 보이스 레이어(완독 버튼·리캡 하이라이트·최근 활동·서재 빈 상태·알림 목록 문구 교체) + v1.1~v1.16 이력을 `hifi_mockup_changelog.md`로 분리. v1.18 = Frame 01 "비회원으로 둘러보기" 제거 + iOS 전용 `Apple로 로그인` 버튼 추가. |
| 09-09 22:46 | `a8c4f82` | **목업 v1.19 — 로그인 버튼 4개 스타일 통일.** Apple 버튼만 색이 달라 어색하다는 피드백. 브랜드 원형 아이콘 로우도 검토했으나 제공사별 공식 에셋 신뢰도 편차(구글·카카오 O / 네이버 콘솔 필요 / Apple 로고 단독 사용 불가)로 **텍스트 버튼 통일** 채택. Apple/네이버/카카오/Google 4개 모두 `.btn-fill`(연회색 채움), v1.18의 전용 `.btn-apple` 클래스 제거. 버튼 순서·여백은 v1.18 유지. App Store 4.8(Apple 로그인 최소 동일 노출) 자연 충족. |

- **최근 진행상황 요약:** 로그인 화면 시안(비회원 폐기 + Apple 추가 + 4버튼 통일)을 FE·백엔드와 같은 타이밍에 확정. `hifi_mockup_v1.html` 산출물 상태표는 v1.19로 갱신됨.
- **TODO/FIXME/WIP:** design/README.md·changelog에서 특이 키워드 없음.
- **미해결 (디자인 몫):**
  - **[P0 · G16] 회원 탈퇴 진입점·확인 다이얼로그** — Apple 5.1.1(v) 필수. `color_chips.html` v2.3 다이얼로그 재사용이라 경작업. **이번에도 목업에 탈퇴 화면 추가 흔적 없음** — 직전 스크럼에서 이미 지적했고 미해결 이월.
  - **[정보 · 이월] OBS-20260909-20** — 로그아웃 확인 다이얼로그: 목업 Frame 05.4로 있으나 앱은 1탭 즉시 실행. 디자인 완료, FE 반영 대기.
  - **[구조 · 이월] README ↔ 실제 파일명 불일치** — README/changelog는 `hifi_mockup.html`로 서술, 디스크는 `hifi_mockup_v1.html`. 08-30부터 이월. PM 의견은 개명 대신 README 서술을 실제 파일명에 맞추는 쪽.

---

## 테스터 (QA)

**신규 리포트 1건 (`reports/2026-09-10.md`, 커밋 `58fe310` 09-10 01:16).**

### 최신 리포트 요약 — BUG-23 재검증 + apk-2026-09-10 배포

- **BUG-20260909-23 (blocker, G13) → 재현 실패(cannot reproduce).**
  - S3 세션의 에뮬레이터가 실제 Railway 서버와 통신했음은 APK 내 URL·데이터로 재확인(로컬 서버 오인 배제).
  - 재검증(에뮬레이터 타임존 GMT→Asia/Seoul로 교정, 현재 HEAD APK): "1984"·"사피엔스" 등록 → `am force-stop` → 재실행 → **둘 다 유지.** logcat 에러/403/크래시 없음. 사용자 실기기도 항상 정상 영속.
  - S3 세션에 있던 책 3권(아몬드/채식주의자/데미안)이 그 사이 전부 사라진 것으로 볼 때 **유력 원인은 그 시점 Railway MySQL 데이터 불안정(G9와 연결)**. 차선 후보는 에뮬레이터 UTC 타임존.
  - **판정 제안:** BUG-23 → "재현 안 됨"으로 상태 변경/닫기. 연동매트릭스 `#11 POST /api/books` **FAIL→✅ 승격**(연동률 7→8). G13에 묶여 있던 G17·S4 선행조건 해제.
- **스모크 테스트 PASS** — 사용자 로컬 빌드 `app-release.apk`(`25a5613`) 설치·실행·안정성 OK, "비회원으로 둘러보기" 사라짐 확인. **GitHub Release `apk-2026-09-10` 배포**(prerelease, 태그 존재 확인).

### 발견된 버그/이슈 현황

| ID | 심각도 | 내용 | 담당 | 상태 |
|---|---|---|---|---|
| BUG-20260909-23 | blocker | 등록한 책 미영속 | BE/FE | **재현 실패 → 닫기 제안. #11 ✅ 승격 반영됨(매트릭스 8/41)** |
| BUG-20260909-21 | major | gender enum 불일치(성별 유실) | FE (D2) | **open** — 이번 세션 미확인. FE 대응(`86ccc4f` userApi)은 커밋됐으나 테스터 재검증 안 됨 |
| BUG-20260901-17 | minor | 서재 검색 placeholder 한글 자모 분해 | FE | **open, 여전히 재현** (9일째) |
| OBS-20260909-24 | 정보 | 에뮬레이터 UTC 타임존 → `startDate` 하루 이름 | 테스트 환경 | 이번에 KST로 교정 |
| (기록) | 정보 | 나루토 카드 날짜 `2026.09.09 ~ 2026.09.08 (0일)` — 완독일이 시작일보다 이름(서버 UTC 추정). 사용자 지시로 별도 버그 등록 안 함 | — | 기록만 |
| G9 | 시한 | Railway 크레딧 결제(만료 09-28) | 사용자 | open — BUG-23 재발 방지 위해서도 필요 |

- **제안된 자동화 테스트 실제 추가 여부:** 테스터는 원칙상 테스트 코드를 작성하지 않음(리포트로만 제안). 개발 측에서 이번 기간 `josa.test.ts`·`strings.test.ts`(FE 유틸)가 추가됐으나, **FINDING-20260908-18(S0 3화면 렌더 회귀 테스트)은 여전히 미추가**. `test/book-service-unit-tests` 브랜치는 2026-07-20 이후 정지·미병합(스테일).
- **미커밋:** 없음(테스터 영역 워킹트리 clean, `docs/test/`만 산출).
- **정합성(추정):**
  - **BUG-20260901-17이 9일째 미수정.** `SearchBar.tsx`가 그 사이 다른 목적으로 여러 번 수정됐는데도 이 증상이 안 잡힘 → FE가 이 건을 우선순위에서 계속 뒤로 미루는 것으로 추정.
  - **gender(BUG-21) 상태 불명확.** FE가 `86ccc4f`에서 매핑을 넣었을 가능성이 높으나 테스터가 "이번 세션 미확인"으로 남겨둠 → S2 슬라이스 종료 판정이 붕 떠 있음. 다음 테스터 세션에서 gender 왕복 재확인 필요.

---

## PM 관점 종합 스크럼

### 영역별 한 줄 요약

- **백엔드:** blocker(BUG-23)가 재현 실패로 풀리면서, 그 조사에서 파생된 **G20 보안 결함(조회 계열 권한 검증 누락, 전 회원 명부 유출)**을 처리. Apple 로그인·ErrorCode 체계도 진척. 다음은 G17 enum 통일.
- **프론트엔드:** Apple 로그인 + 비회원 폐기 + 한글 IME + UX 문구 전면 리소스화까지 대량 반영. 단 **백엔드가 오늘 바꾼 규약(G20-FE·G21) 반영이 한 사이클 밀림.**
- **디자인:** 로그인 화면 3종 변경(비회원 제거/Apple 추가/4버튼 통일)을 FE·백엔드와 동시 확정. **P0 회원탈퇴 화면(G16)은 이틀째 미착수.**
- **테스터:** 최대 병목이던 S3 blocker를 인프라·코드 전수 확인 후 "재현 안 됨"으로 종결, apk-2026-09-10 스모크·배포. 공식 연동률 **8/41(20%)**.

### 가장 급한 이슈 top 2

1. **[P0 · 보안] G20 후속 정리 — 백엔드는 끝, FE·검증이 남음.** 전 회원 명부(`GET /api/users`, 성별 포함)·비공개 서재가 인증만으로 유출되던 결함을 백엔드가 오늘 막았다(`38008fa`, `#8` 삭제). 남은 일: ① FE가 `libraryApi.fetchBooks`의 죽은 `?userId=` 파라미터 제거(G20-FE), ② 테스터가 `GET /api/books`·`/api/users/{id}`·`/api/groups*` 권한 검증을 실제로 재검증, ③ `#7` 본인 전용 잠금이 S6 그룹 멤버 프로필 표시를 막는지 확인(연동매트릭스는 `#31` 응답 확장으로 우회 권장).
2. **[P0 · 스토어] G16 회원 탈퇴 — 디자인 미착수로 전체가 멈춤.** Apple 심사 필수 항목(5.1.1(v))인데 디자인 진입점·확인 다이얼로그가 이틀 연속 미착수. `color_chips.html` v2.3 재사용이라 경작업이므로 우선 처리 필요. 백엔드 `#10`(DELETE) 재사용이라 신규 API는 없음 → 디자인만 나오면 FE가 바로 붙을 수 있음.

### 영역 간 소통 / 정합성 이슈 (추정 포함)

- **(확정) 백엔드 규약 변경 → FE 반영 지연.** G20(조회 권한)·G21(ErrorCode 스키마) 모두 백엔드가 09-10 새벽~아침에 확정했고 FE 마지막 커밋은 그 이전(00:32). 다음 FE 세션에서 두 건을 함께 처리해야 하며, 그때까지 FE에는 무시되는 파라미터·구버전 에러 파싱이 남아 있음. 당장 앱 동작에는 영향 없음(테스터 스모크 PASS).
- **(추정) S2 슬라이스 종료 판정 공백.** gender 버그(BUG-21)에 대한 FE 대응이 커밋된 것으로 보이나 테스터 재검증이 안 돼 S2가 "미종료"로 계속 떠 있음. 개발↔테스터 사이에 "고쳤으니 재검증 요청" 신호가 명시적으로 오가지 않는 것으로 추정.
- **(추정) BUG-20260901-17 장기 방치.** 9일째 열려 있고 심각도는 minor지만, 서재 검색은 노출 빈도가 높은 화면이라 스토어 리뷰에서 눈에 띌 수 있음. FE 백로그에서 명시적으로 우선순위를 매길 필요.
- **(정보) G9(Railway 결제) ↔ BUG-23 재발 가능성.** BUG-23은 닫히지만 유력 원인이 "그 시점 DB 데이터 소실"이라면 미결제 시(만료 09-28) 같은 증상이 재발할 수 있음. 결제는 사용자 액션.

---

## 커밋/푸시 결과

- 커밋: `bfc5449` `[scrum] 2026-09-10 데일리 스크럼 정리` (docs/scrum/2026-09-10.md 1파일, +149)
- 푸시: **성공** — `origin/main` `d72889d..bfc5449`
- 스테이징: `git reset HEAD -- .` 후 `docs/scrum/2026-09-10.md`만 명시 add. 다른 폴더 미커밋 변경 없었음(워킹트리 clean).
- (이 결과 기록 커밋은 별도로 뒤따름)
