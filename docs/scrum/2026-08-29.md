# 데일리 스크럼 — 2026-08-29

- **관측 방식:** 모노레포 루트 `D:\projects\reading-record-app` 단일 git 저장소. 폴더별 `git log --since` + `--all` + 실제 파일/README·리포트 상태 대조.
- **폴더 매칭:** `backend/`, `frontend/`, `design/`, `docs/test/` 모두 존재.
- **관측 시각:** 2026-08-29 새벽 자동 세션 (로컬 머신 시계는 2026-08-28 23:08 KST로 표시됨 — 시계 스큐 추정. 아래 커밋 타임스탬프는 모두 `+0900` 08-28로 기록됨).
- **브랜치:** `main`(기본), 원격 `origin/main`. `test/book-service-unit-tests`는 존재하나 `origin/main` 대비 신규 커밋 없음(마지막 커밋 2026-07-20) — 실질 비활성. `claude/*` 워크트리 브랜치 3개.
- **직전 스크럼 이후 신규 커밋:** `be89a6b`([scrum] 2026-08-28, 08-28 08:16) 이후 12건 — backend 1, frontend 3, design 1, test/docs 7.
- **워킹트리 미커밋 변경:** `backend/.env.example` 1건만 (아래 백엔드 섹션). frontend·design·docs 워킹트리는 clean.

---

## 백엔드 (신규 커밋 1건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-28 20:59 | `3fa8842` | 테스터 리포트 피드백 반영 — ① `.env` 없이 `gradle bootRun` 기동 실패(S3Presigner NPE)에 `@Bean s3Presigner()` `@Lazy` 추가, ② `ShareRecord` API enum이 대문자만 허용하던 것을 소문자 하나로 API 계약 통일 |
| (이후) | `1c4798f` / `b51ec02` / `9d05372` | 앱 리네임 "콩닥책닥" 확정 — Java 패키지 `com.bookflex` → `com.kongdakchaekdak` 118개(main) + 테스트 10개 파일 이동, `build.gradle.kts` group, `settings.gradle.kts`, `docker-compose.yml`·`application*.yml` DB명 `reading_record_app` → `kongdakchaekdak`, Swagger 타이틀 등. (리네임은 전 영역 공통 커밋) |

**요약:** 테스터가 08-27~28에 올린 backend major 2건(BUG-03 로컬 기동 실패, FINDING-04 share enum 계약)에 첫 대응 커밋(`3fa8842`)이 나옴. 이후 앱 리네임이 backend까지 포함해 확정됐고, 리네임 1차 커밋(`1c4798f`)이 테스트 스위트를 깨뜨렸으나(RENAME-00) 후속 `b51ec02`/`9d05372`로 복구 — 테스터 4차 리포트(`2026-08-28d.md`)에서 `gradle test` 74 tests / 0 fail, `com.bookflex` 잔여 0개 확인, RENAME-00 closed.

**미해결 작업 (테스터 리포트 기반, 담당 개발자 미반영):**
- **BUG-20260827-03 (major, open) — `@Lazy`로는 안 됨.** `3fa8842`가 `@Bean`에만 `@Lazy`를 걸었으나 `BookPhotoService`가 **생성자 주입**으로 `S3Presigner`를 받아 컨텍스트 초기화 중 빈이 실제 생성됨. 테스터 3차·4차 재확인: `gradle bootRun`(local, AWS env 없이) → 여전히 `APPLICATION FAILED TO START` (`Access key ID cannot be blank`). 주입 지점 `@Lazy` 또는 조건부 빈(`@ConditionalOnProperty`)으로 재작업 필요. 블랙박스 테스트는 더미 AWS env 우회 유지 중.
- **FINDING-20260827-04 잔여 (minor, open) — `DashboardPeriod` enum 누락.** `3fa8842`의 소문자 통일이 `ShareType`/`ShareScope`/`SharePlatform`은 처리했으나(테스터 확인: `POST /api/share-records` 소문자 바디 → 201), `DashboardPeriod`는 빠짐. 현재 파일 확인: `enum { MONTH, QUARTER, YEAR }` — `@JsonValue`/`@JsonCreator` 없음. 요청 바디 `"dashboardPeriod":"month"` → 400, `"MONTH"` → 201. (단 `GET /api/dashboard?period=month` 쿼리 파라미터는 대소문자 무관 — 바디만 엄격, 불일치)
- **RENAME-02 (major, open) — DB명 변경 데이터 마이그레이션 안내 필요.** `reading_record_app` → `kongdakchaekdak`. `ddl-auto`로 스키마는 재생성되나 기존 로컬 MySQL 볼륨/Railway DB 데이터는 고아·유실. → backend-agent + 사용자.
- **문서 정합성 (추정):** `docs/독서기록앱_개발현황.md §12-3`의 "자격증명 없어도 기동 정상" 서술이 BUG-03과 배치. 리네임도 `docs/` 기획 문서 4종(개발현황·백엔드구축계획·TODO리스트·화면설계서)에는 미반영(내용·파일명). 역사 문서라 의도적일 수 있음 — 확인 필요.
- **(관측 시점 변경 진행 중)** `backend/.env.example` 미커밋 수정 — 카카오/구글 콘솔 등록 완료(2026-08-28) 주석 추가(`KAKAO_CLIENT_ID`=REST API 키, `GOOGLE_CLIENT_ID`=웹 클라이언트 ID, 실제 값은 `backend/.env`에만). 비밀값은 포함 안 됨. 다음 세션에서 커밋될 것으로 추정.

---

## 프론트엔드 (신규 커밋 3건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-28 20:16 | `a11a5e1` | Android·iOS 패키지명 통일(`readingrecordapp`). 이후 리네임 커밋이 `com.kongdakchaekdak`로 재변경 |
| 08-28 20:35 | `4895474` | 테스터 피드백 반영 — BUG-20260827-02(`npm run typecheck` TS2698, `jest.requireActual` 제네릭 지정), OBS-02(jest 콘솔 노이즈, `__mocks__/react-native-screens.js`), FINDING-05(다크 뉴트럴 램프 비단조), FINDING-06(다크 앱 배경 토큰 `appBackground`), FINDING-07(로그인 화면 브랜딩 배지+워드마크), FINDING-09(`BookSearchScreen` 다크모드) |
| 08-28 21:21 | `e6135d2` | FINDING-10의 미구현 3화면 구현 완료 — `GroupManagementScreen`(04.1) / `ScheduleDetailScreen`(02.1) / `NotificationScreen`(10) 신규 + `MainStack` 3라우트 + 진입점 연결(bell→Notifications, 일정 카드→ScheduleDetail, 공유 카드→GroupManagement) |

**요약:** 테스터가 올린 프론트 이슈에 대대적으로 대응한 세션. 테스터 3차 리포트 검증 결과 BUG-02·OBS-02·FINDING-05·06·07·09·10이 모두 **FIXED 확인**. `npm run typecheck` exit 0 회복, `npm test` 6 suites / 20 tests 콘솔 깨끗. 리네임은 프론트 테스트에 영향 없음.

**미해결 작업 (테스터 리포트 기반, 담당 개발자 미반영):**
- **FINDING-20260828-08 (major, open) — Frame 08.2(책 등록 확인) 미구현.** 현재 `BookSearchScreen.tsx`: 책 선택 시 `Alert.alert(...)` → `navigation.goBack()`. 장르 선택(6칩)·읽기 시작일·"등록하기" 단계 없음 → 책 등록 플로우가 중간에서 끊김. docstring도 여전히 "Frame 03".
- **FINDING-20260828-11 (minor, open) — 6개 장르 고정색 프론트 미반영.** design은 `b325db8`로 6장르 고정색(청화블루 `--chart5`, 먹빛 남색 `--chart6`) 확정. 프론트 `theme/colors.ts` 확인: 여전히 `chart1~4`(4색), `chart5`/`chart6` 없음. `GenreDonutChart`는 등수 순환 방식 유지 → 6장르↔색 고정 매핑 + 도넛/칩 로직 전환 필요. (design/README.md에 확정 매핑 있음)
- **BookSearchScreen 빈 상태 copy 미구분 (minor)** — `EmptyState`가 "검색 결과 없음"만 사용, Frame 07 최초-빈 copy와 구분 안 됨. 로딩 스켈레톤 상태 미연결(mock 동기 데이터).
- **전 화면 mock 상태 유지 — 실제 백엔드 API 연동 0건.** 직전 스크럼의 최우선 병목 그대로. 소스 내 TODO/FIXME 18곳(backend+frontend, 주로 API 연동 대기 지점 + `AuthContext` OAuth 실연동).
- **OBS-01 (이월):** lint CRLF/prettier 노이즈, `.gitattributes` 부재. OBS-06(잘못된 enum → 제네릭 400, 빈 `fieldErrors`) 잔존.
- **리네임 잔재 (minor):** `frontend/package-lock.json` name `ReadingRecordApp` 유지(package.json은 `kongdakchaekdak` — `npm ci` 경고 가능), `frontend/android/settings.gradle` `rootProject.name='ReadingRecordApp'`, iOS Xcode 프로젝트/타깃/스킴명 `ReadingRecordApp` 유지(부분 리네임 — 의도적일 수 있으나 명시 필요).

---

## 디자인 (신규 커밋 1건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-28 21:28 | `b325db8` | 6개 장르 고정색 도입 — 과학·경제·경영 2개 장르에 신규 검증색(청화블루 `--chart5` `#0089ff`/다크 `#009af3`, 먹빛 남색 `--chart6` `#1c1a46`/다크 `#98bed3`) 추가. `dataviz` 스킬 검증기로 색약 분리도 확인(첫 시도 "고서 갈색"은 탈락). 적용 3곳: 장르 비율 도넛차트(등수 기반 "기타" 회색 버킷 제거), 관심분야·장르 선택 칩(구버전 하드코딩 오류 정정), 서재 목록 책 태그(장르 배지 신규). `design/README.md` + `hifi_mockup_v1.html` v1.11 |

**요약:** 직전 스크럼에서 "워킹트리 v1.8 진행 중"으로 관측했던 태블릿·폴더블 대응 작업이 그 사이 v1.9(레이아웃 전면 재설계) → v1.10(Frame 02 가로 스크롤·태블릿 재배치) → v1.11(6장르 고정색)까지 진행돼 **모두 커밋 완료**(README 표·changelog 기준 현재 `hifi_mockup_v1.html` v1.11). 디자인 워킹트리 clean.

**산출물 상태 (`design/README.md` 표 기준):**

| 산출물 | 상태 |
|---|---|
| `app_icon.html` v2.1 (텍스트 워드마크 + iOS 다크/틴트드) | 확정 |
| `splash_screen.html` v1 | 확정 |
| `color_chips.html` v2 / `typography_and_icons.html` v1.2 | 확정 |
| `hifi_mockup_v1.html` v1.11 | 커밋 완료 (v1.8→v1.11 반영) |
| Google Play 피처 그래픽·고해상도 아이콘 | v1 제작 완료 |
| App Store/Play 마케팅 스크린샷(실제 앱 화면 기반) | 미착수 — `store_assets.html`에 "별도 라운드" 명시. 어떤 화면·캡션인지 선행 결정 필요 |

**미해결 / 진행 중:**
- 디자인 측 6장르 고정색은 확정·커밋됐으나 **프론트 미반영**(FINDING-11 — 위 프론트 섹션).
- 마케팅 스크린샷은 에뮬레이터 실제 화면이 필요 → 프론트 구현 진척 + 사용자 결정 대기(디자인 단독 이슈 아님).

---

## 테스터 / QA (신규 커밋 7건)

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 08-28 08:54 | `0b622e0` | **디자인 정합성 2차 리포트**(`2026-08-28b.md`) — Hi-Fi 목업 v1.8 ↔ 10개 화면 구조 대조. major 2(브랜딩 미반영·미구현 프레임), minor 4 |
| 08-28 22:16 | `5f28250` | **3차 리포트**(`2026-08-28c.md`) — 피드백 반영 검증 + 앱 리네임 점검 |
| 08-28 22:21 / 22:35 | (`b51ec02` / `9d05372`) | 리네임 후속(테스트 코드 이동·누락 파일) — 개발 커밋이나 테스터가 RENAME-00 조치 확인에 사용 |
| 08-28 23:03 | `8123474` | **4차 리포트**(`2026-08-28d.md`) — 카카오 로그인 실동작 검증(에러 경로 + 해피패스) |
| 08-28 23:06 | `8976408` | `scenarios/social-login.md` 신규 — 카카오/구글/네이버 소셜 로그인 시나리오(에러 경로 + 유효 토큰 해피패스) |

**최신 리포트:** `docs/test/reports/2026-08-28d.md` (2026-08-28, 4차 — 카카오 로그인). 그 전 `2026-08-28c.md`(3차, 피드백 반영+리네임), `2026-08-28b.md`(디자인 정합성 2차), `2026-08-28.md`(디자인 토큰 대조 1차).

**진행 요약:**
- **카카오 로그인 실동작 확인 (신규 성과)** — 유효 카카오 access token으로 `POST /api/auth/kakao` → 우리 앱 JWT 발급(200) → `GET /api/auth/me` 신규 유저 확인 → 재로그인 시 find-or-create로 중복 유저 없음까지 테스터가 직접 재현(K-10~12). 에러 경로 K-01~07(빈/null/빈문자열 400, 가짜 토큰 401, 구글·네이버도 401, 500 아님) 전부 통과. 닉네임 랜덤 폴백(`RandomNicknameGenerator`)은 설계된 동작 — 콘솔 테스트 토큰에 nickname scope 미동의라 폴백 경로까지 검증됨.
  - 단 `gradle bootRun` 자체는 BUG-03로 기동 실패 → 테스터는 `bootJar` + 더미 AWS env로 우회 기동.
- 화이트박스: `gradle test` 74 tests / 0 fail (리네임 후 `9d05372` 기준), `npm run typecheck` exit 0, `npm test` 6 suites / 20 tests 콘솔 깨끗.
- 리네임 검증: `1c4798f`가 테스트 10파일을 `com.bookflex`에 방치해 `:compileTestJava` 실패(RENAME-00, BUG-01과 동일 패턴 재발) → `b51ec02`/`9d05372`로 조치, **RENAME-00 closed**. 다만 "backend는 리네임 제외"라는 인지와 달리 backend 8개 설정 파일 + Java 128개 파일이 실제 대상이었음(RENAME-01).

**발견된 이슈 (누적, 심각도별):**

| ID | 심각도 | 영역 | 상태 | 담당자 반영 |
|---|---|---|---|---|
| BUG-20260827-01 (BE 테스트 컴파일) | blocker | backend | closed (`ef13bbd`) | ✅ |
| RENAME-00 (리네임이 BE 테스트 재차 파괴) | blocker | backend | closed (`b51ec02`/`9d05372`) | ✅ |
| BUG-20260827-02 (FE typecheck TS2698) | major | frontend | closed (`4895474`) | ✅ |
| FINDING-07 (로그인 브랜딩 미반영) | major | frontend | closed (`4895474`) | ✅ |
| FINDING-10 (미구현 프레임 3개) | major | frontend | closed (`e6135d2`) | ✅ |
| FINDING-05 / 06 / 09 / OBS-02 (다크 토큰·콘솔) | minor | frontend | closed (`4895474`) | ✅ |
| **BUG-20260827-03 (`local` 서버 기동 실패)** | **major** | **backend** | **open** | ❌ `@Lazy`가 생성자 주입 때문에 부족 |
| **FINDING-20260828-08 (Frame 08.2 미구현)** | **major** | **frontend** | **open** | ❌ 미착수 |
| **RENAME-02 (DB명 변경 데이터 마이그레이션)** | **major** | **backend** | **open** | ❌ 안내 미제공 |
| FINDING-20260827-04 잔여 (`DashboardPeriod` enum) | minor | backend | open | ❌ 소문자 통일에서 누락 |
| FINDING-20260828-11 (프론트 6장르 고정색) | minor | frontend | open | ❌ design만 완료 |
| RENAME-03~06 (package-lock, android settings, iOS 프로젝트명, docs 문서) | minor | frontend/docs | open | ❌ |
| OBS-01 (lint CRLF) / OBS-06 (제네릭 400) | — | FE / BE | open | — |

**제안된 자동화 테스트 실제 추가 여부:**
- `requests/테스트코드작성요청_v1.md`에 대해 backend `9da7880`(+7 계약 테스트)·frontend `ed91a36`(+16) 1차 반영 완료. frontend 반영분이 BUG-02를 동반했으나 `4895474`로 해소.
- 커버리지 측정(JaCoCo / `jest --coverage`): 리포트에 제안만, **미도입.**
- `.gitattributes`(CRLF, OBS-01): **미추가.**
- `local`/`dev` 프로필 테스트 계정 시더: **미추가** — 소셜 로그인 외 유저 생성 수단 없음(h2-console 직접 seed 필요).
- 자동 E2E: 프론트–백엔드 연동 후로 보류 결정(`d0bab42`) 유지.

**영역 간 정합성 문제 (추정 포함):**
- **[부분 해소] FINDING-04 — 프론트↔백엔드 share enum 계약.** `3fa8842`로 `shareType`/`scope`/`platform`이 소문자 수용으로 통일돼 `frontend/src/types/share.ts`와 일치 확인. 단 **`dashboardPeriod`만 여전히 대문자 전용** — 같은 계약 통일에서 빠진 1건.
- **[신규·양호] 소셜 로그인 실연동 첫 성공.** FE `AuthContext`가 기대하는 소셜 로그인 → 백엔드 JWT 흐름이 카카오에 한해 실제 동작 확인. 구글은 콘솔 등록 완료(동의화면 "테스트 중" — 게시하려면 홈페이지/개인정보처리방침 URL 필요, 앱 배포 후로 보류), 네이버는 에러 경로만 확인.
- **[확인됨] design → frontend 전달 지연.** 6장르 고정색이 design에서 확정·커밋(`b325db8`)됐으나 프론트 `colors.ts` 미반영. 도넛차트 색상 방식(고정 매핑 vs 등수 순환)이 두 영역에서 다름.
- **[추정] "backend 리네임 제외" 인지 오류.** 사용자/일부 세션이 backend를 리네임 대상에서 뺀 것으로 알고 있었으나 실제로는 포함 — 이번엔 테스트가 잡아냈지만 인지-실제 간극이 반복되면 위험.
- 프론트가 참조하는 API 경로는 여전히 모두 백엔드에 실재(없는 API 참조 없음).

---

## PM 종합

### 영역별 한 줄 요약
- **백엔드:** 테스터 피드백에 첫 대응 커밋(`3fa8842`) + 앱 리네임 backend까지 확정·테스트 복구(74 tests 그린). 단 BUG-03는 `@Lazy` 위치 문제로 여전히 미해결, DB명 변경 마이그레이션 안내 없음.
- **프론트엔드:** 테스터 이슈 대량 소화 — typecheck 회귀·다크 토큰·브랜딩·미구현 프레임 3개(FINDING-10)까지 closed. 남은 major는 Frame 08.2(책 등록 확인) 미구현, 그리고 API 연동은 여전히 0건.
- **디자인:** 태블릿·폴더블 대응 v1.8→v1.11(레이아웃 재설계 + Frame 02 개선 + 6장르 고정색) 전부 커밋 완료, 워킹트리 clean. 마케팅 스크린샷만 미착수(선행 결정 대기).
- **테스터/QA:** 3차·4차 리포트 + 소셜 로그인 시나리오 추가. **카카오 로그인 실동작(해피패스 포함) 첫 검증 성공.** 피드백 루프가 실제로 1사이클 돌아 이슈 8건 closed, open major 3건 추적 중.

### 가장 급한 이슈 (top 2)
1. **BUG-20260827-03 (major) — `local` 서버가 AWS 자격증명 없이 기동 실패, 3차 대응 필요.** `3fa8842`가 `@Bean @Lazy`만 걸었으나 `BookPhotoService` 생성자 주입 때문에 무효 — 테스터가 3차·4차 연속 재확인. 클린 체크아웃에서 문서화된 `gradle bootRun`이 깨지고, 블랙박스 테스트·소셜 로그인 검증이 매번 더미 AWS env 우회를 강요당함. 주입 지점 `@Lazy` 또는 조건부 빈으로 확실히 해결 필요. **backend-agent 최우선.**
2. **FINDING-20260828-08 (major) — Frame 08.2(책 등록 확인) 미구현으로 책 등록 플로우 단절.** `BookSearchScreen`에서 책 선택 시 `Alert` 후 `goBack` — 장르 선택·시작일·"등록하기"가 없어 앱의 핵심 동선(책 검색→서재 등록)이 완성되지 않음. FINDING-10(미구현 3화면)은 이번에 해결했으니 다음은 이것. **frontend-agent.**

부차: `DashboardPeriod` 소문자 수용(FINDING-04 잔여, backend), 프론트 6장르 고정색 반영(FINDING-11, frontend), RENAME-02 DB 마이그레이션 안내(backend+사용자), 리네임 잔재 RENAME-03~06.

### 영역 간 소통 / 정합성
- **테스터 → 개발 피드백 루프가 이번 세션에 처음으로 완주.** 직전 스크럼에서 "1사이클도 안 돌았다"고 지적했던 open 이슈들이 backend 1커밋·frontend 2커밋으로 대거 반영됐고, 테스터가 3차 리포트로 즉시 검증해 8건 closed. 협업 체계가 작동하기 시작함.
- **다만 "일부만 고쳐 커밋" 패턴이 반복 (추정):** `3fa8842`가 share enum은 통일하면서 `DashboardPeriod`를 빠뜨렸고, 리네임 1차 커밋이 test 파일을 빠뜨림(RENAME-00, BUG-01과 동일). `테스트코드작성요청_v1.md` 원칙 4 재위반. 커밋 전 `gradle test`·전체 enum 스캔을 체크리스트화할 필요.
- **design → frontend 단방향 전달에 지연.** 6장르 고정색이 design에서 확정됐지만 프론트가 아직 못 따라옴 — design 변경 시 frontend에 명시적 요청 문서를 남기는 흐름이 있으나(README에 언급) 반영까지의 리드타임 관찰 필요.
- **"backend 리네임 제외" 인지 오류가 테스트로 걸러짐 (추정).** 인지-실제 간극이 반복되지 않도록 리네임 같은 광범위 변경은 영향 범위를 먼저 공유하는 게 안전.
- 소셜 로그인: 카카오 실연동 성공, 구글은 콘솔 "테스트 중"(게시 보류 — URL 필요), 네이버 미검증. 프론트–백엔드 실연동의 첫 접점이 인증에서 열림.
