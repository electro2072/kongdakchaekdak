# 데일리 스크럼 — 2026-09-02

- **관측 방식:** 모노레포 루트 `D:\projects\reading-record-app` 단일 git 저장소. 폴더별 `git log --since` + `--all` + 실제 파일/README·리포트·소스 상태 대조. (이 스크럼은 `reverent-franklin-847915` 워크트리에서 실행하되 `git -C`로 메인 체크아웃(`main`)을 관측)
- **폴더 매칭:** `backend/`, `frontend/`, `design/`, `docs/test/` 모두 존재.
- **관측 시각:** 2026-09-02 새벽 자동 세션 (로컬 머신 시계 = KST). 커밋 타임스탬프 모두 `+0900`.
- **브랜치:** `main`(기본) = `origin/main`. `test/book-service-unit-tests`는 마지막 커밋 **2026-07-20** → 여전히 실질 비활성(main 대비 ~24,700줄 뒤처짐). `claude/*` 워크트리 브랜치 다수.
- **직전 스크럼(`9f997cf`, 2026-09-01 08:34) 이후 신규 커밋: 비-스크럼 9건.**
  - 2026-09-01 낮/밤(테스터): `ec62e51` · `e5b6c51` · `307833b` — 3건
  - 2026-09-02 새벽 08:17~08:22(자동 개발 세션 일괄): `7b9e7c8` · `0a50522` · `a6e4106` · `ee65a50` · `d4ad47b` · `5c4c743` — 6건
  - 분포: backend/ 소스 2, frontend/ 소스 1, design/ 소스 1, docs/test/ 3, docs(공통) 2
- **워킹트리 미커밋 변경:** `backend`/`frontend`/`docs` clean. `design/`에 **untracked 파일 1개** — `design/commit_commands.txt`(디자인 에이전트가 준비한 커밋 명령서, mtime 09-01 08:56). 이번 스크럼 커밋 대상 아님(아래 디자인 § 참고).

> 직전 스크럼은 09-01 08:34 커밋으로 09-01 00:27까지의 프론트 작업 + 소셜 3사 E2E PASS를 다뤘고, **top-1 이슈로 "프론트 워킹트리 미커밋 변경(Frame 08.2 WIP)이 검증 완료된 소셜 SDK 초기화·3개 화면 등록을 삭제한 상태 — 커밋 전 재정리 필요"** 를 올렸다. 이번 스크럼은 그 WIP가 어떻게 커밋됐는지, 그리고 09-01 밤 + 09-02 새벽 커밋을 처음 다룬다.

---

## 백엔드

**신규 커밋: 2건 (`backend/` 소스). 3일 만에 소스 커밋 재개.**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-02 08:17 | `7b9e7c8` | **직전 스크럼 지적 3건 처리.** ① `DashboardPeriod` enum에 `@JsonValue`/`@JsonCreator` 추가 → JSON 계약을 소문자 전용으로 통일(`ShareType`과 동일 패턴). `POST /api/share-records`의 `dashboardPeriod`가 이제 `"month"` 수용, `"MONTH"`는 400. (참고: `GET /api/dashboard?period=`는 컨트롤러가 `toUpperCase()` 후 `valueOf` 하는 별도 로직이라 원래부터 대소문자 무관 — 이번 변경 영향 없음.) ② `ImageStorageUnavailableException`(503) 신설 + `GlobalExceptionHandler` 핸들러 + `BookPhotoService`가 presigned URL 발급 실패(`IllegalArgumentException`/`SdkException`)를 잡아 503으로 감쌈 — BUG-20260827-03 "조각2". ③ `DashboardControllerTest`(+4), `ShareRecordControllerTest`(+26) 테스트 보강 |
| 09-02 08:18 | `0a50522` | 위 "조각2" 마무리 — `ImageStorageUnavailableException.java`(+25), `BookPhotoServiceGracefulFailureTest.java`(+67, graceful 실패 회귀 테스트) 신규 |
| 09-02 08:18 | `ee65a50` | (docs) `독서기록앱_TODO리스트_v2.md`에 **RENAME-02** 절차 명시(+4) — DB명 `reading_record_app`→`kongdakchaekdak`. 정식 배포 전이라 실데이터 없음 → 마이그레이션이 아니라 정리만: 로컬 `docker-compose down -v` 볼륨 재생성 / Railway `DB_NAME` 이미 신 DB 지시 → 자동 생성 / 실데이터 쌓인 뒤엔 `mysqldump` 방식 별도 작성 필요 |

**직전 스크럼 열린 3건 → 이번 기간 전부 처리:**
- **FINDING-20260827-04 잔여 (`DashboardPeriod` 소문자):** ✅ `7b9e7c8`로 해소. (실행 재검증은 테스터 몫.)
- **BUG-20260827-03 조각2 (presigned-URL graceful 실패):** ✅ `7b9e7c8`+`0a50522`로 503 우아한 실패 + 회귀 테스트.
- **RENAME-02 (DB명 마이그레이션 안내):** ✅ `ee65a50`로 TODO 문서에 절차 기재 — "안내"가 산출물이었으므로 처리로 간주.

**미해결/미관측:**
- 백엔드 빌드·테스트 실 실행 결과 **이번 세션 미관측** — 테스트 파일 11개(직전 대비 +1), 신규 테스트 커밋됨. CI(`backend-test.yml`) 결과는 이 세션에서 조회 불가(`gh` 미설치). 컴파일·그린 여부 **추정**.
- Railway 무료 크레딧($5/30일) 만료 전 결제 여부 결정 — **사용자 액션, 미해결.** 안 하면 MySQL 볼륨(데이터) 삭제.
- AWS 사진 업로드 키 `backend/.env`/Railway 미입력 — 기동 blocker 아님(이제 503으로 우아하게 실패), 실제 사진 업로드 기능은 여전히 비활성.
- 소스 내 `TODO/FIXME` 연동 대기 지점(이전과 동일).
- **도서/서재 API(`POST /api/books` 등) 미구현** — 프론트가 설계 문서에서 "있었으면 하는" API로 명시. 프론트–백엔드 다음 연동 덩어리(아래 PM § 참고).

---

## 프론트엔드

**신규 커밋: 1건 (`frontend/` 소스).**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-02 08:19 | `d4ad47b` | 제목 "[frontend] 작업 중 사라진 문서 원복 # E2E에서 PASS한 구글·네이버 로그인 SDK 초기화 # 앱 아이콘 적용 # Frame 08.2". 세 갈래 변경이 한 커밋에 섞임: ① `App.tsx`에 `GoogleSignin.configure()` + `initializeNaverLogin()` 초기화 **복원**(+`naverAuth.ts` 수정) → 테스터 blocker 리그레션 요청 해소. ② 앱 아이콘 전면 적용 — android 전 mipmap 밀도(hdpi~xxxhdpi) + adaptive `ic_launcher.xml` + iOS `AppIcon.appiconset`. ③ Frame 08.2(책 등록 확인) 구현 — `BookRegisterConfirmScreen.tsx` 신규, `LibraryContext.tsx` 신규(서재 mock state), `MainStack.tsx` 재작성(BookDetail 헤더가 `useLibrary()` 사용), `BookDetailScreen`/`BookSearchScreen`/`LibraryScreen`/`types.ts` 수정 |

**해소:**
- **소셜 SDK 초기화 리그레션 (blocker, 테스터 요청 `ec62e51`):** ✅ `d4ad47b`가 `App.tsx` 초기화 복원. 현재 `App.tsx`에 `GoogleSignin.configure` + `initializeNaverLogin()` 존재 확인. **단 실기기 재검증 미실시**(테스터 09-01 리포트 §5 "미실시"). 직전 스크럼 top-1의 절반이 닫힘.
- **FINDING-20260828-11 (관심분야 칩 색):** ✅ 테스터 09-01 리포트 §FINDING-11 재확인 — Sep 1 재빌드 APK(`658bcb9`+`5e25dbb`)에서 칩이 6장르 고유색으로 표시(소설=그린/자기계발=골드/과학=블루). close.

**⚠️ 신규 회귀 (major) — 직전 스크럼 top-1 예측이 그대로 현실화:**
- **`d4ad47b`의 `MainStack.tsx` 재작성이 검증 완료된 3개 화면 등록(`GroupManagement`/`ScheduleDetail`/`Notifications`)을 삭제했다.** 이 3개 `Stack.Screen`은 `e6135d2`(FINDING-10 대응)에서 추가됐고 테스터 **2026-09-01 E2E에서 화면 #8(알림)/#9(일정 상세)/#11(그룹 관리)로 정상 렌더·네비게이션 PASS** 확인된 것.
- 현재 상태(HEAD `5c4c743` 기준):
  - 화면 컴포넌트 파일 존재: `GroupManagementScreen.tsx`, `NotificationScreen.tsx`, `ScheduleDetailScreen.tsx` ✅
  - `navigation/types.ts`에 3개 라우트 선언 그대로 남음 ✅
  - 살아있는 호출부: `ScheduleScreen.tsx:71` `navigate('Notifications')`, `ScheduleScreen.tsx:180` `navigate('ScheduleDetail')`, `ShareScreen.tsx:71` `navigate('GroupManagement')` ✅
  - **`MainStack.tsx`에 3개 `Stack.Screen` 등록 없음** ❌ (`import`도 함께 제거됨)
- **결과:** 일정 탭의 벨 아이콘, 모임 카드, 공유 탭 "그룹 관리 · 공유 이력" 진입 시 미등록 라우트로 네비게이션 → 무반응/개발모드 에러. 09-01에 PASS했던 3개 화면이 최신 `main`에서 접근 불가.
- **프론트 CI(typecheck)가 못 잡는다** — 라우트가 `types.ts`에 남아 있어 `navigate()` 호출은 타입 통과, 제거된 import도 클린해서 lint/tsc 에러 없음. 커밋 하나에 무관한 변경(아이콘·Frame 08.2·MainStack 재작성)이 섞여 회귀가 묻혔다.
- **회부:** `reading-record-frontend-agent` — `MainStack.tsx`에 3개 `Stack.Screen` + import 재등록 → 릴리스 APK 빌드 → 게스트 E2E로 화면 8/9/11 재확인.

**이월된 미해결 (테스터 리포트 기반):**
- **BUG-20260901-17 (major, open) — 서재 검색창 placeholder 한글 자모 분해.** `LibraryScreen` 책 제목 검색 `TextInput` placeholder가 `섹 세ᅳ긐ᅳ노 검색`처럼 렌더. **라이트·다크 모두 동일**(테마 무관 → 폰트/자모 조합 문제 확정). `5e25dbb`(한글 IME)는 입력값 검증이지 placeholder가 아님. → frontend-agent.
- **OBS-20260901-18 (minor, open) — BookSearch "검색" 버튼 파란색.** `SearchBar.tsx`가 테마 시스템 이전 레거시 컴포넌트 추정.
- **DARK-01 (minor, open, 신규) — `MonthlyTrendChart` 비강조 막대**가 다크 배경에서 거의 안 보임. **DARK-02 (minor, open, 신규) — 서재 책 카드 보조 텍스트** 다크 대비 낮음.
- **E2E 이슈 2 (open) — 신규/게스트 유저에게 mock 데이터 노출.** 일정("아몬드 62%")/서재("책벙개") 탭이 백엔드 미연동, empty-state 미구현. 뿌리는 도서/서재 API 부재.
- **E2E 이슈 3 (스토어 제출 시 major) — 16KB page size 미대응 경고.** Google Play 제출 차단 요인. RN 0.75+/NDK r27 필요. 스토어 준비 단계.
- **OBS-01 (open) — lint CRLF 다수 에러.** 프론트 CI에서 lint 제외 유지, `.gitattributes` 미도입.

**미커밋 변경:** 없음(`frontend/` clean).

---

## 디자인

**신규 커밋: 1건 (`design/` 소스). ~4일 만에 첫 커밋.**

| 시각(KST) | 커밋 | 내용 |
|---|---|---|
| 09-02 08:17 | `a6e4106` | **로딩 인디케이터 세트 신규.** ① `color_chips.html` v2.1 — 범용 로딩 스피너 컴포넌트(버튼 내부 16px / 섹션·카드 28px / 전체화면 오버레이 40px, 라이트·다크 각각, +96줄). ② `hifi_mockup_v1.html` v1.12 — **Frame 07.2 서재 탭 (전체 화면 dim 로딩 오버레이)** 신규(+130줄, 반투명 스크림 `rgba(10,11,8,0.58)` + 흰 스피너 + "불러오는 중..." 라벨, 콘텐츠 있는 상태에서 재조회용 — 콘텐츠 없을 때의 Frame 07.1 스켈레톤과 별개). ③ `README.md` 산출물 표 버전 갱신(hifi_mockup v1.12, color_chips v2.1) + 콜아웃 2건(+8줄) |

- README 산출물 상태 표 기준: `hifi_mockup` v1.12 · `color_chips` v2.1 · `app_icon.html` v2.1(확정) · `splash_screen.html` v1(확정) — 상태 변화는 위 커밋 반영분뿐.
- **경미한 정합성 불일치 (추정):** committed `design/README.md`는 산출물 파일명을 `_v1` 제거된 이름(`hifi_mockup.html` 등)으로 서술하지만, **실제 디스크 파일은 `hifi_mockup_v1.html`·`branding_proposal_songpyeon_v1.html`·`app_icon_text_concept_v1.html`·`splash_screen_concept_v1.html` 그대로**(`git mv` 미실행). untracked `design/commit_commands.txt`(디자이너가 커밋할 수 없어 사용자에게 남긴 명령서)의 "2. 파일명에서 _v1 제거" 단계가 아직 실행 안 됐기 때문으로 보임. README 서술과 파일명이 어긋난 상태.
- **미착수 유지:** 마케팅 스크린샷(App Store 6.5"/6.9", `store_assets.html` §) — 어떤 화면·캡션을 보여줄지 사용자 결정 + 앱 화면 안정 대기.
- **미커밋 변경:** `design/commit_commands.txt` 1개 untracked(디자이너 준비 파일). 이번 스크럼은 `docs/scrum/` 외 파일을 건드리지 않으므로 stage하지 않음.

---

## 테스터 / QA

**최신 리포트:** `docs/test/reports/2026-09-01.md` (09-01 22:58 갱신 — §4 다크모드 6화면 + FINDING-11 해소 확인 추가). 이전 리포트는 `2026-08-31.md`(빌드 3종 검증).

**이번 기간 테스터 활동 (`[test]` 커밋 3건):**
- `307833b` — `docs/test/fixtures/dashboard-seed.sql`(+83) + `fixtures/README.md`(+70). 유저 `id=9001` / 완독 22권(6장르 전부) / 진행중 3권 / 전부 `CURRENT_DATE` 상대값 → 재실행 가능. H2(로컬)+MySQL 8(Railway) 호환.
- `ec62e51` — `docs/test/requests/소셜로그인_App초기화_리그레션_수정요청_v1.md`(+117). 심각도 **blocker**. 원인 확정(App.tsx 소셜 SDK 초기화 삭제) → frontend-agent. **→ `d4ad47b`로 해소됨(실기 재검증은 대기).**
- `e5b6c51` — `2026-09-01.md` 리포트에 다크모드 6화면(로그인/일정/서재/프로필/대시보드/프로필편집) + FINDING-11 해소 확인 추가(+27).
- `5c4c743` — `docs/test/requests/대시보드_테스트데이터_전달_v1.md`(+91, backend-agent 전달) + `scenarios/backend-api.md` JWT 시크릿 안내 정리 + `tools/mint-jwt.mjs`(+9).

**발견된 버그/이슈 (심각도별):**

| ID | 심각도 | 영역 | 상태 | 비고 |
|---|---|---|---|---|
| **MainStack 3화면 회귀** (GroupManagement/ScheduleDetail/Notifications 미등록) | **major** | frontend | **open (이번 스크럼 신규 확인)** | `d4ad47b`가 삭제. 09-01 E2E PASS 화면. 테스터는 아직 최신 빌드 재확인 안 함 → 다음 테스터 세션 예상, PM이 선회부 |
| BUG-20260901-17 (서재 검색 placeholder 한글 깨짐) | **major** | frontend | **open** | 라이트+다크 재확인. 폰트/자모 조합 확정 |
| OBS-20260901-18 (검색 버튼 파란색) | minor | frontend | open | `SearchBar.tsx` 레거시 |
| DARK-01 (MonthlyTrendChart 비강조 막대 안 보임) | minor | frontend | open (신규) | 다크 가독성 |
| DARK-02 (서재 카드 메타 텍스트 딤) | minor | frontend | open (신규) | 다크 대비 |
| FINDING-20260828-11 (관심분야 칩 색) | minor | frontend | **해소 확인(`658bcb9`)** | close |
| 소셜 SDK 초기화 리그레션 | blocker | frontend | **해소(`d4ad47b`)** | 실기 재검증 대기 |
| E2E 이슈 1 (카카오 콘솔 앱 이름 "독서 기록 어플 개발") | minor | 운영/콘솔 | **open** | Kakao 앱 ID `1502106` → "콩닥책닥". 동의화면 노출 중. **사용자 액션** |
| E2E 이슈 2 (신규/게스트 유저 mock 데이터) | minor | frontend | open | 서재/일정 탭 백엔드 미연동 |
| E2E 이슈 3 (16KB page size 경고) | minor(제출 시 major) | frontend/build | open | Google Play 제출 차단 |
| RENAME-02 (DB명 마이그레이션 안내) | major | backend | **해소(`ee65a50`)** | TODO 문서에 절차 기재 |
| FINDING-20260827-04 잔여 (`DashboardPeriod` enum 소문자) | minor | backend | **해소(`7b9e7c8`)** | 실행 재검증 대기 |
| BUG-20260827-03 조각2 (presigned-URL graceful 실패) | (파생) | backend | **해소(`7b9e7c8`+`0a50522`)** | 503 + 회귀 테스트 |
| 사소 4~5건 (완독 표기 / `«»` vs `『』` / nav 헤더 타이틀 중복 / "책 검색"↔"책 등록" 헤더) | trivial | frontend | open | 09-01 리포트 §3 |

**제안된 자동화 테스트 실제 추가 여부:**
- 테스터는 이번 기간에도 자동화 테스트 코드 미작성(원칙 — 리포트/요청 문서만).
- 백엔드가 이번 기간 `BookPhotoServiceGracefulFailureTest`(회귀 테스트) 1건 자체 추가 — 스크럼 피드백 반영분.
- 기존 CI(`backend-test.yml` / `frontend-test.yml` typecheck+jest, lint 제외) — 이번 기간 워크플로 변경 없음.
- 커버리지 측정(JaCoCo / `jest --coverage`), `.gitattributes`(OBS-01), 자동 E2E: **여전히 미도입.**
- 다음 예정(리포트 §5 미실시): 화면별 다크 세부, 폼 입력 상호작용(닉네임 타이핑), 태블릿/폴드 레이아웃, **소셜 로그인 실기기 재검증(App.tsx 복원 후)**.

**영역 간 정합성 문제 (추정 포함):**
- **[신규·확정, 높은 우선순위] 프론트 MainStack 3화면 회귀** — 위 상세. 테스터 09-01 리포트가 PASS로 기록한 화면 8/9/11이 최신 `main`에서 접근 불가. 직전 스크럼이 top-1로 예측한 회귀가 그대로 커밋됨.
- **[정상 진행] 대시보드 시드 ↔ `DashboardPeriod` 수정** — 테스터가 "리캡 실데이터 확인" 목적으로 시드 SQL 전달, 마침 backend가 이번에 `DashboardPeriod` JSON 계약도 소문자로 정정. 단 프론트 대시보드/서재 탭이 백엔드 미연동이라 **시드 적용 후에도 앱 화면에서는 실검증 불가** — 시드는 백엔드 API 블랙박스(`GET /api/dashboard`) 검증용으로만 유효.
- **[해소] design → frontend 6장르색** — `658bcb9` 반영, 테스터 재확인(FINDING-11 close).
- **[이월] 구글 브랜딩 인증** — `PRIVACY.md` 내용 완성. 남은 건 공개 `https://` 호스팅 + 콘솔 URL 등록, 실도메인 대기.
- 프론트가 참조하는 API 경로는 모두 백엔드에 실재(없는 API 참조 없음). `POST /api/books`는 프론트가 "있었으면 하는" 미구현 API로 설계 문서에 명시.

---

## PM 종합

### 영역별 한 줄 요약
- **백엔드:** 3일간 문서만이던 흐름을 끊고 소스 커밋 재개 — 직전 스크럼 열린 3건(`DashboardPeriod` 소문자·BUG-03 조각2·RENAME-02 안내)을 이번 기간에 전부 처리 + 회귀 테스트 1건 추가. 잔여 blocker는 **Railway 크레딧 결제 결정(사용자)** 뿐. (빌드·CI 그린은 이번 세션 미관측 → 추정.)
- **프론트엔드:** 테스터 blocker(소셜 SDK 초기화)를 `App.tsx` 복원으로 해소 + 앱 아이콘 전면 적용 + Frame 08.2 착수. **그러나 같은 커밋 `d4ad47b`가 `MainStack`에서 검증 완료된 3개 화면 등록을 삭제 — 직전 스크럼 top-1 예측이 그대로 실현됨.**
- **디자인:** ~4일 만에 커밋 1건 — 범용 로딩 스피너 컴포넌트(color_chips v2.1) + Frame 07.2 dim 오버레이(hifi_mockup v1.12). 파일명 `_v1` 제거는 미실행 상태로 남아 README 서술과 실제 파일명이 어긋남(경미).
- **테스터/QA:** 09-01 리포트에 다크모드 6화면 + FINDING-11 해소 보강. 대시보드 시드 SQL·리그레션 수정요청·JWT 안내 등 개발팀 인계 문서 3건 생산. blocker 1건이 프론트에서 해소됨.

### 가장 급한 이슈 (top 2)
1. **[신규·확정, major] 프론트 `MainStack` 3화면 회귀 — `reading-record-frontend-agent` 즉시 조치.** `d4ad47b`의 `MainStack.tsx` 재작성이 `GroupManagement`/`ScheduleDetail`/`Notifications` 3개 `Stack.Screen`을 삭제(무관한 Frame 08.2·아이콘 변경에 섞임). 화면 파일·`types.ts` 라우트·`navigate()` 호출부(ScheduleScreen·ShareScreen)는 그대로 → 일정 탭 벨·모임 카드·공유 탭 "그룹 관리" 진입 시 네비게이션 실패. 2026-09-01 E2E PASS 화면. **프론트 CI typecheck 통과하므로 CI가 못 잡음.** 조치: `MainStack`에 3개 `Stack.Screen` + import 재등록 → 릴리스 빌드 → 게스트 E2E로 화면 8/9/11 재확인. 커밋은 프레임 단위로 분리(무관 변경 섞지 말 것).
2. **[이월, major] BUG-20260901-17 — 서재 검색창 placeholder 한글 자모 분해.** `LibraryScreen`. 라이트·다크 모두 재확인(테마 무관, 폰트/자모 조합). `reading-record-frontend-agent`.

**부차:** 카카오 콘솔 앱 이름 "독서 기록 어플 개발" → "콩닥책닥"(**사용자 액션**, 앱 ID `1502106`, 로그인 동의화면 노출 중) · Railway 크레딧 결제 결정(**사용자**) · 16KB page size(스토어 제출 차단, 준비 단계) · OBS-20260901-18(검색 버튼 색) · DARK-01/02(다크 가독성) · 소셜 로그인 실기 재검증(App.tsx 복원 후, 테스터).

### 영역 간 소통 / 정합성
- **"커밋 전 로컬 검증 + 커밋 분리" 이슈가 예측대로 실현.** 직전 스크럼이 top-1로 "WIP가 소셜 초기화·3화면 등록을 삭제한 상태 — 커밋 전 재정리"를 올렸는데, 결과적으로 소셜 초기화는 복원됐지만 **3화면 삭제는 그대로 커밋**됐다. 한 커밋(`d4ad47b`)에 아이콘·Frame 08.2·`MainStack` 재작성이 섞여 회귀가 묻히기 쉬웠음. → 메모리 `feedback_commit_per_frame`(프레임 단위 커밋) 재확인 + 커밋 직후 게스트 E2E 재확인 습관 필요.
- **백엔드 피드백 루프 정상화.** 3일간 아키텍처 문서만이던 백엔드가 이번 주기에 스크럼 지적 3건을 모두 소화. 백엔드 담당의 활동 주기가 회복됨.
- **다음 연동 덩어리 = 도서/서재 API — 여전히 미해결.** Frame 08.2가 `LibraryContext`(mock state)를 도입했지만 `POST /api/books` 없이는 영속화 불가. 신규/게스트 유저 mock 데이터 노출(E2E 이슈 2)도 같은 뿌리. 테스터의 대시보드 시드 SQL은 준비됐지만 프론트 대시보드 탭이 백엔드 미연동이라 앱 실검증 불가 — 백엔드가 이 API를 열어줘야 프론트 서재/일정/대시보드 탭이 실데이터로 전환 가능.
- **소셜 로그인 마일스톤 사실상 종료** — 3사 Android 실기 E2E PASS(직전). 이번 기간 초기화 리그레션이 잠깐 blocker였으나 `d4ad47b`로 복원. 꼬리: 카카오 콘솔 앱 이름(사용자), 실기 재검증(테스터), iOS E2E(맥 부재), 회원탈퇴 시 unlink 흐름.
- **디자인 산출물 정합성(경미, 추정).** committed `design/README.md`는 `_v1` 제거된 파일명으로 서술하나 실제 파일은 `hifi_mockup_v1.html` 등 유지 — `design/commit_commands.txt`(untracked) step 2 `git mv` 미실행 때문. 디자인 에이전트가 커밋 권한이 없어 사용자 실행 대기 중인 명령이 쌓여 있는 구조적 문제.

---

## 커밋 / 푸시 결과
- 커밋 대상: `docs/scrum/2026-09-02.md` 1개 파일만 (`git -C "D:/projects/reading-record-app" add docs/scrum/2026-09-02.md` — 명시적 경로. `design/commit_commands.txt` untracked는 stage하지 않음)
- 커밋 메시지: `[scrum] 2026-09-02 데일리 스크럼 정리`
- 푸시: `git -C "D:/projects/reading-record-app" push origin main`
- 결과(성공/실패·해시): 아래 실행 로그에 이어서 기록
