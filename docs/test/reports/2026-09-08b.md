# 테스트 리포트 — 2026-09-08 (b)

- 작성: reading-record-tester-agent
- 대상 커밋: `b3fd618` (origin/main HEAD)
- 범위: **S0 슬라이스만** (`docs/연동매트릭스_v1.md` §5) — 사용자 지시로 이번 세션은 S0 진입 크래시 확인에 한정
- 방식: Android 에뮬레이터(Pixel_7, API 34) 실기기 E2E — release APK 네이티브 재빌드 후

---

## 1. 결론

| 슬라이스 | 완료 조건 (§5) | 판정 |
|---|---|---|
| **S0** — `MainStack` 3화면 등록 (커밋 `52e7501`) | 일정 탭 벨 / 일정 카드 / 공유 탭 "그룹 관리" 눌러서 안 죽음 | **✅ PASS** |

3개 진입점 모두 크래시 없이 화면 렌더, 뒤로가기(pop)도 정상. logcat 전체 스윕에서 JS 에러·FATAL·ErrorBoundary catch 0건.

> **A표(§2) 실기기 칸 갱신 없음.** S0는 라우터 등록(네비게이션)이지 엔드포인트 연동이 아니라
> 대응되는 A표 행이 없다. 연동률(x/37)은 그대로. S0는 §5 슬라이스 표에서만 완료 처리 대상.

---

## 2. 화이트박스 — 네이티브 재빌드

FE 전달사항 #1(“EncryptedStorage는 네이티브 모듈이라 재빌드 필수”) 검증:

- `frontend/android` 에서 `./gradlew assembleRelease` → **BUILD SUCCESSFUL in 4m 2s**
  - `react-native-encrypted-storage`(커밋 `df85ab8`) 오토링킹 정상.
  - 빌드 중 `compileSdkVersion 33` 자동 다운로드/설치됨(해당 라이브러리가 SDK 33 요구). 최초 1회 지연.
- APK: `app-release.apk` 58.9MB (4 ABI, JS 번들 포함), 서명 SHA-1 기존과 동일.
- ⚠️ **iOS는 미검증** (Mac 없음). FE 안내대로 `cd ios && pod install` 후 재빌드 필요 — 사용자/CI 몫.

---

## 3. 블랙박스 — 에뮬레이터 E2E

### 절차
1. 이전 설치본 제거 → 새 `app-release.apk` 설치 (`adb install -r`).
2. 앱 실행 → LoginScreen 정상 표시 (클린 설치라 복원할 세션 없음 — 로그인 화면이 맞음).
   - 참고: secureStorage 복원 로직이 세션 없는 상태에서 크래시 없이 로그인 화면으로 떨어짐.
3. **"비회원으로 둘러보기"** → 하단탭(일정/서재/공유/프로필) 진입, 일정 탭 착지.

### S0-1. 알림 (Notifications) — `navigate('Notifications')`
- 진입점: 일정 탭 헤더 우상단 종 아이콘 (`testID=notifications-bell`).
- 결과: **✅ "알림" 화면 렌더.** 알림 4건(새 멤버/반응/일정/리캡), 안 읽음 도트, 시간 라벨.
- 스크린샷: `03-notifications.png` · 크래시/예외 없음.

### S0-2. 일정 상세 (ScheduleDetail) — `navigate('ScheduleDetail')`
- 진입점: 일정 탭 "이번주 독서모임 일정이 있어요!" 카드 (`testID=upcoming-meeting-card`).
- 결과: **✅ "일정 상세" 화면 렌더.** 모임 카드(책벙개 / 7월 20일 14:00 / 홍대입구역 / +5), 소개 문단,
  "캘린더에 추가" · "모임 상세보기" 버튼.
- 스크린샷: `04-scheduledetail.png` · 크래시/예외 없음.
- (버튼 2개는 주석대로 아직 no-op — S0 범위 아님)

### S0-3. 그룹 관리 · 공유 이력 (GroupManagement) — `navigate('GroupManagement')`
- 진입점: 공유 탭 "그룹 관리 · 공유 이력" 카드 (`testID=group-management-link`).
- 결과: **✅ "그룹 관리 · 공유 이력" 화면 렌더.** 내 그룹(독서모임 8명 / 가족 4명, "관리" 버튼),
  "새 그룹 만들기", 공유 이력 3건.
- 스크린샷: `06-groupmanagement.png` · 크래시/예외 없음.

### 뒤로가기
- 각 화면에서 `keyevent 4` (뒤로) → 부모 탭으로 정상 복귀, pop 시 크래시 없음.

---

## 4. 관찰 (S0 범위 밖 — 참고용, 버그 아님)

- 3화면 모두 여전히 mock (`meetingDetail.ts` / `notifications.ts` / `shareGroups.ts` + `shareHistory.ts`).
  §5 기준 S6(공유·그룹) / S7(일정·알림)에서 실연동 예정. S0는 "안 죽는 등록"까지가 목표라 정상.
- 일정 탭 상단 문구 "2026년 7월", 일정 상세 "7월 20일" 등 mock 날짜가 과거(현재 9월) — mock 특성.
- 알림·일정상세는 백엔드 API 자체가 없음(매트릭스 §2 주석, `notifications.ts` "🔴 없음").

---

## 5. 다음 세션에서 이어서 볼 것

- **S1** (커밋 `ae86667` 세션 영속화) — FE 전달사항 #2:
  "로그인 → 앱 강제종료 → 재실행 → 로그인 유지" 실기기 확인. 실제 소셜 계정 필요(사용자 동반).
  기존 로그인 단말은 토큰 저장소/키가 바뀌어 첫 실행 시 1회 로그인 화면으로 떨어짐(정상).
- **S2** (커밋 `b3fd618` 프로필 실연동, `mocks/profile.ts` 삭제) — 닉네임 변경 후 재시작 유지 확인.
- S1 PASS 전에는 S2~S6 실기기 판정 보류 (§5: "S1이 안 되면 S2~S6은 전부 무의미").

---

## 부록 — 재현
```
cd frontend/android && ./gradlew assembleRelease
adb install -r app/build/outputs/apk/release/app-release.apk
adb shell am start -n com.kongdakchaekdak/.MainActivity
# 비회원으로 둘러보기 → 일정탭 종아이콘 / 일정카드 / 공유탭 그룹관리카드 탭
# adb logcat -d | grep -iE "FATAL|Exception|ReactNativeJS.*Error"
```
스크린샷: 세션 스크래치패드 `e2e/03-notifications.png`, `04-scheduledetail.png`, `06-groupmanagement.png`.
