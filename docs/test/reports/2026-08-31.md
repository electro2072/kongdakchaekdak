# 테스트 리포트 — 2026-08-31 (앱 빌드 검증)

- 작성: reading-record-tester-agent
- 대상 커밋: HEAD `0eaa12f` (프론트 버전 고정 커밋 `4bb9c86` 반영 상태)
- 요청: "앱 빌드가 되는지 테스트"
- 환경: Windows, JDK 17.0.19, Node 22.23.1, Android SDK `~/AppData/Local/Android/Sdk`,
  Gradle wrapper 8.14.3

## 요약

| 빌드 | 명령 | 결과 |
|---|---|---|
| 백엔드 | `backend/ ./gradlew build` | ✅ **BUILD SUCCESSFUL** 27s (compile + test 74 + jar) |
| Android 디버그 | `frontend/android/ ./gradlew assembleDebug` | ✅ **BUILD SUCCESSFUL** 2m 3s — `app-debug.apk` 135.8 MB |
| Android 릴리스 | `frontend/android/ ./gradlew assembleRelease` | ✅ **BUILD SUCCESSFUL** 3m 21s — `app-release.apk` 58.8 MB (JS 번들 내장, R8+Hermes, 단독 실행 가능) |

**결론: 백엔드·Android 디버그·Android 릴리스 3종 빌드 모두 정상.** iOS만 미검증(Mac 부재).

**`4bb9c86`의 "앱 빌드 시 오류" 는 해소됨** — safe-area-context를 `^5.8.0` → `4.10.8`로
내리고 버전을 전부 고정한 뒤 Android 디버그 APK가 정상 생성됨.

---

## 1. 백엔드 빌드 — ✅

```
cd backend && ./gradlew build
> Task :test        (74 tests, 0 failures)
> Task :check
> Task :build
BUILD SUCCESSFUL in 27s   (8 actionable tasks: 5 executed, 3 up-to-date)
```
컴파일·테스트·jar 패키징 전부 통과. `kongdakchaekdak-backend-0.0.1-SNAPSHOT.jar` 생성.
(Gradle 9.0 deprecation 경고만 있음 — 동작에 영향 없음.)

## 2. Android 디버그 빌드 — ✅

```
cd frontend/android && ./gradlew assembleDebug
BUILD SUCCESSFUL in 2m 3s   (257 actionable tasks: 164 executed, 93 up-to-date)
```
- 산출물: `frontend/android/app/build/outputs/apk/debug/app-debug.apk` — **135,852,937 bytes (2026-08-31 22:57)**.
- 첫 빌드라 CMake 3.22.1 자동 설치(네이티브 빌드 툴체인) 후 진행됨 — 이후 빌드는 더 빠름.
- 네이티브 모듈 5종 + 소셜 로그인 SDK 3종(kakao/naver/google-signin) 전부 컴파일·JNI 링크 성공:
  `:react-native-screens`, `:react-native-safe-area-context`, `:react-native-svg`,
  `:react-native-config`, `@react-native-google-signin/google-signin`,
  `@react-native-seoul_kakao-login`, `@react-native-seoul_naver-login`.
- `.env` 로딩 정상(`Reading env from: .env` — react-native-config가 KAKAO_NATIVE_APP_KEY 등 주입).
- 경고: `NativeSafeAreaContextSpec.java` / `NativeScreensModuleSpec.java` "deprecated API 사용" —
  Paper(구 아키텍처, `newArchEnabled=false`) 코드젠 shim의 관례적 경고. 빌드/동작에 영향 없음.
  CMake Warning 몇 건도 네이티브 빌드 관례적 경고.

> ⚠️ **디버그 APK는 JS 번들을 내장하지 않는다** — 실행 시 Metro(`npm start`)가 떠 있어야
> JS를 로드한다. "APK가 만들어진다" ≠ "Metro 없이 단독 실행된다". 단독 실행 가능 여부는
> §3 릴리스 빌드(JS 번들 내장)로 확인.

## 3. Android 릴리스 빌드 — ✅

```
cd frontend/android && ./gradlew assembleRelease
BUILD SUCCESSFUL in 3m 21s   (340 actionable tasks: 329 executed, 11 up-to-date)
```
- 산출물: `app/build/outputs/apk/release/app-release.apk` — **58,819,604 bytes (2026-08-31 23:01)**.
  디버그(135.8MB)보다 작음 = R8 minify + Hermes 바이트코드 + 디버그 심볼 제외.
- **JS 번들 내장 확인**: `createBundleReleaseJsAndAssets/index.android.bundle` 2.5MB 생성 → Hermes로
  바이트코드 컴파일 후 APK에 포함. **릴리스 APK는 Metro 없이 단독 실행 가능**.
- `lintVitalRelease` 통과 (릴리스 빌드를 막는 fatal lint 없음).
- R8/ProGuard 통과 (소셜 SDK·네비게이션 등 난독화 규칙 충돌 없음). `proguard-rules.pro` 기본값.
- Hermes 경고(`requestIdleCallback`/`cancelAnimationFrame`/`SharedArrayBuffer` "not declared") 다수 —
  런타임 주입 전역이라 정적 미선언, **관례적 무해 경고**.
- RN 템플릿 기본값대로 release도 `debug.keystore`로 서명 → 별도 서명 설정 없이 빌드됨
  (실제 배포 시엔 별도 키스토어 필요, `build.gradle`에 주석 있음).

---

## 4. 버전 고정 (`4bb9c86`) 검토

`frontend/package.json` — `dependencies`의 `^` 전부 제거하고 정확 버전 고정:
| 패키지 | 이전 | 현재 |
|---|---|---|
| `react-native-safe-area-context` | `^5.8.0` | **`4.10.8`** (major 다운) — 5.x가 RN 0.74와 비호환이라 내린 것으로 보임. 4.10.8은 RN 0.74 지원 |
| `react-native-screens` | `^3.37.0` → (`~4.8.0`) | `~4.8.0` 유지 (RN 0.74 지원 범위, 2026-08-29 리포트에서 확인) |
| `lucide-react-native` | `^1.25.0` | `1.37.0` |
| `react-native-svg` | `^15.15.5` | `15.12.1` |
| `@react-navigation/*` | `^7.x` | 고정 7.x |
| `react-native-config` | `^1.5.10` | `1.5.10` |

`devDependencies`는 아직 `^` 남아 있음(빌드에 영향 적은 babel/eslint/jest 계열). 필요 시 별도 정리.

- `npm install` → "up to date" (lockfile과 node_modules 동기 상태).
- `npm audit`: 20 vulnerabilities (7 moderate, 13 high) — RN 생태계에서 흔함, 빌드 차단 아님. 기록만.

## 5. 참고 / 이월
- `npm run typecheck` ✅, `npm test` ✅ 6/6·20/20, Metro 번들 ✅ (2026-08-29b 리포트에서 확인, 오늘 변화 없음).
- 실기기 실행 + 소셜 로그인 온보딩 E2E(FE-SL-01~09)는 [scenarios/social-login-onboarding-e2e.md]
  (../scenarios/social-login-onboarding-e2e.md) — 여전히 사람이 에뮬레이터에서 실행 필요.
- iOS 빌드는 Mac 부재로 미검증(범위 밖).
- 미해결(이전): FINDING-04 잔여, FINDING-08, FINDING-11, FINDING-16(프론트 CI).

## 6. 커밋
- `docs/test/reports/2026-08-31.md` — `[test]` 접두사.
