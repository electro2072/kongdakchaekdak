# 테스트 리포트 — 2026-09-08

- 작성: reading-record-tester-agent
- 대상 커밋: `9faed6c` (HEAD)

---

## 1. 대시보드 시드 — 엣지 케이스 추가 + 실측 검증

### 배경
백엔드 회신(`docs/test/requests/대시보드_테스트데이터_확인_v1.md` §3):
"시드에 완독 기간 동률 2권 / `total_pages` NULL 1권을 추가해 tie-break·null 합산을 실측 확인해달라."

### 한 일
`docs/test/fixtures/dashboard-seed.sql` 에 엣지 케이스 3권 추가 (완독 22 → 25권):

| id | 제목 | 장르 | total_pages | 완독일수 | 완독일 |
|---|---|---|---|---|---|
| 23 | 오래 물고 있던 책 A (동률·먼저) | 소설 | 300 | 41 | 오늘 |
| 24 | 오래 물고 있던 책 B (동률·나중) | 소설 | 320 | 41 | 오늘 |
| 25 | 페이지수 미상 에세이 | 에세이 | **NULL** | 4 | 오늘 |

### 검증 방법
로컬 `bootRun`(local 프로파일, H2 in-memory) → `RUNSCRIPT` 로 시드 로드 →
유저 9001 JWT(`backend/.env` 의 `JWT_SECRET`) 발급 → 실제 `GET /api/dashboard` 호출.

### 결과 — 전부 PASS

| period | completedBookCount | totalPagesRead | longestReadBook | 비고 |
|---|---|---|---|---|
| month (2026-09) | 6 | 1552 | **bookId 23** (41일) | 932 + 300 + 320 + **0** = 1552 |
| quarter (3분기) | 10 | 2912 | bookId 23 (41일) | 2292 + 620 = 2912 |
| year (2026) | 23 | 6528 | bookId 23 (41일) | 5908 + 620 = 6528 |

- **tie-break** ✅ — 41일 동률 2권(id 23, 24) 중 `longestReadBook.bookId = 23`.
  먼저 INSERT 되어 id가 작은 A가 이김. 백엔드 코드 리뷰 결론(id 작은 쪽)과 일치.
- **null totalPages 합산** ✅ — `total_pages` NULL 인 book(id 25)이 `totalPagesRead` 에 0으로
  더해짐. 세 기간 모두 (기존값 + 300 + 320 + 0) 으로 정확히 일치 → null-safe 확인.
- 분기 경계 / 연간 trance 6개월 고정 — 백엔드 코드 리뷰로 이미 확인됨(회신 §2).

### 갱신한 문서
- `docs/test/fixtures/dashboard-seed.sql` (헤더 주석 + 시드)
- `docs/test/fixtures/README.md`
- `docs/test/requests/대시보드_테스트데이터_전달_v1.md` (§4·§5)

커밋: `9faed6c [test] 대시보드 시드 엣지케이스 3권 추가 + 실측 검증`

---

## 2. 실기기 테스트 APK 재빌드 + 릴리스

### 배경
`d4ad47b [frontend]` 에서 `App.tsx` 의 소셜로그인 SDK 초기화
(`GoogleSignin.configure()` / `initializeNaverLogin()`)가 복구됨
— 이전에 테스터가 보고한 리그레션(`소셜로그인_App초기화_리그레션_수정요청_v1.md`) 반영.
→ 구글/네이버 로그인이 되는 APK 재빌드 필요.

### 한 일
1. `./gradlew assembleRelease` — BUILD SUCCESSFUL (1m 42s), 58.8MB (4 ABI).
2. arm64-v8a 외 네이티브 라이브러리 제거 → `zipalign -p 4` → `apksigner sign`(debug 키스토어).
   결과: **22.3MB**, arm64-v8a 전용.
3. 검증:
   - `apksigner verify` ✅
   - 서명 SHA-1 `5E:8F:...:F6:25` — 원본과 동일, 3사 콘솔 등록값과 일치.
   - JS 번들에 `GOOGLE_WEB_CLIENT_ID` / `initializeNaverLogin` 문자열 존재 → App.tsx 초기화 반영 확인.
   - `resources.arsc` 에 구글 클라이언트 ID(`853676691337-...apps.googleusercontent.com` web/android) 존재.

### 배포
GitHub Release (private repo, prerelease):
**https://github.com/electro2072/kongdakchaekdak/releases/tag/apk-2026-09-08**
- 태그 `apk-2026-09-08` → 커밋 `9faed6c`
- 자산 `kongdakchaekdak-arm64.apk` (22.3MB)

### 남은 확인 (실기기 필요 — 사용자/사람)
- 구글 로그인: 계정 선택 창 뜨고 로그인 완료되는지 (이전엔 "시도해주세요"만 뜨고 실패)
- 네이버 로그인: 이전엔 "SDK 재설치 필요" + 로딩바 멈춤.
  네이버 앱이 설치된 단말에서는 `@react-native-seoul/naver-login 5.0.1` 자체 한계로
  여전히 멈출 수 있음 → 프론트에 라이브러리 버전/패치 검토 요청 별건 유효.

---

## 부록 — 재현 명령

```bash
# 시드 검증
cd backend && ./gradlew bootRun --args='--spring.profiles.active=local'   # 8080
# h2-console(http://localhost:8080/h2-console)에서:
#   RUNSCRIPT FROM 'D:/projects/reading-record-app/docs/test/fixtures/dashboard-seed.sql'
node docs/test/tools/mint-jwt.mjs 9001 "$(grep '^JWT_SECRET=' backend/.env | cut -d= -f2-)"
curl -H "Authorization: Bearer <TOK>" http://localhost:8080/api/dashboard
curl -H "Authorization: Bearer <TOK>" "http://localhost:8080/api/dashboard?period=quarter"
curl -H "Authorization: Bearer <TOK>" "http://localhost:8080/api/dashboard?period=year"

# APK 슬림화
cd frontend/android && ./gradlew assembleRelease
# app/build/outputs/apk/release/app-release.apk 에서 lib/arm64-v8a 외 제거 →
# zipalign -p -f 4 → apksigner sign --ks app/debug.keystore (pass: android)
```
