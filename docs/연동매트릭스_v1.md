# 콩닥책닥 — FE↔BE 연동 매트릭스 v1

> 기준 커밋: `58fe310` (main, 2026-09-10) · 최초 작성 2026-09-08 (`b30c9e9` 기준)
> **공식 연동률: 8 / 41 (20%)** — 실기기 PASS 기준
> 목적: **"진행률"을 화면 개수나 커밋 수가 아니라 "실제로 서버와 붙어서 동작하는 기능 수"로 측정한다.**
> 이 문서가 모든 에이전트(디자인/프론트/백엔드/테스터/PM)의 공용 기준표다.

### 갱신 이력

| 일자 | 기준 커밋 | 반영 내용 |
|---|---|---|
| 2026-09-08 | `b30c9e9` | 최초 작성 |
| 2026-09-08 | `c11451c` | §2.2 백엔드 회신 반영 (interests 연동 완료 / 프로필 카운트 FE 계산) |
| 2026-09-09 | `259c495` | S0~S3 테스트 결과 반영. #5·#9·#13 승격(4→7). 상태 기호 🔵·🟠 신설. G3·G4·G5·G8 해소, G13~G16 신설. §2.5 스토리지 **Cloudflare R2** 결정 |
| 2026-09-09 | `259c495` | **PM 범위 결정 반영** — 모임 일정 ⛔제외 / 좋아요·반응 ⛔제외 / 알림 v1 포함(인앱만) / 푸시 v1.1 / 진행률 v1 포함. **분모 37 → 41.** D2·D3 확정, G17·G18 신설 |
| 2026-09-09 | `259c495` | **D5·D14 확정** — 그룹(F-08)·리캡(F-10) v1 포함 확정(기획서 10장 갱신 필요) / Apple 로그인 #42 v1 구현 / **iOS 출시는 Mac 확보 후로 연기, Android 선출시**. **분모 41 → 42** |
| 2026-09-10 | `58fe310` | **G13/BUG-23 종결** — 재현 실패, 인프라·코드 전수 확인 후 테스트 환경 문제로 판정. **#11 ✅ 승격(7→8)**. #42 Apple 로그인 BE+FE 구현 확인 🔴→🔵. **G9 원인 배제 + 만료일(09-28) 명시**. **G20 신설(서재 목록 조회 권한)** |
| 2026-09-10 | `58fe310` | **09-10 커밋 5건 반영** — ErrorCode 체계 개편(`a32d5e3`·`bc66694`), 비회원 둘러보기 폐기(`effc579`), UX 문구 리소스 분리(`25a5613`). **G21 신설(에러코드 FE 대응 누락)** |
| 2026-09-10 | (미커밋) | **G20 BE 작업 반영** — book·user·group 조회 권한 검증. **`GET /api/users`(#8) 삭제로 분모 42 → 41**, 연동률 8/41(20%) |

> **파일명은 `연동매트릭스_v1.md`로 고정한다.**
> 이전 판에 "분모가 바뀌면 v2로 판올림"이라 적었으나 **철회한다.** 분모 변경은 v1 범위가 확정된 것이지
> 제품이 v2가 된 게 아니다. 이 프로젝트의 v1/v2는 기획서·아키텍처문서와 같은 **제품 버전**이고 앱은 여전히 v1이다.
> 파일명을 바꾸면 프론트 소스 6개와 테스터 리포트 3건의 참조만 깨진다.
> **리비전은 위 "기준 커밋"으로 식별한다.** 문서 개명은 앱이 실제로 v2가 될 때만.

---

## 0. 한 줄 요약

| 지표 | 09-08 | 09-09 초 | 09-09 말 | **현재 (09-10)** |
|---|---|---|---|---|
| **v1 목표 엔드포인트 (분모)** | 37 | 37 | 42 | **41** (#8 삭제) |
| — 구현 완료 | 37 | 37 | 37 | **37** (#42 추가, #8 삭제) |
| — 미구현 (🔴) | 0 | 0 | 5 | **4** (#38·#39·#40·#41) |
| FE 코드가 호출하는 엔드포인트 | 3 | 19 | 19 | **20** |
| **실기기 PASS = 공식 연동률** | 4 / 37 | 7 / 37 | 7 / 42 | **8 / 41 (20%)** |
| 프론트 화면 | 14 | 15 | 15 | **15** (1개 삭제 예정 → 14) |
| 실데이터로 동작 (테스터 검증됨) | 1 | 5 | 5 | **6** |
| 화면에 mock이 뜨는 파일 | 7 | 4 | 4 | **4** (1개 삭제 예정 → 3) |

> ⚠️ **09-09에 연동률이 19% → 17%로 "떨어졌던" 이유(기록).** 후퇴한 게 아니라 **분모의 의미가 바뀌었다.**
> 이전 분모 37 = "BE가 이미 구현한 엔드포인트 수". 새 분모 42 = **"v1 출시에 필요한 엔드포인트 수"**.
> 범위 결정으로 알림 3개·진행률 1개·Apple 로그인 1개가 v1에 들어왔으므로 분모에 포함한다.
> 아직 안 만든 것을 분모에서 빼면 진행률이 실제보다 좋아 보인다. 그건 이 문서가 막으려는 바로 그것이다.
>
> ⚠️ **20 대 8 — 지금의 핵심은 여전히 이 격차다.**
> FE 호출 코드는 20개인데 실기기 검증은 8개다. 지금 필요한 건 새 연동 코드가 아니라 **검증과 버그 수정**이다.
> (분모 41은 v1 목표 기준이라 FE 호출 20개보다 크다 — 아직 FE가 안 붙은 그룹·공유 계열이 남아서다.)
> **공식 진행률은 8/41 하나뿐이다.** ✅ 개수(= A표 `실기기` 칸이 ✔인 행)를 센다.
> 🔵(코드는 있으나 미검증)는 카운트하지 않는다. 부분점수 없음.

---

## 1. 범례

| 기호 | 의미 |
|---|---|
| ✅ | BE 구현 + FE 연동 + 실기기 확인 완료 (**이것만 진행률에 카운트**) |
| 🔵 | BE 구현 + FE 호출 코드 있음, **실기기 미검증** (테스터 대기열) |
| 🟠 | FE 호출 코드 있으나 **실기기에서 FAIL** — 버그 ID 필수 기재 |
| 🟡 | BE 구현 완료, FE가 아직 호출 안 함 |
| 🔴 | BE 자체가 없음 (신규 개발 필요, v1 범위 확정) |
| ⛔ | v1 범위에서 제외 결정 |
| — | 해당 없음 |

**완료 판정 규칙:** BE·FE·테스트 셋 중 하나라도 안 되면 그 항목은 **0%다. 부분점수 없음.**

### 용어 주의

- **`S0`~`S10`은 §6 작업 슬라이스 번호 전용이다.** 오브젝트 스토리지는 항상 **`R2`**로 쓴다.
  (스토리지를 Cloudflare R2로 결정하면서 기존 "S3" 중의성은 대부분 해소됨 — §2.5)
- **엔드포인트 번호(#)는 추가 순서이며 재배열·재사용하지 않는다.** 테스터 리포트가 번호로 직접 참조 중이라
  중간에 끼워 넣거나 다시 매기면 과거 리포트가 전부 어긋난다. 그래서 진행률 API가 §2.3에 있으면서 #41이다.

---

## 2. A표 — 엔드포인트 × 연동 상태 (42개)

### 2.1 인증 / 헬스 (6)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 1 | GET | `/health` | — (운영) | ✔ | — | ✔ | ✅ |
| 2 | POST | `/api/auth/kakao` | LoginScreen | ✔ | ✔ | ✔ | ✅ |
| 3 | POST | `/api/auth/google` | LoginScreen | ✔ | ✔ | ✔ | ✅ |
| 4 | POST | `/api/auth/naver` | LoginScreen | ✔ | ✔ | ✔ | ✅ |
| 5 | GET | `/api/auth/me` | 앱 부팅 시 세션 복원 | ✔ | ✔ | ✔ | ✅ |
| **42** | POST | **`/api/auth/apple`** | LoginScreen | ✔ | ✔ | — | **🔵 (iOS 대기)** |

> ✅ **#5 승격 완료** (`14b7d5a` / `docs/test/reports/2026-09-09.md`).
> S1-1 PASS — 로그인 → `am force-stop` → 재실행 → logcat `[INFO][AuthContext] 저장된 세션 복원 완료` 2회 재현.
> 네트워크 폴백(`status:0`)이 아닌 진짜 200 복원임을 로그 라인으로 구분 확인.
> S1-5(만료 토큰 폐기)만 1시간 대기 불가로 미실시 — 유닛 테스트 `__tests__/AuthContext.test.tsx:126`이 커버.
> 구현은 AsyncStorage가 아니라 **`react-native-encrypted-storage`**(`df85ab8`). 네이티브 모듈이라 재빌드 필요.
>
> ⚠️ **#2·#4는 실기기 재확인 이력이 없다.** 검증된 소셜 제공자는 **Google(#3)뿐**이다.
> 카카오·네이버는 v1 최초 작성 이후 아무도 확인하지 않았다. 출시 전 3사 모두 1회씩 필요.
> (실계정이 필요해 사용자 동반 세션이어야 함)
>
> 📌 **비회원 둘러보기 폐기 (`effc579`, 2026-09-10).** LoginScreen에서 게스트 진입이 제거되어
> **모든 사용자가 소셜 로그인을 거친다.** 테스터 스모크에서 버튼 소멸 확인(`2026-09-10.md` §2).
> → 로그인 없이 접근 가능한 경로는 이제 `/public/share/{token}`(#37) 하나뿐이다.
>
> 🔵 **#42 Apple 로그인 — 2026-09-10 구현 완료(S10 종료).**
> 기획서 10.1이 *"Sign in with Apple — iOS 출시 시 필수(App Review Guideline 4.8)라 실질적으로 P0"* 로
> 적어뒀는데 09-09 시점에 코드 0건이라 A표에 등재했고, 하루 만에 BE·FE 양쪽이 들어왔다.
> - BE: `AppleOAuthClient` + `AppleOAuthProperties` + `AuthController#appleLogin` (`POST /api/auth/apple`)
>   + `SocialAuthService` 에 `APPLE_PROVIDER` 분기 + `AppleOAuthClientTest`
> - FE: `services/socialAuth/appleAuth.ts` + `@invertase/react-native-apple-authentication@2.5.0`
> - SecurityConfig `PUBLIC_PATHS` 에 `/api/auth/apple` 등재 확인
>
> **예상대로 기존 3사와 같은 패턴이라 반나절에 끝났다.** `SocialUserInfo` 추상화가 값을 했다.
>
> ⚠️ **Apple 개발자 계정($99, G7) 없이는 완성되지 않는다.** Service ID·Key를 그 계정 안에서 발급받아야
> `.env`에 넣을 값이 나온다. **코드는 지금 짜두되 실제 동작 검증은 iOS 착수 시점**이다.
> → **#42는 🔵(코드 있음, 미검증)까지만 가고 ✅는 iOS 시점이다.** "왜 계속 🔵냐"는 혼선을 막기 위해 명시해둔다.
>
> ⚠️ **Apple 고유 함정 — 최초 1회만 이름·이메일을 준다.**
> 재로그인 시에는 내려주지 않으며, 이메일 가리기(Private Relay)를 쓰면 `@privaterelay.appleid.com` 주소가 온다.
> 현재 `SocialAuthService`는 가입 시 닉네임 기본값을 이메일로 만드는데(테스터 계정 닉네임이
> `electro2072@gmail.com`이었던 이유), **Apple은 첫 응답을 놓치면 복구가 안 된다. 최초 응답을 반드시 저장할 것.**
>
> 📌 **대안(보류):** Apple 로그인을 iOS 착수 시점으로 통째로 미루는 것도 합리적이다.
> 어차피 iOS에서만 필수이고 검증도 그때 가능하다. 다만 지금 짜두면 나중에 로그인부터 다시 만들지 않아도 된다.
> **현재 결정은 "v1에 코드까지 구현"**이며, 라이브러리 버전 노후화가 우려되면 재검토한다.

### 2.2 사용자 / 프로필 (5)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 6 | POST | `/api/users` | SignupScreen | ✔ | ✖ | ✖ | 🟡 |
| 7 | GET | `/api/users/{id}` | — (현재 미사용) | ✔ | ✖ | ✖ | 🟡 **본인만** |
| 9 | PATCH | `/api/users/{id}` | ProfileEditScreen | ✔ | ✔ | ✔ | ✅ |
| 10 | DELETE | `/api/users/{id}` | (회원탈퇴 화면 없음 — G16) | **△** | ✖ | ✖ | 🟡 |

> ✅ **#9 승격 완료** (`328ed79` / `2026-09-09b.md`). 닉네임 `mario` 변경 → 재시작 유지.
> `mocks/profile.ts` 삭제됨(`b3fd618`), 프로필 통계도 실서버 값.
> ⚠️ **BUG-20260909-21(gender)은 open 유지** → G14. S2 완전 종료는 이 버그 수정 후.
>
> 🚨 **#10 BE 칸이 ✔가 아니라 △인 이유 — 껍데기만 있다.**
> ```java
> public void delete(Long id, Long currentUserId) {
>     requireOwner(id, currentUserId);   // 본인 확인은 있음
>     userRepository.delete(user);       // 유저 행 삭제 한 줄이 전부
> }
> ```
> `User`에 `@OneToMany`가 없고 `Book.user`는 cascade 없는 `@ManyToOne`이다.
> → **책이 1권이라도 있는 사용자에게 호출하면 FK 제약 위반으로 실패한다.**
> 그 외에 JWT 무효화·공개 공유링크 차단·접속로그 3개월 분리보관·소셜 연결해제가 전부 없다.
> **"BE 완료"로 읽으면 안 된다.** G16에서 재작성 대상.
>
> 📌 **#7 경로 정정.** ProfileScreen은 `/api/users/{id}`가 아니라 **`/api/auth/me`(#5)** 로 읽는다.
> v1 최초 작성의 "ProfileScreen → #7" 매핑은 오류였다. #7은 타인 프로필 조회용이며 현재 FE 미호출.
>
> 📌 **백엔드 회신 (`c11451c`) — 유효.**
> **① `interests`** — `UserUpdateRequest.interests`(`Set<Genre>`) 존재. `null`=변경없음, `[]`=전체해제.
> 값은 **한글 라벨 그대로**(`["소설","과학"]`). ⚠️ 실기기 왕복은 **미검증** — S2 재검증 시 gender와 함께 확인.
> **② `booksReadCount`/`sharedRecordsCount`** — FE 계산 확정, `services/profileStatsApi.ts` 구현 완료. 테스터 PASS.
> **③** FE 주석의 `GET /api/users/me`는 존재하지 않는다. 실제는 `GET /api/auth/me`.

### 2.3 책 / 서재 (7)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 11 | POST | `/api/books` | BookRegisterConfirmScreen | ✔ | ✔ | ✔ | **✅** |
| 12 | GET | `/api/books/{id}` | BookDetailScreen | ✔ | ✖ | ✖ | 🟡 |
| 13 | GET | `/api/books` | LibraryScreen | ✔ | ✔ | ✔ | ✅ |
| 14 | PUT | `/api/books/{id}` | BookDetail (총페이지 입력) | ✔ | ✖ | ✖ | 🟡 |
| 15 | PATCH | `/api/books/{id}/complete` | BookDetail (완독 버튼) | ✔ | ✔ | ✖ | 🔵 |
| 16 | DELETE | `/api/books/{id}` | BookDetailScreen | ✔ | ✔ | ✖ | 🔵 |
| **41** | **PATCH** | **`/api/books/{id}/progress`** | **BookDetail / 일정탭 "이어서 읽기"** | **✖** | **✖** | **✖** | **🔴 신규** |

> ✅ **#13 승격 완료** (`259c495` / `2026-09-09c.md`). 실서버 3권(아몬드·채식주의자·데미안) 로드·렌더.
> `LibraryContext`는 `useState([])` 시작이고 mock 폴백이 없으므로 실데이터 확정.
>
> ✅ **#11 승격 완료 (`58fe310` / `2026-09-10.md`). BUG-20260909-23은 종결.**
> 에뮬레이터(타임존 KST로 수정) + 사용자 실기기 양쪽에서 등록 → `am force-stop` → 재실행 → 목록 유지 확인.
> 2권(1984·사피엔스) 연속 등록 후 재시작에도 둘 다 유지.
>
> 🔍 **원인 조사 결과 — 인프라·코드 모두 무결. 테스트 환경 문제로 판정한다.**
> 09-09 시점에는 blocker로 봤으나, 후보를 하나씩 전수 배제했다. **재조사하지 않기 위해 근거를 남긴다.**
>
> | 후보 | 판정 | 근거 |
> |---|---|---|
> | Railway 크레딧 소진 (G9) | **배제** | 09-10 잔액 `$3.47 / $5`. 09-09에도 충분했다. 애초에 소진 증상은 "컨테이너 정지"라 API가 201을 주는 상황과 안 맞는다 |
> | MySQL 볼륨 미연결 → 재배포 시 초기화 | **배제** | 볼륨 정상 연결. Mount Path `/var/lib/mysql`, 사용량 ~150MB / 500MB. 재배포와 무관하게 유지됨 |
> | 서버 코드 유실 | **배제** | `BookService.create`는 `@Transactional` + `save()` 정상. 클래스 기본이 `readOnly=true`이고 쓰기 메서드에만 `@Transactional`이 붙어 있어 커밋된다 |
> | 에뮬레이터가 로컬 서버를 봄 | **배제** | APK `resources.arsc`/`dex`에 Railway URL만 존재. 로컬 8080 무응답 (`2026-09-10.md` §1①) |
> | **테스트 환경 (구 빌드/구 계정 상태)** | **유력** | 사용자 실기기는 처음부터 끝까지 정상 영속. 특정 에뮬레이터 세션에서만 발생 |
>
> ⚠️ **재발 시 첫 조치는 앱이 아니라 DB다.** `SELECT id, user_id, title, created_at FROM books ORDER BY id DESC LIMIT 20`
> 을 먼저 떠서 **"행이 없는지" / "다른 `user_id`로 있는지"** 를 구분할 것. 09-09 세션은 이걸 안 해서
> "사라졌다"와 "안 보인다"를 구분하지 못한 채 blocker로 올라갔다.
>
> 🔴 **#41 진행률 API 신설 (2026-09-09 PM 결정).**
> `Book`에 **`currentPage`(Integer, nullable)** 와 **`progressUpdatedAt`** 필드 추가 필요.
> `PUT /api/books/{id}`(#14)를 쓰지 않고 별도 엔드포인트를 두는 이유:
> - 진행률 갱신은 자주 일어나는 가벼운 동작인데 전체 PUT은 무겁다
> - `PATCH /{id}/complete`(#15)와 패턴이 맞는다 (상태 변경 전용)
> - `currentPage >= totalPages`일 때 자동 완독 처리 같은 규칙을 여기 담기 좋다
>
> **정책: 기능은 필수, 입력은 선택.**
> `totalPages`는 알라딘 응답에 항상 있지 않고, 필수 입력으로 막으면 책 등록 자체를 포기하게 된다.
> - `totalPages` 있으면 → `%` 표시
> - 없으면 → **"N일째 읽는 중"** 폴백 (`startDate` 기준)
> - `totalPages`는 나중에 #14로 채워 넣을 수 있게 한다 → 지금까지 미사용이던 #14에 존재 이유가 생김
>
> **`progressUpdatedAt`이 없으면** 일정 탭의 *"어제 62p까지 읽었어요"* 에서 "어제"를 계산할 수 없어
> 문구를 *"62p까지 읽었어요"* 로 줄여야 한다.
>
> 📌 **#12·#14는 `services/libraryApi.ts`에 함수 자체가 없다.**
> #14는 위 결정으로 용도가 생겼다. #12(단건 조회)는 목록 캐시로 대체 중 — 필요 여부 확정 필요(D7).
>
> 🔧 **G20 — 조회 권한 검증 (BUG-23 조사 중 발견, 2026-09-10). BE 완료 / FE 후속 대기**
>
> `POST`·`PUT`·`PATCH`·`DELETE`는 전부 `@AuthenticationPrincipal`로 소유자를 검증하는데
> **조회 계열만 클라이언트가 보낸 값을 그대로 신뢰**하고 있었다. 3개 도메인에서 같은 패턴이 나왔다.
>
> | 엔드포인트 | 수정 전 | 조치 |
> |---|---|---|
> | `GET /api/books` (#13) | `?userId=3` → 3번 회원 서재 / 파라미터 없으면 `findAll()` 전체 덤프 | `@AuthenticationPrincipal`로 교체, `findAll()` 분기 제거 |
> | **`GET /api/users` (#8)** | **전 회원 명부**(닉네임·프로필·자기소개·**성별**·소셜제공자·가입일) | **엔드포인트 삭제** (FE 호출부 0건 확인) |
> | `GET /api/users/{id}` (#7) | id 순회로 임의 회원 조회 | 본인 아니면 `NOT_OWNER` 403 |
> | `GET /api/groups` (#28) | 전체 그룹 목록 | 본인 소속 그룹만 반환 |
> | `GET /api/groups/{id}` (#27), `/{id}/members` (#31) | 비멤버도 조회 | 비멤버면 `NOT_GROUP_MEMBER` 403 |
>
> ⚠️ **"조회는 열어둔다(공유 앱 특성)"의 범위를 넘는 건이었다.** 그 원칙은 *특정 책의 소감/사진*을
> 공개하자는 것이었고(`BookNoteService` 주석), 위 항목들은 **공개 범위를 `비공개`로 고른 책과
> 성별을 포함한 회원 명부까지 인증만으로 유출**되는 문제였다. `/public/**`(#37)은 정책상 의도된
> 비로그인 경로이므로 건드리지 않았다.
>
> 📌 **#8 삭제로 분모가 42 → 41이 됐다.** 관리용으로 만들어졌고 FE가 쓰지 않아 되살릴 이유가 없다.
> 운영 조회가 필요해지면 API가 아니라 DB나 별도 관리도구로 간다.
>
> ⏭ **FE 후속 (G20-FE):** `services/libraryApi.ts`의 `fetchBooks`에서 `?userId=${userId}` 제거.
> `status`는 유지. 그룹 API는 아직 전부 mock이라 FE 영향 없음.
>
> ⚠️ **#7을 본인 전용으로 잠근 부작용을 S6에서 확인할 것.** 그룹 멤버 프로필을 화면에 띄우려면
> 타인 조회가 필요해진다. `GET /api/groups/{id}/members`(#31)의 `GroupMemberResponse`에
> 닉네임·프로필이미지가 이미 실려 있으면 #7 없이 해결되고, 아니면 #31 응답을 넓히는 쪽이
> #7을 다시 여는 것보다 안전하다.

### 2.4 소감 (BookNote) (4)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 17 | POST | `/api/books/{bookId}/notes` | BookDetail / BookNoteEdit | ✔ | ✔ | ✖ | 🔵 |
| 18 | GET | `/api/books/{bookId}/notes` | BookDetailScreen | ✔ | ✔ | ✖ | 🔵 |
| 19 | PATCH | `/api/books/{bookId}/notes/{noteId}` | BookNoteEditScreen | ✔ | ✔ | ✖ | 🔵 |
| 20 | DELETE | `/api/books/{bookId}/notes/{noteId}` | BookDetailScreen | ✔ | ✔ | ✖ | 🔵 |

> `LibraryContext`가 4종 모두 호출한다. #18은 S3 세션에서 200(빈 목록)까지 확인됐으나
> **소감 작성·수정 왕복은 미검증** → S5에서 판정.

### 2.5 사진 (BookPhoto / 오브젝트 스토리지) (4)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 21 | POST | `/api/books/{bookId}/photos/presigned-url` | BookDetail (사진 추가) | ✔ | ✔ | ✖ | 🔵 |
| 22 | POST | `/api/books/{bookId}/photos` | BookDetailScreen | ✔ | ✔ | ✖ | 🔵 |
| 23 | GET | `/api/books/{bookId}/photos` | BookDetail (갤러리) | ✔ | ✔ | ✖ | 🔵 |
| 24 | DELETE | `/api/books/{bookId}/photos/{photoId}` | BookDetailScreen | ✔ | ✔ | ✖ | 🔵 |

> 📌 **스토리지 결정 (2026-09-09): AWS S3 → Cloudflare R2.**
> R2는 **S3 호환 API**라 기존 코드를 그대로 쓴다. 필요한 변경은 엔드포인트 주입뿐:
> - `config/S3Config.java` — `S3Presigner.builder()`에 `.endpointOverride(URI.create(...))` 추가
> - `config/S3Properties.java` — `String endpoint` 필드 추가
> - `application.yml` — `app.s3.endpoint: ${AWS_S3_ENDPOINT:}` 추가
> - `.env` — `AWS_S3_ENDPOINT` + R2 자격증명 (기존 변수명 3종 재사용)
> - `BookPhotoService`·`BookPhotoController`·FE `libraryApi.uploadPhoto` **수정 불필요**
>
> **선택 이유:** 무료 한도 10GB/월(12개월 체험이 아니라 매월 갱신), 전송량 요금 0원, 카드 등록 불필요.
> 예상 사용량(사용자 100명 × 20권 × 사진 3장 × 500KB ≈ 3GB)이 무료 한도 안에 들어온다.
>
> 💡 **로컬 개발:** 같은 `endpointOverride`로 **MinIO**(docker-compose)를 붙이면 클라우드 계정 없이 개발 가능.
> `application-local.yml`에서 `endpoint: http://localhost:9000`.
>
> ⚠️ **읽기 경로는 동작 정황이 있다.** S3 세션에서 "아몬드"가 사진 3장과 함께 실서버에서 로드됐고
> BookDetail 장소 사진 섹션이 정상 렌더됐다. 다만 **업로드 왕복은 미검증** → S5.
> ⚠️ `S3Config`는 `@Lazy`라 자격증명이 없어도 서버는 뜬다(BUG-20260827-03 대응).
> **"서버가 떴다"가 "스토리지가 설정됐다"를 뜻하지 않는다.**

### 2.6 대시보드 (1)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 25 | GET | `/api/dashboard?period=&date=` | DashboardScreen / **일정 탭 헤더** | ✔ | ✔ | ✖ | 🔵 |

> ✔ BE는 tie-break·null 합산 엣지케이스까지 테스터 실측 PASS (2026-09-08).
> ✔ FE 연동 완료 — `hooks/useDashboard.ts`의 `fetchDashboard`가 실호출로 교체됨. `mocks/dashboard.ts`는 미사용.
> ⏸ 실기기 판정은 **S3 선행 대기** — 완료 조건이 "등록한 책이 대시보드 숫자에 반영"인데 BUG-23으로 검증 불가.
> 📌 **일정 탭 실데이터화(S7)에서도 이 API를 쓴다** — "이번달 · 지금까지 N권 완독" 헤더.
>
> ✅ **D14 결정: 리캡(F-10, 대시보드)은 v1 포함.** 기획서 10.2가 "v1.1"로 적어뒀으나
> **BE 완성 + FE 연동 완료 + 백엔드 로직 테스터 검증(2026-09-08 PASS)까지 이미 끝난 상태**다.
> 빼려면 만들어둔 것을 되돌려 숨겨야 하므로 그게 더 일이다. 기획서 10장을 갱신한다.

### 2.7 그룹 (8)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 26 | POST | `/api/groups` | GroupManagementScreen | ✔ | ✖ | ✖ | 🟡 |
| 27 | GET | `/api/groups/{id}` | GroupManagementScreen | ✔ | ✖ | ✖ | 🟡 |
| 28 | GET | `/api/groups` | ShareScreen (공유 대상 선택) | ✔ | ✖ | ✖ | 🟡 |
| 29 | PATCH | `/api/groups/{id}` | GroupManagementScreen | ✔ | ✖ | ✖ | 🟡 |
| 30 | DELETE | `/api/groups/{id}` | GroupManagementScreen | ✔ | ✖ | ✖ | 🟡 |
| 31 | GET | `/api/groups/{id}/members` | GroupManagementScreen | ✔ | ✖ | ✖ | 🟡 |
| 32 | POST | `/api/groups/{id}/members` | SignupScreen (독서모임 추가) | ✔ | ✖ | ✖ | 🟡 |
| 33 | DELETE | `/api/groups/{id}/members/{userId}` | GroupManagementScreen | ✔ | ✖ | ✖ | 🟡 |

> ✅ **D14 결정 (2026-09-09): 그룹(F-08)은 v1 포함.**
> 기획서 10.1은 *"이번 버전 제외(P1 이후): F-08 그룹 관리"* 라고 적혀 있으나 **그 문장은 2026-08-31 이후 갱신되지 않았다.**
> 그 사이 백엔드가 8개를 전부 구현했고, 그룹을 빼면 세 가지가 함께 무너진다:
> - 공유 시 "누구에게"를 고를 수 없다 → **전체 공개(`all`)만 가능**
> - 회원가입의 "소속 독서모임 추가" 단계 소멸
> - **알림 2종 중 "새 멤버가 참여했어요"가 사라져 v1 알림이 월간 리캡 1종만 남는다** → 알림함의 의미가 없어짐
>
> 무엇보다 이 앱의 취지가 *"독서 모임에서 소감을 공유하되 모임 멤버에게만"*(기획서 페르소나 B)이라
> 그룹을 빼면 컨셉 자체가 약해진다. **→ 기획서 10.1·10.2를 이 결정에 맞춰 갱신할 것.**
>
> 그룹 도메인은 **FE 착수 전**이다. `GroupManagementScreen`이 여전히 mock, `SignupScreen`에 API 호출 0건.
> S6에서 일괄 처리. 📌 **#32는 알림 "새 멤버가 참여했어요"의 발생원이므로 S9(알림)의 선행이다.**

### 2.8 공유 (4)

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| 34 | POST | `/api/share-records` | ShareScreen | ✔ | ✖ | ✖ | 🟡 |
| 35 | GET | `/api/share-records` | ProfileScreen (통계) / GroupManagement (이력) | ✔ | ✔ | ✔? | **🔵 판정 요청** |
| 36 | DELETE | `/api/share-records/{id}` | GroupManagementScreen | ✔ | ✖ | ✖ | 🟡 |
| 37 | GET | `/public/share/{token}` | (웹, Thymeleaf 서버렌더링) | ✔ | — | ✖ | 🟡 |

> ❓ **#35 판정 요청 — 테스터 확인 필요.**
> `profileStatsApi.ts`가 실호출하고, 2026-09-09b 리포트에서 프로필 "공유한 기록 **0**"이 이 호출 결과로 PASS 판정됐다.
> 다만 테스터가 승격을 명시 요청하지 않았고, 값이 0이라 "빈 배열 응답만 확인된 것 아니냐"는 여지가 있다.
> **→ 다음 세션에서 공유 기록 1건 이상 있는 상태로 재확인 후 판정.**
> (§7 규칙상 ✅ 승격 권한은 테스터에게만 있으므로 PM이 임의 승격하지 않는다.)
>
> ⛔ **"좋아요 / 반응" 기능은 v1 범위에서 제외 결정 (2026-09-09).**
> 알림 4종 중 "공유한 기록에 반응이 있어요"가 여기 걸려 있었으나 함께 제외.
> 기획서·API 어디에도 없던 기능이라 알림 1종만을 위해 신설할 이유가 없다.
>
> #37은 RN 작업 대상 아님. **비로그인 브라우저 + 카카오 링크 디버거 OG 미리보기 실측이 아직 안 됐다.**

### 2.9 알림 (3) — 신규, v1 범위

| # | Method | Path | 사용 화면 | BE | FE 연동 | 실기기 | 상태 |
|---|---|---|---|---|---|---|---|
| **38** | GET | `/api/notifications` | NotificationScreen | ✖ | ✖ | ✖ | **🔴 신규** |
| **39** | PATCH | `/api/notifications/{id}/read` | NotificationScreen | ✖ | ✖ | ✖ | **🔴 신규** |
| **40** | GET | `/api/notifications/unread-count` | 일정 탭 종 아이콘 배지 | ✖ | ✖ | ✖ | **🔴 신규** |

> 📌 **PM 결정 (2026-09-09): 알림은 v1 포함, 단 인앱만. 푸시는 v1.1.**
>
> **알림 종류 4종 → 2종 + 리마인더 1종으로 축소:**
>
> | 알림 | 상태 | 발생원 |
> |---|---|---|
> | 새 멤버가 참여했어요 | ✅ 유지 | `POST /api/groups/{id}/members`(#32) — **S6 선행** |
> | 월간 리캡이 도착했어요 | ✅ 유지 | 매월 1일 스케줄러 (`@Scheduled` 또는 Railway cron) |
> | ~~공유한 기록에 반응이 있어요~~ | ⛔ 제외 | 좋아요 기능 자체를 안 만들기로 결정 |
> | ~~이번주 독서모임 일정 알림~~ | ⛔ 제외 | 모임 일정 도메인 제외 결정 |
> | 오랜만이에요 (N일 미접속) | **이미 동작** | `/api/auth/me`의 `daysSinceLastLogin` — 신규 API 0 |
>
> ⚠️ **알림은 저절로 생기지 않는다.** 각 도메인 서비스에 알림 발행 훅을 심어야 한다.
> 지금 당장 울릴 수 있는 이벤트는 **0개다** — 그룹(S6)이 동작해야 "새 멤버", 스케줄러가 있어야 "월간 리캡".
> **그래서 S9(알림)은 S6 이후다.** 지금 만들면 빈 알림함이 된다.
>
> **엔티티 설계 시 v1.1 대비:**
> `Notification`에 **`targetType`/`targetId`** 를 반드시 넣을 것. 알림 탭 시 이동할 대상이며,
> v1.1 푸시 payload에 그대로 재사용된다. 나중에 추가하면 마이그레이션이 생긴다.
> 프로필 설정에 **알림 on/off 토글 자리**도 미리 잡아둘 것 — v1.1 수신 동의 UI에서 화면을 다시 짜지 않아도 된다.
>
> **v1.1로 미룬 것:** Firebase 세팅, `DeviceToken` 테이블 + 등록/해제 API 2개, 네이티브 재빌드,
> `POST_NOTIFICATIONS` 권한(targetSdk 34라 런타임 권한 필수), 포그라운드/백그라운드/종료 3상태 수신,
> APNs 인증키(**Apple 개발자 계정 $99 필요 — G7과 커플링**).
>
> ⚠️ **푸시를 빼면 "오랜만이에요" 리마인더는 앱을 열어야만 보인다.**
> 안 여는 사람을 부르는 게 목적인 알림인데 v1에서는 그 기능을 못 한다. 인앱 배너로만 남는다.
> **v1의 알림함은 "월간 리캡이 가끔 쌓이는 곳" 수준일 것으로 예상하고 진행한다.**
>
> ⚠️ **v1.1 푸시 착수 시 법률 확인 필요.** "오늘 한 페이지 읽어볼까요?" 류 리마인더가
> 서비스 알림인지 광고성 정보인지 경계에 있다. 광고성이면 별도 수신 동의 + 야간(21시~08시) 발송 추가 동의가
> 필요하다(정보통신망법). **발송 시간대 낮 고정 + 설정 on/off 토글**이 안전한 기본값. 애매하면 동의 UI를 넣는 쪽으로.
> (법률 자문 영역이며 이 문서가 단정하지 않는다.)

---

## 3. B표 — 화면 × 데이터 출처 (15개 → 14개 예정)

| 화면 | 현재 데이터 출처 | 필요 API (#) | 상태 |
|---|---|---|---|
| LoginScreen | **실서버** | 2,3,4,42 | ✅ (Google만 검증 · #42는 🔵 iOS 대기) |
| SignupScreen | 로컬 state | 6, 32 | 🟡 |
| ScheduleScreen (일정 탭) | **하드코딩 + 실데이터 혼재** | 13, 25, 41 | 🟡 **재정의됨** |
| ~~ScheduleDetailScreen~~ | `MOCK_MEETING_DETAIL` | — | ⛔ **삭제 대상** |
| NotificationScreen | `MOCK_NOTIFICATIONS` | 38, 39 | 🔴 |
| LibraryScreen (서재 탭) | **실서버** | 13 | ✅ |
| BookSearchScreen | **실서버** (알라딘/카카오) | — | ✅ |
| BookRegisterConfirmScreen | `LibraryContext` (API) | 11 | ✅ |
| BookDetailScreen | `LibraryContext` (API) | 12,14,15,16,17~24,**41** | 🔵 |
| BookNoteEditScreen | `LibraryContext` (API) | 17, 19 | 🔵 |
| ShareScreen (공유 탭) | `LibraryContext` + `MOCK_SHARE_GROUPS` | 13, 28, 34 | 🟡 |
| GroupManagementScreen | `MOCK_SHARE_GROUPS` + `MOCK_SHARE_HISTORY` | 26~33, 35, 36 | 🟡 |
| ProfileScreen | **실서버** (`/api/auth/me` + 통계) | 5, 13, 35 | ✅ |
| ProfileEditScreen | **실서버** | 9 | ✅ (gender 제외 — G14) |
| DashboardScreen | **실서버** | 25 | 🔵 |

### 📌 ScheduleScreen 재정의 (2026-09-09 PM 결정)

**이 화면은 앱의 첫 착지 탭이다** (기획서 F-06: "하단 탭 일정 — 앱 기본 진입 탭").
통째로 없앨 수 없다 — **"새 책 등록하기" 진입점이 여기 있다.**

| 요소 | 현재 | 결정 |
|---|---|---|
| "이번달 독서 일정 · 지금까지 3권 완독" | `COMPLETED_THIS_MONTH = 3` 하드코딩 | → **#25 대시보드로 실데이터화** |
| `MONTH_LABEL = '2026년 7월'` | 하드코딩 (**현재 9월 — 두 달 전 날짜를 보여주는 중**) | → 실제 날짜 계산 |
| 종 아이콘 → 알림 | mock | **v1 유지** (S9에서 #38·#40 연동) |
| 미접속 배너 | **실데이터** (`daysSinceLastLogin`) | 그대로 |
| 주간 캘린더 스트립 | `WEEK_DAYS` 하드코딩 | → `today`는 날짜 계산, **`meeting` 상태 삭제**. 책 `startDate`/`endDate` 표시로 전환 |
| "오늘은 『아몬드』…62p·62%" | 하드코딩 | → **#13 + #41로 실데이터화**. `totalPages` 없으면 "N일째 읽는 중" |
| "이어서 읽기" 버튼 | `onPress` 없음 (동작 안 함) | → **#41 페이지 입력 다이얼로그 연결** (`PromptDialog.tsx` 재사용) |
| "이번주 독서모임 일정이 있어요" 카드 | mock | ⛔ **삭제** |
| "새 책 등록하기" | **동작함** | 그대로 |

> **"이어서 읽기"에 입력을 붙이는 이유:** 서재 → 상세 진입 → 입력 → 저장은 3탭이다.
> 첫 화면에서 1탭이어야 사람들이 실제로 쓴다.

### ⛔ v1 범위 제외 확정 (2026-09-09)

| 영역 | 결정 | 근거 |
|---|---|---|
| **독서모임 일정 (Schedule / Meeting)** | ⛔ **제외** | *"사용자들이 독서모임 일정을 이 앱에서 등록하진 않을 것. 캘린더 앱이 따로 있으니까."* → **신규 BE 도메인 0개.** 기획서 F-06 미확정 사항("OS 캘린더 연동인지 앱 자체인지")도 자동 해소 |
| **좋아요 / 반응** | ⛔ **제외** | 기획서·API 어디에도 없던 기능. 알림 1종만을 위해 신설할 이유 없음 |
| **푸시 알림** | **v1.1 연기** | §2.9 참고 |
| **iOS 출시** | **Mac 확보 후로 연기** | Mac이 없어 `pod install`·빌드·검증이 불가능하다. `encrypted-storage`·Apple 로그인·향후 Firebase가 전부 네이티브 모듈이라 Mac 없이는 iOS 빌드가 나오지 않는다. **Android 선출시.** Apple 로그인(#42) 코드는 v1에 구현해두어 나중에 다시 만들지 않는다 |

> **"독서 일정"은 이미 데이터가 있다.** `Book.startDate` / `Book.endDate`가 저장·표시되고 있고
> 테스터가 BookDetail "읽은 기간 `2026.09.08 ~ 진행중`" 렌더를 확인했다.
> 사용자 요구("내가 이 책을 언제부터 읽었더라")는 **신규 백엔드 작업 0개로 충족된다.**
>
> 🔴 **남은 BE 미구현 영역은 알림(§2.9)과 진행률(#41) 둘뿐이다.** 일정 도메인은 이 표에서 삭제됐다.

---

## 4. C표 — 릴리스 게이트

| # | 항목 | 담당 | 상태 |
|---|---|---|---|
| G1 | **릴리스 keystore 생성 + `build.gradle` 적용** — `android/app/build.gradle:101`이 아직 `signingConfigs.debug` → 스토어 업로드 불가 | 사용자 | ✖ |
| G2 | **Cloudflare R2 버킷 + API 토큰 → `backend/.env`** + `endpointOverride` 코드 적용 (§2.5) | 사용자 + BE | ✖ |
| G3 | ~~세션 영속화~~ | FE | ✅ |
| G4 | ~~`MainStack` 3화면 등록~~ | FE | ✅ |
| G5 | ~~이용약관~~ | 사용자 | ✅ (`TERMS.md`) |
| G6 | Google Play 콘솔 계정 ($25) | 사용자 | ✖ |
| G7 | Apple 개발자 계정 ($99) — **D5 결정: iOS 출시를 Mac 확보 후로 연기 → 이 게이트도 함께 보류.** #42 Apple 로그인 검증·APNs 발급의 선행 | 사용자 | ⏸ **보류** |
| G8 | ~~스토어 스크린샷~~ | 디자인 | ✅ (아래 근거) |
| G9 | **Railway 트라이얼 만료 `2026-09-28` — 그 전에 Hobby($5/월) 전환 필요.** 잔액 `$3.47 / $5`(09-10). **G13 원인 아님 — 배제 완료**(§2.3) | 사용자 | ✖ **시한 D-18** |
| G10 | BUG-20260901-17 서재 검색창 한글 자모 분해 (간헐) | FE | ✖ |
| G11 | 개인정보처리방침 | — | ✅ (`PRIVACY.md`) |
| G12 | 백엔드 배포 (Railway 라이브) | — | ✅ |
| ~~G13~~ | ~~BUG-20260909-23 등록한 책 미영속~~ — **종결.** 재현 실패 + 인프라·코드 전수 배제, 테스트 환경 문제로 판정 (§2.3) | — | ✅ **닫힘** |
| G14 | **BUG-20260909-21 — gender enum 불일치.** → **D2: FE가 BE 값에 맞춘다** (아래) | FE | ✖ major |
| G15 | **OBS-20260909-22 — 닉네임 규칙 불일치.** BE `@Size(max=30)`만 / FE `2~10자 + 한글영숫자`. 21자 닉네임이 편집화면에서 저장만 눌러도 10자로 잘림 | 기획 결정 (D4) | ✖ |
| G16 | **회원 탈퇴 (P0)** — Apple 5.1.1(v) 필수. → **D3: `#10` 재사용, 신규 엔드포인트 없음** (아래) | 기획+디자인+FE+BE | ✖ **P0** |
| **G17** | **enum 직렬화 규칙 통일** — `Gender`·`BookStatus`에 `@JsonValue toLowerCase()`, `BookController.search`의 `toUpperCase()` 임시코드 제거, FE 대응. **선행: G13** | BE + FE | ✖ |
| **G20** | **조회 권한 검증** — book·user·group 3개 도메인. **BE 완료(미커밋), FE 후속 1줄 대기** (아래) | BE ✔ / FE | ◐ **FE 대기** |
| **G21** | **에러코드 체계 FE 대응** — BE는 `message: null` + `error` 코드만 내려보내는데 FE `apiClient`는 아직 `body.message`만 읽는다. 매핑 0건 (아래) | FE | ✖ **major** |
| **G18** | **API 계약 단일화** — `/v3/api-docs` → `docs/api/openapi.json` 커밋 + `openapi-typescript`로 FE 응답 타입 자동 생성 | BE + FE | ✖ |

### 📌 G8 판정 근거 (2026-09-09, Apple 공식 스펙 확인)

출처: `https://developer.apple.com/help/app-store-connect/reference/app-information/screenshot-specifications`
- 6.5" 항목의 요구사항은 **"6.9" 스크린샷이 제공되지 않은 경우에만 필수"** 이고, 미제공 시 6.9"를 축소해 사용.
  → **6.9" 단독 제출로 충분.** 6.3"·6.1"·5.5"·4.7"도 모두 상위 클래스에서 축소되는 구조.
- 6.9" 허용 세로 크기: **1260×2736 / 1290×2796 / 1320×2868**. 보유분 1320×2868이 정확히 일치.
  (Apple은 픽셀 단위 일치를 요구하므로 리사이즈 금지)
- Google Play는 짧은 변 1080px 이상이면 되므로 1080×2340도 충족.

⚠️ **iOS 출시 시점에 확인할 것(D5로 보류됨):** 13" iPad(2064×2752 / 2048×2732)는 **앱이 iPad에서 실행되면 별도 필수**다.
iPhone 세트로 대신할 수 없다. iPad를 지원 기기에서 빼거나 세트를 추가해야 한다.
**Android 선출시 결정(D5)으로 이 항목은 v1 Android 출시를 막지 않는다.**

### 📌 G14 / D2 — gender는 FE가 BE에 맞춘다

**단순 대소문자 문제가 아니다.** 세 번째 값은 단어가 다르다.

| FE | BE |
|---|---|
| `male` | `MALE` — 대소문자만 |
| `female` | `FEMALE` — 대소문자만 |
| `unspecified` | **`NONE`** — 단어가 다름 |

→ `toUpperCase()`만 붙이면 `UNSPECIFIED`가 되어 여전히 깨진다.

**FE가 맞추는 이유:**
1. `User.gender`가 `@Enumerated(EnumType.STRING)`이라 DB에 `MALE`/`FEMALE`/`NONE`이 **이미 저장돼 있다.** BE 변경 = 데이터 마이그레이션
2. `@Column(length = 10)`인데 `UNSPECIFIED`는 11자 → DDL 변경까지 필요
3. FE는 상수 파일 하나. 저장된 데이터도 스키마도 없다

**수정 범위: `services/userApi.ts` 한 파일.**
`GENDER_OPTIONS`의 키(`unspecified`)는 **그대로 둔다** — 서버 명명이 UI 코드로 새면 안 된다. 변환은 API 경계에서.
현재 인바운드 매핑(`mapGenderResponseToKey`)은 값이 틀렸고 **아웃바운드 매핑은 아예 없다**(`JSON.stringify(fields)` 그대로 전송). 양방향 다 필요.

⚠️ **`unspecified`는 `null`이 아니라 `"NONE"`으로 보낼 것.** `interests`와 동일한 규약(`null` = 변경 없음)이
gender에도 적용되므로, `null`을 보내면 "성별을 안 바꾸겠다"가 된다 → **사용자가 "선택 안 함"으로 되돌릴 수 없다.**
⚠️ **BE 응답이 `null`인 경우도 있다.** `gender` 컬럼은 nullable이라 미저장 사용자는 `null`. `NONE`과 구분할 것.

**BE는 건드리지 않는다.** 백엔드 세션 프롬프트에 "`Gender.java` 수정 금지"를 명시할 것.
G17에서 소문자 통일 시 이 매핑 테이블은 값만 바꾸면 되고(3줄), 통일 후에는 사실상 불필요해진다.

**테스터 재검증 조건:**
1. 성별 "남성" 선택 → 저장 → 재시작 → 유지
2. **다시 "선택 안 함"으로 변경 → 저장 → 재시작 → 유지** ← `NONE` 왕복 확인. **2번이 핵심**
3. logcat `알 수 없는 gender 값` 경고 0건

### 📌 G16 / D3 — `#10 DELETE /api/users/{id}` 재사용

**신설(`DELETE /api/users/me`) 대신 재사용으로 확정. 분모 변화 없음.**
당초 "신설이 구조적으로 안전"이라 판단했으나, `UserService.delete`에 이미 `requireOwner`가 있어
본인 확인이 되고 있었다. 보안 차이가 없으므로 거의 같은 엔드포인트를 늘릴 이유가 없다.

**단, 어느 쪽을 골라도 `UserService.delete`는 재작성해야 한다.** 현재 빠진 것:
1. **연관 데이터 처리 없음** — `User`에 `@OneToMany`가 없고 `Book.user`는 cascade 없는 `@ManyToOne`.
   → **책이 1권이라도 있으면 FK 제약 위반으로 실패한다.**
2. **JWT 무효화 없음** — 탈퇴 후에도 기존 토큰이 만료 전(1시간)까지 살아 있다 (수용기준 4)
3. **공개 공유 링크 차단 없음** — `ACCOUNT_DELETION.md`에 *"이미 생성된 공개 공유 링크도 접근할 수 없게 됩니다"* 로 **이미 공지됨** (수용기준 3)
4. **접속 로그 3개월 분리 보관 없음** — 통신비밀보호법, `PRIVACY.md`에 명시됨
5. **소셜 연결 해제 없음** — 카카오 `unlink` / 구글 `revokeAccess` 등 (FE 몫)
6. **R2 사진 파일 삭제 없음** — DB 행이 지워져도 오브젝트는 남는다

🚨 **배포 문서와 앱이 어긋나 있다.** `ACCOUNT_DELETION.md`는 "프로필 → 설정 → 회원 탈퇴"를 1순위 경로로
안내하는데 그 화면이 존재하지 않는다. 심사 리젝 + 개인정보 고지 위반 양쪽 리스크.

📌 **`ACCOUNT_DELETION.md` 삭제 목록 보강 필요.** 현재 "알림 내역"과 푸시 관련 항목이 없다.
v1.1 푸시에서 또 고치지 않도록 **지금 한 번에** *"알림 내역 및 알림 발송을 위한 기기 정보"* 로 포괄해둘 것.

📌 요청서 §6은 같은 유형("문서엔 있는데 앱엔 없는 심사 필수 기능")으로 **게시물 신고·사용자 차단**
(약관 10조, Apple UGC 1.2)도 지목한다. 별도 요청서 접수 시 G19로 등재 (D12).

### 📌 G17 배경 — BE 내부부터 규칙이 갈려 있다

경계를 넘는 enum 8개의 직렬화 규칙이 **3가지**다.

| 나가는 형태 | enum | 방식 |
|---|---|---|
| **소문자** | `ShareScope` · `SharePlatform` · `ShareTargetType` · `ShareType` · `DashboardPeriod` | `@JsonValue toLowerCase()` |
| **대문자** | `Gender` · `BookStatus` | `@JsonValue` 없음 |
| **한글 라벨** | `Genre` | `label` 필드 |

`gender`는 "`@JsonValue`를 안 붙인 쪽"이었을 뿐이고, FE는 소문자로 나가는 다수를 보고 추측했다가 틀렸다.
`BookStatus`는 더 나가서 **요청은 소문자, 응답은 대문자**다 — `BookController.search`에 `status.toUpperCase()` 임시코드가 있다.

**목표: 소문자로 통일.** 이미 5:2로 다수이고 `@JsonValue`를 2곳에 붙이면 끝난다.
**`Genre`는 예외로 유지한다** — 한글 라벨이 UI에 그대로 뿌려지고, DB에도 `GenreConverter`로 한글이 저장돼 있으며,
FE `INTEREST_OPTIONS`와 `genreColors.ts`가 한글 키로 색을 고정 매핑 중이다. 바꾸면 마이그레이션까지 번지고 얻는 게 없다.

⚠️ **G13보다 먼저 하면 안 된다.** `BookStatus`를 소문자로 바꾸면 이미 **정확히** 짜인 FE 코드가 깨진다:
```ts
export type BookStatusResponse = 'READING' | 'DONE';
status: book.status === 'DONE' ? 'done' : 'reading',
```
`libraryApi.ts`·`LibraryContext.tsx`는 지금 G13 조사 대상이다. 같은 파일을 건드리면
"버그가 원래 있던 건지 방금 만든 건지" 구분이 안 된다.

**필드 이름(카멜케이스)은 이미 통일돼 있다.** Jackson 기본값이라 저절로 맞았다. 정리 불필요.

### 📌 G21 배경 — 에러 계약이 BE만 바뀌었다 (2026-09-10)

`a32d5e3`·`bc66694`로 백엔드 에러 체계가 개편됐다. **설계 자체는 옳다** — 코드 15종을 `ErrorCode` enum
하나로 모으고, 각 예외가 코드를 들고 다니며, 도메인 서비스 9곳이 전부 새 코드로 이관됐다.
`NOT_OWNER`·`NOT_GROUP_MEMBER`·`GROUP_LEADER_CANNOT_LEAVE`처럼 **프론트가 분기할 수 있는 입도**로 쪼갠 것도 좋다.

**문제는 이 개편이 FE/BE 계약을 바꿨는데 FE가 안 따라왔다는 것이다.**

| | 개편 전 | 개편 후 |
|---|---|---|
| 응답 | `{"message": "본인 책 기록에만 …"}` | `{"status":403, "error":"NOT_OWNER", "fieldErrors":[]}` |
| 사용자 문구 | **BE가 한글로 내려줌** | **FE가 `error` 코드로 자체 매핑** (설계 원칙) |

`ErrorResponse` Javadoc이 명시한다 — *"`message`는 원칙적으로 `null`이다."*

그런데 `services/apiClient.ts`는 여전히 이렇다:
```ts
let message = `요청 실패 (${response.status})`;
if (body && typeof body.message === 'string') { message = body.message; }
```

**`body.error`를 읽는 코드가 없고, 코드→문구 매핑도 0건이다**(`grep NOT_OWNER frontend/src` → 없음).
`message`가 항상 `null`이므로 **모든 에러가 `요청 실패 (403)` 같은 기본 문구로 떨어진다.**

⚠️ **파급 3가지**
1. **`fieldErrors`가 완전히 버려진다.** 필드별 검증 사유를 내려보내는데 화면에 도달하지 않는다.
2. **`INTERNAL_SERVER_ERROR`는 `message`에 traceId가 실린다.** 지금 FE 로직이면 그 traceId가 **사용자에게 에러 문구로 그대로 노출된다.** 의도는 "오류 번호로 안내"였으나 지금은 날것의 문자열이다.
3. **화면 단 문구가 이를 가린다.** `ko.ts`의 `completeFailure` 같은 화면별 폴백이 있어 *일부* 화면은 멀쩡해 보인다. → **테스터가 놓치기 쉬운 버그다.** 폴백이 없는 경로부터 샌다.

🚨 **`docs/콩닥책닥_에러코드체계_설계_v1.md`가 저장소에 없다.** `ErrorCode`·`ErrorResponse` Javadoc이
이 문서를 계약의 근거(§3 원칙 3)로 두 번 인용하는데 커밋되지 않았다. **G18이 막으려던 바로 그 상황
— "확인할 곳이 없어서 추측한다" — 이 다시 만들어졌다.** 문서를 커밋하거나, 인용을 지우고
`ErrorCode` Javadoc을 단일 출처로 확정할 것.

📌 **G18과 함께 처리하면 싸다.** `/v3/api-docs`를 뽑을 때 `ErrorCode` 15종을 `@Schema(allowableValues=...)`로
같이 내보내면, FE 매핑 테이블을 손으로 유지하지 않아도 된다.

### 📌 G18 배경 — 계약의 단일 출처가 없다

`build.gradle`에 **springdoc-openapi가 이미 들어 있고**(`2.5.0`), 컨트롤러 8개 전부에 `@Operation`이 붙어 있으며
`application.yml`에 `/swagger-ui.html` 경로까지 설정돼 있다. **서버를 띄우면 지금도 `/v3/api-docs`에서
실제 코드에서 생성된 스펙이 나온다.** "인터페이스 파일"은 이미 존재하며, 꺼내오기만 하면 된다.

FE가 소통을 안 한 게 아니다. `libraryApi.ts`의 `BookStatus`는 **제대로 맞춰놨다.**
문제는 `userApi.ts`의 이 주석이다:
> *"백엔드 gender 값 형식이 재확인되기 전까지는 알려진 3개 키만 신뢰하고..."*

**확인할 곳이 없어서 주석에 유보를 달고 추측한 채 배포한 것이다.** 한 번 맞춰도 단일 출처가 없으면 또 어긋난다.

⚠️ **springdoc이 `@JsonValue`를 항상 완벽히 반영하지는 않는다.** 스펙을 받은 뒤 위 enum 8개의 값이
실제 응답과 맞는지 눈으로 대조할 것. 안 맞으면 `@Schema(allowableValues=...)`로 명시.

---

## 5. 열린 버그 / 관찰

| ID | 심각도 | 내용 | 담당 | 상태 |
|---|---|---|---|---|
| ~~BUG-20260909-23~~ | ~~blocker~~ | 등록한 책 미영속 — **closed (2026-09-10).** 재현 실패, 원인=테스트 환경 추정. 인프라·코드 배제 근거는 §2.3 | — | **closed** |
| **BUG-20260910-25** | **major(보안)** | 조회 권한 미검증 — books·users·groups (G20). **BE 수정 완료(미커밋), FE 후속 대기** | BE ✔ / FE | ◐ |
| **BUG-20260910-27** | **major** | 에러 문구가 전부 `요청 실패 (403)` 류로 표시됨. BE 개편(`bc66694`) 후 FE 미대응 (G21) | FE | open |
| BUG-20260909-21 | major | gender enum 불일치 (G14, D2 결정됨) | FE | open |
| BUG-20260901-17 | minor | 서재 검색창 한글 자모 분해 (간헐) | FE | open |
| OBS-20260909-22 | 정보 | 닉네임 규칙 FE/BE 불일치 (G15) | 기획 | open |
| OBS-20260909-20 | 정보 | 로그아웃에 확인 다이얼로그 없음 (1탭 즉시 실행) | 디자인 | open |
| OBS-20260909-24 | 환경 | 에뮬레이터 시각 하루 느림. `startDate`가 기기 로컬 시각(`toIsoDate(new Date())`)에 노출되는 구조는 검토 여지 | — | 참고 |
| **OBS-20260910-26** | **minor** | 완독일 < 시작일 역전 (`2026.09.09 ~ 2026.09.08`, 0일). 서버 UTC 기준 완독 처리 추정. **S4 대시보드가 `endDate` 기간 집계라 착수 전 정리 필요** | BE | open |
| FINDING-20260908-18 | 정보 | S0 3화면 자동화 렌더 테스트 없음 | FE | open |
| FINDING-20260908-19 | minor | release cleartext 예외 없음 | FE | open |

> **테스트 계정 상태(사용자 확인 필요):** `electro2072@gmail.com` 닉네임이 **`mario`** 로 변경됨
> (한글 adb 입력 불가로 fallback). FE 편집 규칙(10자·특수문자 금지)으로 앱에서 원복 불가.
> BUG-23 검증 중 "1984"를 4~5회 등록 시도했으므로 DB 잔여물 가능 — G13 조사 시 함께 정리.

---

## 6. 작업 순서 (세로 슬라이스)

> 계층별(FE/BE/디자인)로 나누지 말고 **기능 단위로 묶어서** 지시할 것.
> 각 슬라이스는 `BE 확정 → FE 연동 → 테스터 실기기 확인`이 **한 세트로 끝나야 완료.**

| 순서 | 슬라이스 | 대상 # | 완료 조건 (테스터 판정) | 상태 |
|---|---|---|---|---|
| S0 | `MainStack` 3화면 등록 | G4 | 3개 진입점 눌러서 안 죽음 | **✅ 완료** (`f47daf6`) |
| S1 | 공용 `apiFetch` + 토큰 저장/복원 | 5 | 로그인 → 강제종료 → 재실행 → 유지 | **✅ 완료** (`14b7d5a`) |
| S2 | 프로필 | 7, 9 | 닉네임 바꾸고 재시작 → 유지 | **🟠 §5 PASS, 미종료** — G14 수정 후 재검증 (+interests 왕복 확인) |
| S3 | 서재 | 11, 12, 13, 15 | 책 등록 → 재시작 → 목록에 남아 있음 | **✅ 완료** (`58fe310`) — #11·#13 승격. #12·#15 미검증 잔여 |
| S4 | 대시보드 | 25 | 등록한 책이 대시보드 숫자에 반영 | **▶ 착수 가능** (S3 해제). 단 OBS-26 선행 |
| S5 | 소감 + 사진 | 17~24 | 사진 업로드 → R2 왕복 → 다른 기기에서도 보임 | ⏸ **G2 선행** (FE 코드 완료) |
| S6 | 공유 + 그룹 | 26~36 | 공유 생성 → 비로그인 브라우저에서 링크 열림 | ⏸ FE 미착수 |
| **S7** | **일정 탭 실데이터화 + 진행률** *(재정의)* | 13, 25, **41** | 페이지 입력 → 재시작 → 진행률 유지. 일정 탭에 실제 이번달·실제 읽는 책 표시 | ✖ **BE 1개 신규** |
| **S8** | **회원 탈퇴** *(신규)* | 10 | 탈퇴 → 재로그인 시 신규가입 플로우 + 기존 JWT 인증 실패 + 공유링크 접근 불가 | ✖ **P0** (G16) |
| **S9** | **알림 (인앱)** *(재정의)* | 38, 39, 40 | 그룹에 멤버 추가 → 알림함에 뜸 → 탭하면 해당 화면 이동 | ✖ **S6 선행** |
| **S10** | **Apple 로그인** *(신규)* | **42** | 코드 구현까지 v1. **실기기 검증은 iOS 착수 시점(G7 보류)** | **✅ 코드 완료** (`58fe310`) — 🔵 유지 |
| ~~구 S7 일정/알림~~ | — | — | — | ⛔ **폐기** — 모임 일정 제외, 알림은 S9로 분리 |

> ~~S1이 안 되면 S2~S6은 전부 무의미하다.~~ → **S1 완료.**
> ~~지금 병목은 G13이다.~~ → **G13 종결 (2026-09-10). 쓰기 경로 정상 확인.**
>
> **G13이 풀리면서 S3가 닫혔고 S4·S5의 선행 조건이 해제됐다.** 다만 **S5는 여전히 G2(R2 버킷) 대기**다.
> FE는 S3~S5 연동 코드를 이미 다 깔아놨으므로(🔵 12개) **지금도 새 기능보다 검증·수정이 우선이다.**
>
> **권장 실행 순서 (2026-09-10 기준):**
> 1. ~~BE — G20 조회 권한~~ → **BE 완료.** `./gradlew test` 통과 후 커밋. **FE 후속 1줄이 남았다**(2번에 포함)
> 2. **FE — G21 에러코드 매핑** (`bc66694`로 계약이 이미 바뀌어 **지금 프로덕션 APK의 에러 문구가 깨져 있다.** 되돌릴 수 없으니 FE가 따라가야 한다)
> 3. **BE — OBS-26 완독일 타임존** (S4 착수 전 필수. 대시보드가 `endDate` 기간 집계라, 안 고치면 "숫자가 안 맞네"로 보이고 원인이 안 보인다)
> 4. **FE — G14 gender 매핑** (2번과 같은 세션에서. 둘 다 `services/` 계층이라 묶으면 싸다)
> 5. **디자인 — G16 탈퇴 진입점·다이얼로그** (병렬. `color_chips.html` v2.3 재사용이라 가벼움)
> 6. 테스터 — **S4 대시보드** 검증 (1·3번 반영 후). 그전에 **#2·#4 소셜 로그인 검증**(사용자 동반) + #35 재판정 가능
> 7. 그 뒤 → **G17 enum 통일 + G18 계약 단일화** (묶어서 단독 세션. G13이 닫혀 선행 조건 해제됨)
>
> ⚠️ **FE에게 새 연동(S6)을 먼저 시키지 말 것.** 지금 20 대 8인 격차가 26 대 8로 벌어질 뿐
> 공식 진행률은 오르지 않는다. FE는 버그 수정과 검증 지원에 붙인다.
>
> 📅 **G9 — 서버는 `2026-09-28`에 멈춘다.** 트라이얼은 *$5 소진 또는 30일 중 먼저 오는 쪽*에서 끝나고,
> 잔액은 남아도(`$3.47`) **30일 만료가 먼저 온다.** 이후 Free 플랜은 월 $1 / 0.5GB RAM 한 replica라
> Spring Boot + MySQL 2개 서비스를 못 얹는다(현재 소비율 약 $0.13/일 = 월 $3.8).
> **출시를 그 전에 끝내거나, Hobby로 전환하거나 둘 중 하나다. 결정을 미루면 자동으로 전자가 실패한다.**

---

## 7. 갱신 규칙

- **PM 에이전트**: 스크럼마다 A표의 `FE 연동` / `실기기` 칸을 갱신하고, **연동률(x/42)을 스크럼 맨 위에 한 줄로 적는다.** 이 숫자가 유일한 공식 진행률이다.
- **PM 에이전트**: 테스터 리포트의 **§H "A표 갱신 요청"** 을 확인하는 즉시 반영하고, 반영 커밋 해시를 헤더 갱신 이력에 적는다.
  **테스터 리포트 커밋과 매트릭스 갱신 커밋이 2커밋 이상 벌어지면 안 된다.**
  *(2026-09-08~09에 이 규칙이 없어 리포트 4건이 쌓이는 동안 매트릭스가 방치됐다.)*
- **PM 에이전트**: 문서 상단 "기준 커밋"은 **갱신 직전 HEAD**로 잡는다.
  *(v1 최초 작성 시 12시간 전 커밋을 기준으로 잡아 G5·G8이 작성 시점부터 이미 낡아 있었다.)*
- **분모(42) 변경은 PM 결정으로만.** 범위가 바뀌면 갱신 이력에 결정 사유를 남긴다.
  **분모가 바뀌어도 파일명은 바꾸지 않는다** (헤더 참고).
- **엔드포인트 번호는 재사용·재배열하지 않는다.** 추가는 뒤에 붙인다.
- **프론트 에이전트**: 호출 코드를 넣으면 🟡 → **🔵**로 올린다. ✅는 아니다. **✅ 승격 권한은 테스터에게만 있다.**
- **테스터 에이전트**: 실기기 확인 전에는 ✅로 바꾸지 않는다. 승격 요청은 리포트 §H에
  `#번호 / 현재→요청 / 사유` 형식으로. FAIL이면 🟠로 내리고 **버그 ID를 반드시 기재한다.**
- **계약 (신설, G18):**
  - BE가 DTO·enum·경로를 바꾸면 **같은 커밋에서 `docs/api/openapi.json`을 갱신한다.**
  - FE는 손으로 쓴 응답 타입을 새로 만들지 않는다. **생성된 타입만 쓴다.**
  - **확인이 안 된 값은 추측해서 넘어가지 않는다** — 주석에 유보를 다는 대신 `docs/test/requests/`에 요청서를 올린다.
    *(gender 버그가 정확히 여기서 나왔다.)*
- **모든 에이전트**: 오브젝트 스토리지는 `R2`. `S0`~`S10`은 슬라이스 번호 전용. (§1 용어 주의)
- **`src/mocks/`에서 화면에 렌더되는 파일이 하나 줄 때마다 진짜 진행률이 오른 것이다.**
  단, **타입만 남은 파일은 진행률에 영향 없다.**

---

## 부록: 현재 `src/mocks/` 잔존 (6파일 / 실사용 4 → 목표 0)

| 파일 | 실태 | 대체 |
|---|---|---|
| ~~`profile.ts`~~ | **삭제 완료** (`b3fd618` → `types/profile.ts`) | #5, #13, #35 |
| `dashboard.ts` | ☠️ **죽은 코드** — 아무 데서도 import 안 함. **바로 삭제 가능** | #25 (연동 완료) |
| `libraryBooks.ts` | ⚠️ **반쯤 죽음** — `MOCK_LIBRARY_BOOKS` 미사용. 타입만 5개 파일이 참조. **`types/library.ts`로 옮기고 삭제 권장** | #13 (연동 완료) |
| `meetingDetail.ts` | ⛔ **삭제 대상** — 모임 일정 v1 제외 결정 | — |
| `shareGroups.ts` | 🔴 사용 중 — ShareScreen, GroupManagementScreen | #28 (S6) |
| `shareHistory.ts` | 🔴 사용 중 — GroupManagementScreen | #35 (S6) |
| `notifications.ts` | 🔴 사용 중 — NotificationScreen | #38 (S9) |

> **남은 실사용 mock 3개는 두 덩어리로 정리된다:** 공유·그룹(S6) + 알림(S9).
> 모임 일정(`meetingDetail.ts`)은 범위 제외로 그냥 삭제된다.
> `dashboard.ts`·`libraryBooks.ts` 정리는 언제든 가능한 청소 작업이며 진행률과 무관하다.
