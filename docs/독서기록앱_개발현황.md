# 📚 독서 기록 공유 앱 — 개발 현황

> 작성일: 2026-07-02 · 최종 업데이트: 2026-07-20 · 프로젝트 지식(Project Knowledge) 참고용 요약

## 1. 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | React Native (TypeScript) |
| 언어 | TypeScript (vanilla, 별도 상태관리 라이브러리 미사용) |
| 도서 검색 API | 알라딘(Aladin) Open API, 카카오(Kakao) 도서 검색 API — 2개 동시 사용 |
| 아키텍처 패턴 | Provider 패턴 — 도서 API를 공통 인터페이스(`BookSearchProvider`)로 추상화 |

## 2. 현재까지 만든 것

**화면설계서 Frame 03(독서 기록 작성)의 요소 1 "책 검색/선택" 컴포넌트**만 구현된 상태입니다. Frame 03의 나머지 요소(별점, 리뷰 입력, 태그, 공개범위+등록)는 아직 없습니다.

### 구현된 컴포넌트 구조

```
BookSearchScreen (화면 루트)
 ├─ SearchBar          : 검색어 입력창 + 검색 버튼
 ├─ BookList           : FlatList 목록, 로딩/빈 상태 처리
 │   └─ BookListItem   : 도서 1권 카드 (표지·제목·저자·출판사)
 └─ useBookSearch (커스텀 훅) : 검색 상태 관리, API 결과를 화면에 연결
      └─ BookService   : 여러 도서 API를 등록/선택해서 호출하는 오케스트레이터
           ├─ AladinApi (BookSearchProvider 구현)
           └─ KakaoApi  (BookSearchProvider 구현)
```

### 파일 구조

```
src/
 ├─ types/
 │   ├─ bookProviderId.ts   # API 제공자 enum (Aladin / Kakao)
 │   ├─ book.ts             # 앱 전체가 공유하는 도메인 타입 Book
 │   └─ bookProvider.ts     # 모든 도서 API가 구현해야 하는 공통 인터페이스
 ├─ services/
 │   ├─ bookService.ts      # provider 등록/선택 오케스트레이터
 │   ├─ aladin/
 │   │   ├─ aladinApi.ts        # 알라딘 fetch 호출 + 도메인 타입 변환
 │   │   └─ aladinApi.types.ts  # 알라딘 원본 응답 타입 (이 API 전용)
 │   └─ kakao/
 │       ├─ kakaoApi.ts         # 카카오 fetch 호출 + 도메인 타입 변환
 │       └─ kakaoApi.types.ts   # 카카오 원본 응답 타입 (이 API 전용)
 ├─ hooks/
 │   └─ useBookSearch.ts    # 검색 상태(books/isLoading/error) 관리 훅
 ├─ components/
 │   ├─ SearchBar.tsx
 │   ├─ BookList.tsx
 │   └─ BookListItem.tsx
 └─ screens/
     └─ BookSearchScreen.tsx
```

### 설계 원칙
- **UI 컴포넌트는 API를 모른다**: `SearchBar`/`BookList`/`BookListItem`은 props만 받고, 네트워크 호출은 전혀 하지 않음
- **API별 원본 타입은 격리**: 알라딘/카카오 각각의 원본 응답 형태(`AladinRawItem`, `KakaoRawDocument`)는 해당 API 폴더 안에서만 쓰이고, 밖으로 나갈 때는 공통 `Book` 타입으로 변환됨
- **API 추가/교체가 쉬움**: 새 도서 API를 붙이려면 `BookSearchProvider` 인터페이스를 구현하는 클래스 하나만 추가하고 `bookService.ts`에 등록하면 됨 (화면/훅 코드 수정 불필요)
- 현재 두 API 모두 등록되어 있고, `search(query, providerId)` 호출 시 `providerId`를 지정해 특정 API를 선택하거나, 지정 안 하면 알라딘이 기본값

## 3. 아직 안 만든 것 (TODO)

### 화면 (기획서 4장 / 화면설계서 기준)
- [ ] Frame 01 로그인/온보딩 (소셜 로그인: 네이버/카카오/구글)
- [ ] Frame 01.1 회원가입 (닉네임 필수 입력 + 성별/관심분야 다중선택/독서모임 검색·연결 선택 입력)
- [ ] Frame 02 홈 (이번달 독서 일정, 캘린더 스트립, 오늘의 리딩 카드)
- [ ] Frame 03 독서 기록 작성 — 책 검색(완료) 외 별점/리뷰/태그/공개범위+등록 UI
- [ ] Frame 04 독서 기록 상세보기
- [ ] Frame 05 마이페이지(독서 대시보드/Recap) — 도넛 차트, 막대그래프, 카드 이미지 내보내기
- [ ] 공유 탭 (그룹 관리, 공유 범위 설정, 공유 이력)
- [ ] 외부 SNS 공유 연동 (인스타그램/스레드/틱톡)

### 백엔드 / 인프라
- [ ] 백엔드 서버 및 DB (테이블정의서 기준: User, Book, BookPhoto, BookNote, Group, GroupMember, ShareRecord, ShareRecordTarget)
- [ ] 소셜 로그인 인증 연동
- [ ] 이미지 저장소 (S3 / Firebase Storage)
- [ ] 알라딘/카카오 API 키 발급 및 환경변수 설정 (`.env`, 현재 코드에는 빈 값으로 자리만 있음)

### 기타
- [ ] 두 API 결과 병합/중복 제거 로직 (필요 시 `bookService.ts`에서 처리)
- [ ] 상태관리 라이브러리 도입 여부 결정 (현재는 컴포넌트별 로컬 상태 + 커스텀 훅만 사용)

## 4. 참고 문서
- 기획서: `독서기록앱_기획서_v2.md`
- 테이블 정의서: `독서기록앱_테이블정의서-1.xlsx`
- 화면설계서(와이어프레임): `독서기록공유앱_화면설계서_wireframe_v2.html`

## 5. 백엔드 진행 상황 (2026-07-19 기준)

### Step 1. 프로젝트 뼈대 — 완료
- Spring Boot 3.2.5 + Gradle(Kotlin DSL) + Java 17 뼈대 생성
- `application.yml` (MySQL, Docker Compose) + `application-local.yml` (H2, Docker 없이 로컬 실행 확인용) 프로필 분리
- `GET /health` 헬스체크 API

### Step 2. User CRUD — 오늘(2026-07-19) 구현 완료 (Book CRUD는 미착수)
- `domain/user` 패키지: `User` 엔티티, `UserRepository`, `UserService`, `UserController`
- API: `POST /api/users`, `GET /api/users`, `GET /api/users/{id}`, `PATCH /api/users/{id}`, `DELETE /api/users/{id}`
- `common/exception`에 `GlobalExceptionHandler` 추가 — 404(리소스 없음)/400(검증 실패)을 공통 에러 응답 포맷으로 변환
- `config/OpenApiConfig` 추가로 Swagger(`/swagger-ui.html`) 문서에 API 정보 노출
- `UserControllerTest` (MockMvc + H2, `src/test/resources/application.yml`로 테스트 전용 DB 분리) — 생성/조회/수정/삭제/검증 실패 시나리오 커버

**이번 세션에서 판단·결정한 사항 (무인 실행이라 스스로 결정):**
1. 테이블정의서상 `User` 테이블명을 그대로 쓰지 않고 엔티티 `@Table(name = "users")`로 매핑함 — `USER`가 MySQL 예약어라 충돌 방지 목적.
2. 테이블정의서상 `social_provider`/`social_id`는 NOT NULL이지만, Step 3(소셜 로그인)이 아직 없어 지금은 nullable로 완화함. Step 3에서 OAuth2 붙일 때 다시 NOT NULL + unique 제약을 검토해야 함 (코드에 TODO 주석 남김).
3. 닉네임 미입력 시 테이블정의서 설명("미설정 시 랜덤 닉네임 부여")대로 랜덤 닉네임(`책벌레1234` 형태)을 자동 부여하도록 구현.
4. `docs/독서기록앱_백엔드구축계획.md`의 Step 2 체크박스가 실제 코드보다 앞서 모두 체크되어 있던 오류를 바로잡음 (User는 체크, Book은 아직 미구현이므로 체크 해제).

### 검증
- **Gradle 빌드/테스트는 이번 세션에서 실행하지 못함** — 이 샌드박스의 아웃바운드 네트워크가 Maven Central/Gradle Plugin Portal에 접근할 수 없어(proxy 403), `gradle test`가 스프링부트 플러그인조차 받아오지 못하고 실패함. 코드는 작성 후 정적으로 재검토(임포트/시그니처/괄호 짝 확인)만 수행함. **다음 세션에서 Maven Central 접근이 가능한 환경(또는 로컬 개발 환경)에서 `./gradlew test` 실행 확인이 필요함.**
- 프론트엔드는 이번 세션에서 변경하지 않음 (`npm run typecheck` 실행 대상 없음).

### 다음 우선순위 제안
1. ~~위 Gradle 테스트를 실제로 실행해 통과 확인~~ → 아래 6번에서 로컬 실행/검증 완료
2. ~~Book Entity/Repository/Service/Controller~~ → 아래 6번에서 구현 완료
3. 프론트 Frame 03 나머지 요소(별점/리뷰/태그/공개범위+등록 UI)

## 6. Book CRUD 구현 + 로컬 실행 검증 (2026-07-19, 사용자와 실시간 페어 세션)

### Step 2-b. Book CRUD — 완료
- `domain/book` 패키지: `Book` 엔티티(User와 동일하게 `@CreatedDate`/`@LastModifiedDate` + `AuditingEntityListener` 컨벤션 사용), `BookStatus`(READING/DONE) + `BookStatusConverter`(테이블정의서상 소문자 'reading'/'done' 저장과 매핑), `BookRepository`, `BookService`, `BookController`
- API: `POST /api/books`, `GET /api/books?userId=&status=`, `GET /api/books/{id}`, `PUT /api/books/{id}`, `PATCH /api/books/{id}/complete`, `DELETE /api/books/{id}`
- `BookControllerTest` 추가 (User CRUD 테스트와 동일한 MockMvc + H2 방식)
- `status` 쿼리 파라미터는 API 설계 초안(`status=reading`, 소문자)에 맞춰 문자열로 받아 대소문자 무관하게 enum으로 변환

### 로컬 실행 검증 완료
- 사용자 컴퓨터(Windows)에 Java 17(Temurin) + Gradle 8.14를 새로 설치
- `gradle bootRun --args="--spring.profiles.active=local"` 로 Docker 없이 H2 인메모리 DB로 실행 → `http://localhost:8080/health` 정상 응답(`{"status":"ok",...}`) 확인
- Sourcetree로 GitHub 저장소를 로컬(`D:\projects\reading-record-app`)에 clone하여 앞으로 pull/push로 계속 동기화하기로 함

### ⚠️ 협업 관련 중요 메모 (다음 세션이 꼭 읽어야 함)
이 프로젝트에는 **매일 새벽 6시(KST)에 자동으로 TODO를 골라 구현하고 push하는 예약 세션("독서 자랑 어플 일일 개발")**이 별도로 돌아가고 있습니다 (사용자가 설정). 오늘(2026-07-19) 그 자동 세션이 먼저 User CRUD(Step 2-a)를 구현해서 push했고, 그 뒤 사용자와의 실시간 세션에서 독립적으로 Book CRUD를 이어서 구현하다가 커밋이 갈라지는 걸 뒤늦게 발견했습니다. 다행히 서로 다른 영역(User vs Book)이라 큰 충돌 없이 정리했지만, **앞으로 작업을 시작하기 전에는 항상 `git fetch && git log origin/main` 등으로 최신 커밋을 먼저 확인**해서, 이미 완료된 작업을 중복 구현하지 않도록 주의해야 합니다. 이 파일의 "다음 우선순위 제안" 섹션이 최신 상태인지도 항상 먼저 확인하세요.

### 다음 우선순위 제안 (갱신)
1. 프론트 Frame 03 나머지 요소(별점/리뷰/태그/공개범위+등록 UI)
2. Step 3. 인증 (이메일/PW → 카카오 → 구글 → 네이버 OAuth2)
3. 프론트 `BookSearchScreen` → 백엔드 `POST /api/books` 실제 연동 테스트

## 7. 프론트엔드 네이티브 스캐폴드 생성 (2026-07-19)

기존 `frontend/`에는 `src/` 로직 레이어만 있고 실제로 실행 가능한 React Native 프로젝트(android/ios 폴더)가 없었다. 사용자가 Android Studio를 설치한 김에 `npx react-native init`(0.74.0)으로 실제 android/ios 네이티브 프로젝트를 생성하고, 기존 `src/`를 그대로 이식해서 `App.tsx`가 `BookSearchScreen`을 렌더링하도록 연결했다.

- 이 세션(클라우드 샌드박스)에서 `npm install`, `npm run typecheck`, `npx jest` 모두 통과 확인함 (npm 레지스트리는 이 샌드박스에서도 접근 가능했음)
- 다만 실제 Android 빌드/에뮬레이터 실행은 Gradle의 Android용 의존성(Google Maven 등)이 필요해서 이 샌드박스에서는 확인 못 함 — 사용자 로컬(Android Studio + 에뮬레이터)에서 확인 필요했음

### ✅ 로컬 Android 빌드/실행 검증 완료 (2026-07-19)
사용자 컴퓨터에 Android Studio 설치 + 에뮬레이터(AVD) 생성 후 `npm run android`로 실제 빌드/설치/실행까지 확인함.
- 트러블슈팅: Windows Defender가 Gradle 캐시 이동을 막아서(`AccessDeniedException`) 프로젝트 폴더를 예외 처리로 등록해 해결, `local.properties`에 `sdk.dir` 설정, `platform-tools`를 PATH에 추가해서 `adb` 인식되게 해결
- 에뮬레이터에서 책 검색 화면(`BookSearchScreen`)이 정상 렌더링되는 것을 스크린샷으로 확인함 (검색창/버튼/빈 상태 문구 모두 의도대로 표시). 알라딘/카카오 API 키가 아직 없어 실제 검색 결과는 비어있는 게 정상.
- 이걸로 백엔드(Step 1~2, gradle test 통과)에 이어 프론트엔드도 로컬 환경에서 end-to-end 실행 검증 완료.

### ⚠️ 프론트엔드 작업 일시 중단 (2026-07-19)
사용자 로컬 디스크에서 `src/navigation/`, `AuthContext.tsx`, `RootNavigator.tsx` 등 이 세션이 작성하지 않은 네비게이션/인증 화면 코드가 발견됨 (git에는 없고 로컬에만 존재, `react-native-screens` 등 미설치 패키지를 참조해 Metro 에러 유발). 다른 자동 세션(매일 새벽 6시 예약 작업으로 추정)이 동시에 프론트엔드를 건드리고 있을 가능성이 있어, 사용자가 **"frontend 작업을 중단하고 backend 작업을 하라"**고 명시적으로 지시함. 이후 세션은 **사용자가 별도로 재개를 요청하기 전까지 `frontend/` 디렉터리를 건드리지 않을 것**. (미해결 이슈로 남겨둠 — 다음에 프론트 작업 재개 시 먼저 이 충돌 원인을 확인할 것.)

## 8. Step 3 인증 — 이메일/PW 로그인 (2026-07-19, 사용자 지시로 백엔드 전환 후 구현)

### 구현 내용
- `domain/user/User.java`: `passwordHash` 컬럼 추가 + `forLocalSignup(nickname, email, encodedPassword)` 팩토리 메서드. 이메일/PW 로그인은 별도 `email` 컬럼을 새로 만들지 않고 기존 `socialProvider="local"`, `socialId=이메일`로 저장(테이블정의서 변경 최소화 목적).
- `domain/user/UserRepository`: `findBySocialProviderAndSocialId(String, String)` 추가.
- `User` 테이블에 `(social_provider, social_id)` unique 제약 추가 — 이메일 중복 가입을 애플리케이션 레벨 체크뿐 아니라 DB 레벨에서도 방지 (동시 가입 요청에 대한 최후 방어선).
- `security` 패키지(신규): `JwtProperties`(app.jwt.secret/expiration-seconds 바인딩), `JwtProvider`(jjwt 0.12.5 발급/검증), `JwtAuthenticationFilter`(Authorization: Bearer 헤더 → SecurityContext), `JwtAuthenticationEntryPoint`(인증 실패 시 다른 API와 동일한 `ErrorResponse` JSON 포맷으로 401 응답), `SecurityConfig`(현재는 `/api/auth/me`만 인증 필요, 나머지 전부 permitAll — Step 2 CRUD 테스트/화면이 인증 없이 계속 동작하도록 하기 위한 임시 구성).
- `domain/auth` 패키지(신규): `AuthController`(`POST /api/auth/signup`, `POST /api/auth/login`, `GET /api/auth/me`), `AuthService`, DTO `SignupRequest`/`LoginRequest`/`TokenResponse`.
- `common/exception`에 `InvalidCredentialsException`(401), `DuplicateResourceException`(409) 추가.
- 로그인 시 이메일 미가입/비밀번호 불일치 두 경우 모두 bcrypt 연산을 동일하게 수행해(더미 해시 비교) 응답 시간으로 가입 여부를 추측하지 못하게 처리.
- `build.gradle.kts`에 `spring-boot-starter-security`, `jjwt-api/impl/jackson:0.12.5`, `spring-security-test` 추가.
- `application.yml` / `src/test/resources/application.yml`에 `app.jwt.secret`(환경변수 `JWT_SECRET`로 교체 가능) / `app.jwt.expiration-seconds` 추가.
- `AuthControllerTest` 추가 — 회원가입→내정보조회→로그인 흐름, 잘못된 비밀번호 401, 이메일 중복 409, 토큰 없이 `/me` 401, 짧은 비밀번호 400 시나리오 커버.

### 이번 세션에서 판단·결정한 사항
1. 카카오 등 실제 소셜 로그인은 아직 없어 이메일/PW 인증 구조부터 검증(백엔드구축계획 Step 3 순서 그대로).
2. 접근 제어는 아직 `/api/auth/me` 하나만 인증 필요 — Book/User CRUD를 포함한 나머지 API는 이번 단계에서는 그대로 permitAll 유지 (소유자 검증은 이후 단계에서 강화 예정, 코드에 근거 주석 남김).
3. h2-console(local 프로필)이 프레임을 쓰기 때문에 `frameOptions`는 완전 비활성화 대신 `sameOrigin`으로 완화 (클릭재킹 방어는 유지).

### 검증
- **이번 세션도 Gradle 빌드/테스트를 실행하지 못함** — 동일한 이유로 샌드박스에서 Gradle Plugin Portal 접근이 막혀 있음 (재확인함, 여전히 차단). 코드는 작성 후 세컨드 오피니언(리뷰 서브에이전트)으로 한 차례 더 정적 검토했고, 발견된 이슈(프레임 옵션 과다 완화, 인증 실패 응답 포맷 불일치, 이메일 중복가입 경쟁 상태, 로그인 타이밍 사이드채널, 토큰 이중 파싱)를 모두 수정함.
- **다음 세션/사용자가 로컬에서 꼭 확인해야 함**: `gradle test` (특히 신규 `AuthControllerTest`), 그리고 실제 MySQL(Docker) 환경에서 `(social_provider, social_id)` unique 제약이 `ddl-auto: update`로 기존 테이블에 잘 적용되는지 (Hibernate의 update 모드는 기존 테이블에 제약을 추가하지 못할 수 있어, 안 되면 테이블 재생성 또는 수동 `ALTER TABLE` 필요할 수 있음).

### 다음 우선순위 제안 (갱신)
1. 사용자 로컬에서 `gradle test` 실행해 Step 3 인증 코드 검증
2. 카카오 → 구글 → 네이버 OAuth2 소셜 로그인 추가
3. (보류 중) 프론트 Frame 03 나머지 요소 + 위 "프론트엔드 작업 일시 중단" 이슈 해결 — 사용자가 재개 요청하면 진행

## 9. Step 3 후속 — User/Book API 인증 필수화 (2026-07-19)

소셜 로그인(카카오/구글/네이버) 연동은 사용자가 각 플랫폼 앱 등록(Client ID/Secret 발급)을 먼저 해야 진행 가능하다고 안내했고, 그 사이에 API 키가 필요 없는 작업부터 먼저 진행하기로 함 — "User/Book API가 인증 없이 열려 있는 문제"를 해결.

### 구현 내용
- `SecurityConfig`: `PUBLIC_PATHS`(`/health`, `/api/auth/signup`, `/api/auth/login`, `/swagger-ui/**`, `/swagger-ui.html`, `/v3/api-docs/**`, `/h2-console/**`)만 permitAll로 남기고, `/api/users/**`·`/api/books/**`를 포함한 나머지 전부를 `authenticated()`로 전환.
- `OpenApiConfig`: Swagger UI 우측 상단에 "Authorize" 버튼이 뜨도록 `bearerAuth` `SecurityScheme` 등록 — 로그인해서 받은 토큰을 넣으면 Swagger에서 바로 인증이 필요한 API를 테스트할 수 있음.
- `UserController`/`BookController`: Javadoc을 "인증 없이 검증용"에서 "인증 필요"로 갱신 (로직 변경 없음).
- `UserControllerTest`/`BookControllerTest`: 모든 MockMvc 요청에 `JwtProvider`로 발급한 토큰을 `Authorization: Bearer` 헤더로 첨부하도록 전면 수정 + "토큰 없이 요청하면 401" 케이스 각각 추가.

### 판단·결정 사항
1. 아직 리소스 소유자 검증(내 프로필/책만 수정 가능)은 없음 — "로그인만 되어 있으면" 다른 사람의 데이터도 조회/수정 가능한 상태. 이건 의도적으로 범위를 좁힌 것이며, 다음 단계에서 강화 필요 (코드 Javadoc에 명시해둠).
2. `POST /api/users`(관리자/테스트용 회원 생성 엔드포인트)도 함께 잠금 — 실제 회원가입은 `POST /api/auth/signup`을 쓰는 게 맞아서, 이 엔드포인트가 열려 있을 이유가 없어짐.

### 검증
- 이번에도 Gradle을 실행할 수 없어 리뷰 서브에이전트로 정적 검토(Spring Security 6.x DSL 문법, springdoc 기본 경로, h2-console 경로, 테스트의 JwtProvider 오토와이어링 등)만 한 번 더 수행 — 이슈 없음으로 확인됨.
- **다음 세션/사용자가 로컬에서 꼭 확인해야 함**: `gradle test` 통과 여부(특히 새로 추가된 "토큰_없이_요청하면_401" 케이스), 그리고 토큰 없이 `GET /api/users/1` 등을 호출하면 401이 나는지 실제로 한 번 확인.
- 이 작업 중 원격 저장소에 다른 세션(자동화 작업으로 추정, 커밋에 `Co-Authored-By: Claude Sonnet 5` 표기)이 TODO 리스트의 알라딘/카카오 API 키 발급 체크박스를 갱신한 커밋이 push되어 있는 걸 발견함 — 문서 파일이라 충돌 없이 merge했지만, 사용자가 끄셨다고 알고 있던 예약 작업이 여전히 뭔가를 커밋하고 있을 가능성이 있어 확인이 필요해 보임.

### 다음 우선순위 제안 (갱신)
1. 사용자 로컬에서 `gradle test` 실행해 이번 인증 필수화 변경 검증
2. 카카오 → 구글 → 네이버 OAuth2 소셜 로그인 추가 (사용자가 각 플랫폼 앱/키 등록 완료되는 대로)
3. 리소스 소유자 검증 강화 (본인 프로필/책만 수정 가능하도록)
4. (보류 중) 프론트 Frame 03 나머지 요소 + "프론트엔드 작업 일시 중단" 이슈 해결 — 사용자가 재개 요청하면 진행

## 10. 카카오/구글/네이버 소셜 로그인 코드 선반영 (2026-07-19)

사용자가 아직 카카오/구글/네이버 앱 등록(Client ID/Secret 발급)을 하지 않은 상태에서, "오늘 안에 `backend/.env` 파일을 만들어둘 테니 그걸 전제로 코드를 미리 만들어달라"고 요청해서 실제 로그인 연동 코드를 먼저 구현함.

### 설계
- **패턴**: 모바일 앱(React Native)이 각 제공자의 공식 SDK로 이미 로그인해서 토큰을 받아오고, 그 토큰을 백엔드로 넘기면 백엔드는 그 토큰이 진짜인지 제공자 API에 물어봐서 확인만 하는 방식. 카카오/네이버는 "access token"을 쓰고(`Authorization: Bearer` 로 사용자 정보 API 호출), 구글은 "ID token"을 쓰는 게 표준이라 방식이 다르다 — 각 API 실제 응답 형식은 웹 검색으로 확인 후 구현함(카카오 `v2/user/me`, 네이버 `v1/nid/me`, 구글 `tokeninfo?id_token=`).
- 구글은 `aud`(발급 대상) 클레임을 `GOOGLE_CLIENT_ID`와 반드시 비교해야, 다른 앱을 위해 발급된 구글 토큰으로 우리 백엔드에 로그인하는 걸 막을 수 있음 — 이 검증을 넣었고, `GoogleOAuthClientTest`로 단위 테스트까지 작성함.
- 구글의 `tokeninfo` 엔드포인트는 구글 공식 문서가 "개발/디버깅용, 프로덕션에서는 요청 제한 있음"이라고 명시함 — 지금은 MVP 단계라 이 방식으로 우선 구현했고, **실 사용자가 늘면 `google-api-client` 라이브러리 기반 서명(JWK) 검증으로 교체 필요** (TODO).

### 구현 내용
- `domain/auth/oauth2` 패키지(신규): `KakaoOAuthClient`/`GoogleOAuthClient`/`NaverOAuthClient`(각 제공자 사용자 정보 API 호출), `SocialUserInfo`(공통 결과 DTO), `KakaoOAuthProperties`/`GoogleOAuthProperties`/`NaverOAuthProperties`(`app.oauth2.*` 바인딩), `OAuth2Config`(타임아웃 5초 설정한 공용 `RestTemplate` 빈).
- `domain/auth/SocialAuthService`(신규): 제공자별 로그인 메서드 3개 → 공통 find-or-create 로직 → `AuthService.issueToken()` 재사용해서 우리 앱 JWT 발급 (이메일/PW와 동일한 토큰 포맷).
- `AuthController`에 `POST /api/auth/kakao`, `/google`, `/naver` 추가 (각각 `SocialAccessTokenRequest`/`GoogleIdTokenRequest` DTO).
- `SecurityConfig`의 `PUBLIC_PATHS`에 위 3개 경로 추가 (로그인 엔드포인트라 인증 없이 호출 가능해야 함).
- `User.forSocialLogin(nickname, socialProvider, socialId)` 팩토리 추가.
- **리팩터링**: `UserService`/`AuthService`에 중복돼 있던 "랜덤 닉네임 생성" 로직을 `RandomNicknameGenerator` 컴포넌트로 뽑아서 `SocialAuthService`까지 3곳에서 공유하도록 정리 (그대로 3번째 복사본을 만들지 않기 위함).
- `build.gradle.kts`: `gradle bootRun` 실행 시 `backend/.env` 파일(KEY=VALUE 형식)을 읽어 환경변수로 주입하는 `doFirst` 블록 추가 — 새 의존성 추가 없이 순수 Gradle 스크립트로 구현. `.env` 파일이 없으면 경고 로그만 남기고 정상 실행됨.
- `backend/.env.example` 추가 — `KAKAO_CLIENT_ID`, `KAKAO_CLIENT_SECRET`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` 자리 표시. `backend/.env`(실제 값)는 루트 `.gitignore`의 `.env` 패턴에 이미 걸려있어 커밋되지 않음.
- `application.yml`/테스트 `application.yml`에 `app.oauth2.kakao/google/naver.client-id`/`client-secret` 플레이스홀더 추가.
- `SocialAuthControllerTest` 추가 — 카카오/구글/네이버 OAuthClient를 `@MockBean`으로 대체해서 "제공자가 이런 사용자 정보를 돌려줬다"는 상황을 가정하고 회원가입/재로그인/토큰발급/인증실패/검증실패 시나리오 커버. `GoogleOAuthClientTest`는 aud 검증 로직만 따로 순수 단위 테스트로 커버.

### 판단·결정 사항
1. 백엔드 쪽에는 사실 Client Secret이 필요 없는 구조다(모바일 SDK가 이미 토큰을 받아오고, 백엔드는 그 토큰으로 사용자 정보만 조회하기 때문) — 그래도 `.env`에는 Secret까지 다 받아서 프로퍼티에 자리를 만들어뒀다. 나중에 "서버가 인가 코드를 직접 토큰으로 교환하는" 방식으로 바꾸면 필요해지기 때문.
2. 아직 온보딩(성별/관심분야 등 추가 정보 입력, Frame 01.1)은 붙이지 않았다 — 소셜 로그인 최초 가입 시 바로 로그인시키고, 추가 정보 입력 플로우는 프론트가 준비되면 별도로 붙일 것.
3. 프론트(React Native) 쪽 카카오/구글/네이버 SDK 연동은 하지 않음 — "frontend 작업 중단" 지시가 아직 유효하다고 보고 백엔드 코드만 준비함. 실제 엔드투엔드 테스트는 프론트에서 SDK로 토큰을 받아와야 가능하다.

### 검증
- 이번에도 Gradle 실행 불가 — 리뷰 서브에이전트가 정적 검토했고, 특히 `build.gradle.kts`의 `.env` 로딩 부분은 리뷰 에이전트가 별도의 임시 Gradle 프로젝트를 만들어 실제로 실행까지 해보고 문법이 유효함을 확인함. `RestTemplate.exchange`의 vararg/제네릭 오버로드 사용, Mockito의 `ParameterizedTypeReference` 제네릭 매처 문법도 확인함. 이슈 없음.
- **다음 세션/사용자가 로컬에서 꼭 확인해야 함**: `gradle test` (신규 `SocialAuthControllerTest`, `GoogleOAuthClientTest` 포함), 그리고 실제 카카오/구글/네이버 키를 `.env`에 넣고 `gradle bootRun`으로 띄운 뒤 Swagger나 Postman에서 실제 토큰으로 `/api/auth/kakao` 등을 호출해보는 것(단, 실제 토큰은 프론트에서 각 SDK로 로그인해야 얻을 수 있어서, 그 전까지는 Swagger 테스트도 불가능함).

### 다음 우선순위 제안 (갱신)
1. 사용자가 카카오/구글/네이버 앱 등록 + `backend/.env` 작성 완료되는 대로, 로컬에서 `gradle test` 실행해 이번 소셜 로그인 코드 검증
2. ~~리소스 소유자 검증 강화 (본인 프로필/책만 수정 가능하도록)~~ → 아래 11번에서 구현 완료
3. (보류 중) 프론트 Frame 03 나머지 요소 + 소셜 로그인 SDK 연동 + "프론트엔드 작업 일시 중단" 이슈 해결 — 사용자가 재개 요청하면 진행

## 11. 리소스 소유자 검증 강화 (2026-07-20)

로그인만 되어 있으면 다른 회원의 프로필/책도 수정·삭제할 수 있던 문제를 막기 위해, User/Book의 쓰기(수정/삭제) API에 "본인 소유 리소스인지" 검증을 추가함. 조회(단건/목록) API는 공유 앱 특성상 의도적으로 계속 열어둠 (다른 사람의 프로필/독서 기록을 구경하는 것 자체가 기능).

### 구현 내용
- `common/exception/ForbiddenException`(신규) + `GlobalExceptionHandler`에 403 매핑(`error: "FORBIDDEN"`) 추가 — 기존 404/401/409/400 예외 처리와 동일한 패턴.
- `UserService.update`/`delete`: `Long currentUserId` 파라미터 추가, `targetUserId.equals(currentUserId)`가 아니면 `ForbiddenException`. `create`/`getById`/`getAll`은 변경 없음.
- `BookService.create`: 요청 바디의 `userId`가 토큰 주체와 다르면 403 (남의 이름으로 책 등록 방지). `update`/`complete`/`delete`: 책을 먼저 조회한 뒤 `book.getUser().getId()`가 토큰 주체와 다르면 403. `getById`/`search`는 변경 없음.
- `UserController`/`BookController`: 위 메서드들에 `@AuthenticationPrincipal Long currentUserId` 파라미터 추가해서 서비스로 전달 (기존 `AuthController.me()`에서 이미 쓰던 패턴 재사용).
- `UserControllerTest`: 기존 "생성 후 조회/수정/삭제" 테스트가 생성된 리소스의 실제 소유자 토큰으로 수정/삭제하도록 보강 + 다른 사람 정보 수정/삭제 시도 시 403을 확인하는 신규 테스트 2개 추가.
- `BookControllerTest`: 다른 사람 명의로 책 등록 시도, 다른 사람 책을 수정/완독/삭제 시도 시 각각 403을 확인하는 신규 테스트 2개 추가. 기존 테스트는 그대로 통과(이미 소유자 토큰을 쓰고 있었음).

### 검증
- 이번에도 Gradle 실행 불가 — 리뷰 서브에이전트가 컴파일 정합성(다른 호출부 누락 여부), 로직(소유자 비교 방향, LAZY 연관관계에서 `@Transactional` 범위 내 `getUser().getId()` 안전성), 신규/기존 테스트 시나리오를 모두 검토했고 이슈 없음으로 확인됨.
- **다음 세션/사용자가 로컬에서 꼭 확인해야 함**: `gradle test` 통과 여부 (특히 신규 403 테스트 4개), 그리고 Swagger/Postman에서 다른 사용자 토큰으로 남의 프로필/책 수정을 시도하면 실제로 403이 나는지 한 번 확인.

### 다음 우선순위 제안 (갱신)
1. 사용자 로컬에서 `gradle test` 실행해 이번 소유자 검증 변경 검증
2. ~~Step 4. 이미지 업로드 (S3 presigned URL, BookPhoto API)~~ → 아래 12번에서 구현 완료
3. Step 5. 소감/공유 기능 (BookNote, Group, ShareRecord API)
4. (보류 중) 프론트 Frame 03 나머지 요소 + 소셜 로그인 SDK 연동 + "프론트엔드 작업 일시 중단" 이슈 해결 — 사용자가 재개 요청하면 진행

## 12. Step 4 — 이미지 업로드 (S3 presigned URL, BookPhoto API) (2026-07-20)

책 상세 페이지의 장소 사진(BookPhoto, 테이블정의서 기준) 업로드/조회/삭제 API를 구현함. 실제 사진 데이터는 이 서버를 거치지 않고 클라이언트가 S3에 직접 업로드하는 presigned URL 방식을 사용 — 서버는 업로드 URL 발급과 메타데이터(위치/좌표) 저장만 담당한다.

### 설계 — 2단계 업로드 흐름
1. `POST /api/books/{bookId}/photos/presigned-url` — 파일명/Content-Type을 보내면 S3에 직접 PUT할 수 있는 presigned URL(기본 10분 유효)과, 업로드 완료 후 접근 가능한 공개 URL(`imageUrl`)을 함께 발급받음.
2. 클라이언트가 그 URL로 S3에 직접 업로드.
3. `POST /api/books/{bookId}/photos` — 1번에서 받은 `imageUrl`(+ 위치/좌표)을 보내 실제 DB 레코드를 생성. presigned URL 발급만으로는 DB에 아무것도 기록되지 않음.

### 구현 내용
- `config/S3Properties`(`app.s3.*` 바인딩: bucket/region/accessKey/secretKey/presignedUrlExpirationSeconds) + `config/S3Config`(`S3Presigner` 빈 등록, `StaticCredentialsProvider`/`AwsBasicCredentials` 사용).
- `domain/bookphoto` 패키지(신규): `BookPhoto` 엔티티(테이블정의서 그대로 — image_url/location_text/latitude/longitude, updated_at은 없음), `BookPhotoRepository`, `BookPhotoService`, `BookPhotoController`.
- API: `POST /api/books/{bookId}/photos/presigned-url`(발급), `POST /api/books/{bookId}/photos`(등록), `GET /api/books/{bookId}/photos`(목록 조회), `DELETE /api/books/{bookId}/photos/{photoId}`(삭제).
- 등록/삭제는 본인 소유 책에 대해서만 가능(Step 3 소유자 검증과 동일한 `ForbiddenException`/403 패턴 재사용). 조회는 User/Book과 동일한 원칙으로 열어두되, `bookId` 자체가 URL 경로상 리소스 식별자라 존재하지 않는 책이면 404.
- S3 key는 원본 파일명을 그대로 쓰지 않고 확장자만 추출해서 `book-photos/{bookId}/{UUID}.{ext}` 형태로 생성 — 경로 조작(`../../`) 및 특수문자 문제 방지.
- Content-Type은 `image/*` 형식만 허용하도록 요청 DTO에 검증 추가 (임의 파일 업로드 방지).
- `build.gradle.kts`: `software.amazon.awssdk:bom:2.47.4` + `software.amazon.awssdk:s3` 추가 (S3Presigner는 실제 AWS 네트워크 호출 없이 로컬에서 서명만 계산하므로 별도 HTTP 클라이언트 의존성 불필요).
- `backend/.env.example`에 `AWS_S3_BUCKET`/`AWS_S3_REGION`/`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` 자리 추가.
- `BookPhotoControllerTest` 추가 — presigned URL 발급, 이미지가 아닌 Content-Type 거부(400), 등록/조회/삭제 전체 흐름, 존재하지 않는 책 조회 시 404, 다른 사람 책에 등록/삭제 시도 시 403, 토큰 없이 요청 시 401 커버. **S3Presigner는 실제 AWS 호출 없이 로컬 서명만 하므로 `@MockBean` 없이 테스트 전용 더미 자격증명(`src/test/resources/application.yml`)으로 실제 SDK 로직까지 검증함.**

### 판단·결정 사항
1. presigned URL이 가리키는 공개 URL은 표준 virtual-hosted-style(`https://{bucket}.s3.{region}.amazonaws.com/{key}`)로 계산해서 응답에 포함 — 버킷이 퍼블릭 읽기이거나 앞단에 CDN을 두는 것을 전제로 함. 버킷을 private로 유지하기로 하면 이 URL 생성 로직을 CloudFront 서명 URL 등으로 교체해야 함(Step 5-2 공유 카드/공개 웹뷰 설계 시 재검토 필요, 코드에 TODO 남김).
2. IAM 사용자는 해당 버킷에 대한 `s3:PutObject` 권한만 있으면 충분(최소 권한) — `.env.example`에 안내 문구 추가.
3. accessKey/secretKey가 비어 있어도(.env 작성 전) 서버 기동 자체는 정상 — 실제 presigned URL 발급을 호출하는 시점에만 실패함 (OAuth2 소셜 로그인과 동일한 완화 원칙).

### 검증
- 이번에도 Gradle 실행 불가 — 리뷰 서브에이전트가 AWS SDK v2 공식 Javadoc을 직접 웹에서 확인해 `S3Presigner`/`PutObjectPresignRequest`/`PresignedPutObjectRequest`/`PutObjectRequest` API 사용이 실제 시그니처와 일치하는지, Gradle BOM 문법, YAML 프로퍼티 바인딩(kebab-case ↔ record 필드), 빈 자격증명으로 기동 시 안전성, 소유자 검증 로직, 신규 테스트 시나리오를 모두 검토했고 이슈 없음으로 확인됨.
- **다음 세션/사용자가 로컬에서 꼭 확인해야 함**: `gradle test`(신규 `BookPhotoControllerTest`), 그리고 실제 S3 버킷 생성 + IAM 키를 `.env`에 넣고 `gradle bootRun` 후 Swagger에서 presigned URL을 발급받아 실제로 그 URL에 사진을 PUT 업로드해보는 엔드투엔드 확인 (지금까지는 로컬 서명 로직만 검증됐고, 실제 AWS 왕복은 검증 전).

### 다음 우선순위 제안 (갱신)
1. 사용자가 AWS 계정/S3 버킷/IAM 키 준비 + `backend/.env`에 값 채우는 대로, 로컬에서 `gradle test` + 실제 업로드 엔드투엔드 확인
2. Step 5. 소감/공유 기능 (BookNote, Group, ShareRecord API)
3. (보류 중) 프론트 Frame 03 나머지 요소 + 소셜 로그인 SDK 연동 + "프론트엔드 작업 일시 중단" 이슈 해결 — 사용자가 재개 요청하면 진행
